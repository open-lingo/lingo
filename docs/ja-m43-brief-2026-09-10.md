# JA m43 dispatch brief — n4-14 "Certainty ladder: かもしれない, はず, でしょう-deepen" (2026-09-10)

**For:** ONE fresh Sonnet agent, authoring m43 lesson-by-lesson, unattended.
**Do:** author IR, compile, register, gate, ship-ready. **Don't:** wait for
sign-off — infer, decide, record reasoning inline in the IR `notes:` block,
proceed. **m42 (n4-13, hearsay そうだ/って/らしい) is dispatched to a
concurrent reviewer and had not been confirmed final when this brief was
written** — `ir/m42.ir.yaml` is being read, never edited, by this brief.
Snapshot 2026-09-10: live registry state reads as **m42 fully landed** —
`frequencyAtoms.ts`'s `JA_FREQ_LAST_MODULE = 42`, `taughtVocab.ts`'s
`IR_BY_MODULE` spans through m42, `mockCourse.ts`'s JA `n4` array carries
all 12 `ja-m42-neo-*` tiles through `ja-m42-neo-challenge`, `mockLessons.ts`
imports `M42_NEO_LESSONS`, and `ir/m42.ir.json` is compiled — but the
reviewer may still adjust m42's atom list or lesson text before it is
truly frozen. Treat every "m42 shipped X" fact below as **"expected from
m42,"** re-verify against the live `ir/m42.ir.yaml` (read-only) before
starting.

**Spec source:** `docs/spine-n4.md`, module `n4-14` (search that string,
line ~597). Cross-refs: §0 module map (line ~40, confirms m43 is the very
next beat after m42's n4-13 in the module order — the N4 spine is nowhere
near exhausted, m43 of 51, so there is no N3-fallback question here);
§1.2's corrections table (line ~85, items 2 and 4 — かもしれない and はず
placement rulings); §1.3's family table (line ~126, "Epistemic" row); §4's
`thr-n4` thread (line ~1024, glue-adverb drip and the cross-tier-deepens
line naming でしょう at m43 explicitly).

---

## 0. What m43 teaches (from spine-n4.md, corrected below)

- **ONE dimension, four rungs, assembled as a ladder** (spine's own framing):
  - **かもしれない** (possible) — epistemic modality, NOT conditional-adjacent
    despite the surface resemblance to たら/ば (spine §1.2 item 2 explicitly
    corrects this misfiling). New grammar point.
  - **でしょう/だろう** (probable) — a DEEPEN of m25's conjecture module, not
    new content. でしょう is production-legal (m25 precedent); **だろう stays
    RECOGNITION-ONLY** — spine's own words: "だろう stays recognition + 何だろう
    self-talk; that ruling holds here." This is a firm carry-forward
    constraint, not a suggestion — do not promote だろう to a graded
    production target in m43.
  - **はず** (expected, from evidence) — the top of the graded ladder; spine
    §1.2 item 4: "correctly N4, but not conditional and not evidential — it
    is expectation-from-evidence." New grammar point. Its two production
    spends are **はずだった** ("it was supposed to…") and **はずがない**
    ("no way") — per spine, these are "the two spends that make はず worth
    teaching," not optional flourishes.
  - **にちがいない** (certain) — **RECOGNITION ONLY** (spine §1.3 table marks
    it explicitly `(recognition)`). Never a build/cloze/production target.
  - **かも** as a casual sentence-ender — production, register-cued, per
    spine's own teaches list.
- **Introductions are PAIRWISE, the ladder is a REVIEW object**: spine's own
  words — "かもしれない vs でしょう, then はず vs でしょう); the four-rung
  ladder is a REVIEW object, not a teaching one." Do not open the module
  with a labeled four-way table (interleave-dont-block-teach doctrine,
  reinforced here by the spine's own explicit sequencing instruction).
- **Placement rationale** (why m43 sits directly after m42, not elsewhere):
  spine's own `why` — "Placed one module after hearsay so the two questions
  a listener actually asks — 'how do you know?' (evidential, m42) and 'how
  sure are you?' (epistemic, m43) — sit adjacent and get explicitly
  distinguished, which is the confusion N4 learners actually report." This
  confirms m43 is the correct next module against what m39–m42 actually
  shipped (evidential family: そう m36, そうだ/って/らしい m42; epistemic
  family starts fresh at m43) — no ambiguity, no N3 fallback needed.
- **Domain vocabulary**: promises/predictions/confidence register —
  spine `vocab.must`: やくそく, たしか, かならず, ぜったい, きっと,
  もしかしたら; `vocab.prefer`: よそう, けっか, せいこう, しっぱい, うたがう,
  しんじる, あんしん, しんぱい; `vocab.sidequest`: かくじつ, みこみ.
  `vocab.count: 32` (spine ceiling — non-binding per every prior N4
  module's own corrected brief, all of which landed well under).
- `register: plain` (N4 doctrine). `wave: 4`. `spiralWith: "n4-15"` — this is
  a **forward** reference to m44 (ようだ/みたい, unbuilt), the same class as
  m40's forward-only `spiralWith` to n4-16 that m41's brief flagged: nothing
  to realize concretely in m43's own reviews (unlike m41's backward pull
  from m33), because m44 does not exist yet. Leave the concrete
  realization to m44's own author when it deepens n4-13/n4-07/n4-14.

---

## 1. Corrections to the spine's own numbers (verified against live repo)

- **Four of the spine's six `must`/`prefer` words are already taught —
  do not re-register them:**
  - **でしょう** — `id: "deshou"`, `fromModule: "m25"`,
    `introducedByLessonId: "ja-m25-neo-1"` (live, not a kata pointer),
    `blocked: true`. Already taught. m43's job is a DEEPEN, referencing
    this id, never re-teaching it from scratch.
  - **だろう** — `id: "darou"`, `fromModule: "m25"`,
    `introducedByLessonId: "ja-m25-neo-9"` (live), `blocked: true`, note:
    "recognition only — sentence-final だろう to a listener is blunt/
    masculine (spine n13)." Already taught, already recognition-only —
    m43 inherits this ruling unchanged (§0).
  - **きっと** — `id: "kitto"`, `fromModule: "m25"`,
    `introducedByLessonId: "ja-m25-neo-5"` (live), `blocked: true`, note:
    "abstract modal adverb — no honest emoji." Already taught at m25 —
    **and already wired into a grammar point**: `ir/m25.ir.yaml` registers
    a `grammarPointId: tabun-kitto` (line ~528). The spine's m43 `must`
    list is wrong to imply this is new vocabulary; use as a carrier only,
    and reference `tabun-kitto` by id if a probability-adverb aside is
    useful in the でしょう-deepen lesson.
  - **けっか** — `id: "kekka"`, `fromModule: "m30"`, `blocked: true`,
    kind: noun. Already taught at m30 (no `introducedByLessonId` set, but
    no kata-lesson trap either — never claimed a lesson pointer at all).
    Carrier only.
  - **All four carry LIVE `introducedByLessonId` values or no pointer at
    all — none of the four is the ニュース-trap** (a static
    `introducedByLessonId` pointing at a retired `ja-mN-kata` lesson). Two
    of the four (でしょう, だろう) point at real `ja-m25-neo-*` ids;
    checked directly, neither resolves to a kata lesson.
- **ぜったい is a reusable stub, not new content and not a "carrier only"
  word either — a genuine cash-in, the same class as m41's ならぶ/ならべる
  and m42's `p-tte`.** `id: "zettai"`, kana ぜったい, kanji 絶対, kind: vocab,
  `fromModule: "thr-n4"`, `blocked: true`, note: "modal adverb — thr-n4
  glue-adverb drip," **no `introducedByLessonId` set** — genuinely
  unallocated, unlike でしょう/だろう/きっと/けっか above. `docs/spine-n4.md`
  §4's `thr-n4` thread lists it as part of the ongoing glue-adverb drip
  (やっぱり/もちろん/べつに/なるべく/ぜひ/さっそく/どうしても/なんとか — the
  thread's own 8-word list doesn't name ぜったい explicitly, but the
  courseAtoms note ties it to the same drip mechanism, and the spine's own
  m43 `must` list independently wants it). **m43's determination: flip
  `fromModule` from `"thr-n4"` to `"m43"`, reuse `id: "zettai"`, do not
  create a duplicate id** — the same reuse discipline m41 used for
  ならぶ/ならべる.
- **Genuinely new vocab, confirmed by direct grep — no existing
  `courseAtoms.ts` row for any of these** (checked by both `id:` guess and
  `kana:` string, 2026-09-10): やくそく (約束), たしか (確か), かならず
  (必ず), もしかしたら, よそう (予想), せいこう (成功), しっぱい (失敗),
  うたがう (疑う), しんじる (信じる), あんしん (安心), しんぱい (心配),
  かくじつ (確実), みこみ (見込み). かもしれない, はず, にちがいない, かも
  also confirmed absent — clean, no stub, no collision.
- **`ja-core6k-order.json` freqRanks** (pulled 2026-09-10, by reading):
  やくそく 941, かならず 884, たしか 1188, きっと 1066 (already taught),
  あんしん 734, しんぱい 782, けっか 1113 (already taught), しんじる 1126,
  せいこう 1228, かくじつ 2749, しっぱい 2813 — all reasonably
  high-to-mid frequency. **Not found in the top-3000**: ぜったい,
  もしかしたら, よそう, うたがう, みこみ, かも, にちがいない. This is
  **not a straightforward "cut these" signal the way m42's ひなん/けいほう
  reading was** — several of the absent words are grammatical/modal items
  (かも, にちがいない, もしかしたら) whose absence from a noun-weighted
  frequency list is expected and uninformative, not a sign of low value;
  they are core to the grammar point itself. The genuinely low-frequency
  CONTENT words among the absentees — うたがう, みこみ — are the honest
  sidequest/first-cut candidates, together with spine's own sidequest tier
  みこみ and the near-threshold かくじつ (rank 2749, present but weak).
- **Two near-synonym pairs worth a pedagogy note, neither a token
  collision:**
  1. **あんしん (安心, "peace of mind/relief") vs あんぜん (安全, "safe,"
     `id: "anzen"`, `fromModule: "m36"`, blocked) — different kana strings,
     distinct map keys, no `atomIndex()` collision.** But semantically
     adjacent (both translate loosely to "safe/safety" in a rushed gloss) —
     disambiguate by collocation (あんしんする as a felt psychological
     state vs あんぜん(な) as an objective property of a place/situation),
     the same class of care m41 gave じゅんび/したく.
  2. **たしか (確か, "for sure/certain") shares its kanji root (確) with
     the unregistered verb たしかめる (確かめる, "to confirm/check").** No
     current collision — たしかめる has no `courseAtoms` row — but flag for
     whichever future module registers it: don't let a `derivedFrom` chain
     accidentally point たしかめる at the `tashika` id, they are different
     parts of speech (adjective/adverb vs verb) sharing a root, not a
     conjugation pair.

---

## 2. Engineering: does the compiler/engine already support this ladder?

**Verified 2026-09-10 — NO new engine work is required, matching m30/m38/
m41/m42's precedent exactly, NOT m40's.**

- `conjugationEngine.ts`'s `ChainForm` type (read directly, full list:
  `masu | masu-neg | masu-past | masu-past-neg | te | ta | nai | tai |
  nai-past | tai-neg | tai-past | tai-neg-past | volitional | ba |
  potential | tara | imperative | prohibitive | causative | passive`) has
  no `kamoshirenai`/`hazu`/`deshou`/`darou` member. Nothing needs adding:
  かもしれない, はず, でしょう, だろう, にちがいない all attach to the
  plain/dictionary form (already a live surface since early N5), not a new
  conjugation of the base verb/adjective.
- `TRANSFORM_RULESETS` (`ja/conjugation/transformRulesets.ts`, confirmed by
  direct grep 2026-09-10) has exactly 6 entries — `nai, masu, te,
  volitional, ba, passive`. `docs/authoring-invariants-pinned.md` inv 49's
  own closed definitional list of what REQUIRES a ruleset is て, た, ない,
  ます, potential, passive られる, causative させる, volitional よう/おう,
  ば, 命令形/禁止形 — かもしれない/はず/でしょう/にちがいない are not in
  it, matching m30/m38/m41/m42's compositional precedent.
- **Net determination: m43 needs NO changes to `ChainForm`, NO new
  `TRANSFORM_RULESETS` entry.** Author `kind: rule` grammar cards for each
  rung (mirroring m25's own `deshou`/`deshou-adjective`/`deshou-verb`/
  `darou` cards), no engine involvement.
- **m25 already has a rich set of live grammarPointIds to reference, not
  reinvent** (confirmed by direct read of `ir/m25.ir.yaml`): `deshou`,
  `deshou-adjective`, `deshou-verb`, `deshou-casual`, `darou`,
  `kana-wondering`, `tabun-kitto`. m43's でしょう-deepen beat should cite
  these by id in `priorGrammarPoints` and open with the spine's own
  prescribed "60s rehash of the intro" (thr-n4 thread rule for all deepen
  beats), not re-derive them.

---

## 3. Grammar points (IR-local ids, inv 42-compliant)

Reference existing registry/prior-module ids where the concept is a
continuation (inv 42), invent new ids only for genuinely new N4 concepts:

- **`kamoshirenai`** (new) — plain-form + かもしれない, epistemic
  possibility. Contrasted PAIRWISE against `deshou` (m25, id-referenced) by
  strength, not taught as a standalone island. かも as the casual
  sentence-ending truncation rides the SAME grammar point — see §5's firm
  ruling against registering かも as its own lemma atom.
- **`hazu`** (new) — plain-form + はず, expectation from evidence.
  Contrasted PAIRWISE against `deshou` by evidential basis (はず requires a
  reason the speaker can point at; でしょう doesn't). Covers はずだった and
  はずがない as the point's two required production spends (§0) — whether
  these need their own sub-ids (mirroring m25's `deshou`/`deshou-adjective`/
  `deshou-verb` split) or stay inside one `hazu` card with two example rows
  is an authoring judgment call, UNVERIFIED which is cleaner until the
  actual sentences are drafted — record the choice in the IR `notes:`.
- **`certainty-ladder`** (new) — the REVIEW-object synthesis point: all
  four rungs (かもしれない / でしょう·だろう / はず / にちがいない)
  assembled as one ordered scale, matching spine's explicit instruction
  that this is a review object, never a teaching one. Should not introduce
  any grammar not already covered by `kamoshirenai`/`hazu`/`deshou`/
  `darou` — it is an organizing card, not new content. Analogous to m16's
  tense-paradigm synthesis card (referenced by n4-15's own spine text at
  m44) and to m42's explicit deferral of its own そう-vs-そう synthesis to
  m51 — m43's ladder synthesis happens INSIDE m43 (unlike m42's cross-
  module deferral), because the spine places it here, not at a capstone.
- **にちがいない** — RECOGNITION ONLY (§0); if authored, cite it inside the
  `certainty-ladder` card's prose/example set, never as its own graded
  `kind: rule` beat with a production spend, matching the spine's explicit
  scope limit.

Reference existing owned ids via `priorGrammarPoints`, not by reinventing:
`deshou`, `deshou-adjective`, `deshou-verb`, `deshou-casual`, `darou`,
`kana-wondering`, `tabun-kitto` (all m25, confirmed live in
`ir/m25.ir.yaml`).

---

## 4. Lesson-by-lesson plan (12 lessons, `ja-m43-neo-<n>`, interleaved
per thr-n4 doctrine — pairwise introductions, ladder as review object only)

| # | id | role | focus | new grammar | new atoms |
|---|----|------|-------|--------------|-----------|
| 1 | neo-1 | teach | promises/plans domain intro | — | やくそく, たしか, かならず |
| 2 | neo-2 | teach | かもしれない (possibility), light pairwise contrast vs でしょう recall | kamoshirenai | もしかしたら, ぜったい (stub flip) |
| 3 | neo-3 | teach | でしょう-deepen: 60s rehash open (thr-n4 rule), だろう STAYS recognition-only, たぶん・きっと reuse | (deepen — references deshou/darou/tabun-kitto by id) | せいこう, しっぱい |
| 4 | review (early third) | THIS MODULE ONLY | かもしれない + でしょう-deepen | — | — |
| 5 | neo-4 | teach | はず (expectation from evidence), pairwise vs でしょう on evidential basis | hazu | よそう, けっか (carrier) |
| 6 | neo-5 | teach | はずだった / はずがない production spends | hazu (deepen) | うたがう, しんじる |
| 7 | neo-6 | teach | domain integration + にちがいない recognition aside (never graded) | — | あんしん, しんぱい |
| 8 | review (mid third) | THIS MODULE ONLY | かもしれない + でしょう-deepen + はず | — | — |
| 9 | neo-7 | teach | `dialogue_sim`: mixed-certainty conversation (someone hedging, someone sure) | — | かくじつ (or sidequest cut) |
| 10 | neo-8 | teach | pre-review integration step: assemble the four-rung `certainty-ladder` as a REVIEW OBJECT (inv 26 shape, no new teaching) | certainty-ladder | みこみ (or sidequest cut) |
| 11 | review (late third) | THIS MODULE ONLY | full-module review, ladder in reviewPool | — | — |
| 12 | challenge | challenge, LAST | combine ≥3 grammar points (inv 26), novel shape not seen elsewhere in-module | — | — |

Interleaving notes: かもしれない (neo-2) and はず (neo-4/neo-5) sit two
teaching lessons apart, split by the でしょう-deepen (neo-3) and review-1 —
mirrors m41's and m42's pattern of never placing the two "confusable"
mechanisms back to back. だろう is never promoted past recognition anywhere
in this table (§0/§2, firm). にちがいない never gets its own teach slot —
recognition-only per spine, folded into neo-6's aside and neo-8's ladder
card.

---

## 5. Done-criteria

**Per lesson:**
- Every particle cloze avoids が/を/は (inv 5 — the placement ratchet's
  grace window is same-module only, and は/が/を were introduced m3–m7,
  decades outside it by m43; use semantic verb-form/word clozes instead).
- **No standalone かも atom registered or exercised as its own lemma
  anywhere in the module** — bound-morpheme hazard (inv 41, the same class
  m42 flagged for って); grep the compiled `ir/m43.ir.json` for a free-
  standing `kana: "かも"` entry before considering any lesson done; fail if
  found. Fuse it into the `kamoshirenai` point as a derived casual-ending
  variant, or as a `kind: rule` card example, never a separate atom row.
- **だろう never appears as a graded production target** — spot-check every
  `mode: build`/`particle-cloze`/`translate` step tagged with `darou`'s
  kana; recognition-only surfaces (MCQ, listening-comp) are fine.
- **にちがいない never appears as a graded production target** — same
  check, recognition-only per spine §0/§3.
- Filler/listening-comp sentences do not duplicate authored listening-comp
  lines elsewhere in the module (standing constraint).
- Every content word decomposes into atoms already taught by that lesson's
  position (comprehensibility-gating, CLAUDE.md).

**Per module:**
- 12 lessons: 8 teaching + 3 review (beginning/middle/end thirds, THIS
  MODULE ONLY per inv 25) + 1 challenge (last).
- Every teaching lesson's pre-review integration step AND the final
  challenge combine ≥3 grammar points in a shape not repeated elsewhere in
  the module (inv 26).
- Atom budget ~11–13 net-new (well under the spine's `count: 32`, matching
  every prior N4 module's pattern — see §1's freqRank-driven note for which
  sidequest words to cut first if trimming is needed: みこみ first, then
  かくじつ).
- `node scripts/compile-ir.mjs m43` compiles clean; all 5 registration
  touch points updated (taughtVocab.ts, frequencyAtoms.ts → bump
  `JA_FREQ_LAST_MODULE` to 43, mockCourse.ts, mockLessons.ts →
  `M43_NEO_LESSONS`, courseAtoms.ts); `npm run module-gate -- m43` passes.
- **`MAX_GRADED_BUT_NEVER_WRITES` (currently 79, `atomExposureAudit.test.ts`)
  is expected to rise by approximately +2** — by precedent, not a verified
  count (the IR doesn't exist yet at brief-writing time): m43's two new
  VERBS, うたがう and しんじる, are the atoms most likely to land exclusively
  on same-module-gated graded steps the way m42's つたえる/しらせる did
  (both verbs, both +2, while m42's noun/adverb atoms all picked up a
  cross-step write within the module's own review/challenge lessons). The
  m43 author must re-run `EXPOSURE_REPORT=1 npx vitest run
  .../atomExposureAudit.test.ts` after landing, record the REAL delta, and
  bump the constant with a dated comment in the same style as the m41/m42
  entries — do not quote this brief's +2 estimate as verified.
- Whole-course tile diff is NOT required here (unlike m42's って stub
  cash-in) — none of m43's reused atoms are bound-morpheme collision risks
  once かも is kept off the atom registry per §5's firm rule above.

---

## 6. Explicitly rejected approaches (so you don't re-derive and re-reject them)

- **A block lesson that lists all four certainty rungs as a labeled table
  up front** — rejected; the spine explicitly states the ladder is a
  REVIEW object, not a teaching one, and mandates pairwise introduction
  (§0). Matches interleave-dont-block-teach doctrine and every prior N4
  module's own rejection of the equivalent move.
- **Promoting だろう to a graded production target in the deepen** —
  rejected; the spine explicitly carries forward m25's recognition-only
  ruling ("that ruling holds here"), citing register bluntness as the
  reason. Not this brief's call to relax.
- **Registering にちがいない as a gradable `kind: rule` beat with its own
  production spend** — rejected; spine §1.3 marks it `(recognition)`
  explicitly. Cite it in the ladder synthesis card's prose only.
- **Registering かも as a standalone lemma atom** — rejected; bound-morpheme
  hazard, same class as inv 41's くん/さま/ちゃん warning and m42's って
  caution. Fuse into `kamoshirenai` as a derived casual variant.
- **A `kind: contrast` beat type for かもしれない-vs-でしょう or
  はず-vs-でしょう discrimination** — rejected for the same reason m39–m42
  each rejected it: proposed, not shipped in `moduleCompiler.ts`
  (`docs/todo-transfer-diagram-rollout.md`). Use particle-cloze/build
  mechanisms (semantic, not particle-choice, per §5) instead.
- **Re-registering でしょう/だろう/きっと/けっか as new atoms** — rejected,
  see §1; all four already taught (m25 ×3, m30 ×1). Use as carriers only.
- **Realizing `spiralWith: "n4-15"` concretely in m43's own review pools**
  — rejected/deferred; n4-15 is m44, unbuilt at brief-writing time, a
  forward-only spiralWith in the same class as m40's forward reference to
  n4-16 (flagged in m41's brief). Nothing to pull from a module that
  doesn't exist yet; leave the concrete realization to m44.
- **Treating `vocab.count: 32` as a hard quota** — rejected, see §1; every
  prior N4 module has landed well under its spine-stated ceiling, and two
  of the spine's own sidequest/lower-frequency words (みこみ, かくじつ) are
  reasonable first cuts if the budget needs trimming.

---

## READY TO DISPATCH: yes, with three carried-forward flags (none block dispatch)

1. **m42 must be checked fresh, not assumed.** Live registry state reads as
   "m42 fully landed" (§ intro), but the brief-writing session was told a
   reviewer might still be mid-edit on `ir/m42.ir.yaml`. Re-run
   `node scripts/authoring-context.mjs m43` at the start of m43 authoring
   and treat any divergence from this brief (m42's final atom list, exact
   grammar-point ids, `JA_FREQ_LAST_MODULE`'s value) as the live repo
   winning over this document.
2. **The `hazu` sub-id split (single card vs `hazu`/`hazu-adjective`/
   `hazu-verb`-style split, mirroring `deshou`'s own m25 precedent) is
   UNVERIFIED and left as the author's judgment call** (§3) — record the
   choice and reasoning in the IR `notes:` block, not something this brief
   could settle without drafting the actual example sentences.
3. **The `MAX_GRADED_BUT_NEVER_WRITES` +2 delta (§5) is an ESTIMATE by
   precedent from m42's own landing, not a verified count** — the actual
   IR doesn't exist yet. The author must re-run the exposure audit after
   landing and record the real number, per [[pipeline-judgment-in-inventory]]
   doctrine: never quote a pass rate (or a ratchet delta) unread.

**Atom count:** ~11-13 genuinely new (やくそく, たしか, かならず,
もしかしたら, よそう, せいこう, しっぱい, うたがう, しんじる, あんしん,
しんぱい, plus かくじつ/みこみ as sidequest-tier, first cuts if tight) + 1
stub cash-in (ぜったい, flip `fromModule` "thr-n4"→"m43") + 4 already-taught
carrier-only words not to be re-registered (でしょう m25, だろう m25, きっと
m25, けっか m30) + 3 new IR-local grammar-point ids (kamoshirenai, hazu,
certainty-ladder) + 0 new `TRANSFORM_RULESETS` entries (§2 — confirmed
unnecessary) + 0 bare-かも atoms and 0 graded にちがいない instances (firm
exclusions, §3/§5/§6).
