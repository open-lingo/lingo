# PT m1 QA walk — 2026-09-18 (lane PTQA)

Walked PT m1 (6 lessons, 42 atoms, 105 clips) as Cris would. Dev-bypass +
local `ptBeta.enabled:true` + dev identity allow-listed (not committed).

## Fixed inline (plumbing, tests added)

1. **content:emit wrote 0 module files for pt.** `PT_ALL_LESSONS` was never
   spread into `lessonRegistry.eager.ts` (unlike SPANISH/FRENCH_LESSONS) —
   every `pt-m1-*` id unregistered, emitter skipped pt entirely (index had
   6 lessons, no module JSON). Fixed; now writes `pt/m1.<hash>.json`;
   manifest version legitimately changes (expected, not a regression).
2. **`/pt/qa/m1` stuck on false "lesson complete."** `ProtoModuleWalker`'s
   build effect fires before pt's JSON arrives (es/fr's is already hot)
   and never re-runs. Fixed: `PtQaM1Page.tsx` holds the walker unmounted
   until content is ready. Verified live (was stuck even after manual
   restart; now real steps render).
3. **Every PT audio button 404'd in dev.** The 105 staged clips live in
   `tts-publish/pt/`; the dev TTS middleware never looked there. Added it
   as a candidate. Verified: clip 404→200; flashcard review: 21
   errors/404s → 0 errors, 7×200.

## What works

- **Flashcards**: 7 L1 Track-A vocab cards (particles correctly excluded).
  Front=word/back=gloss, emoji atoms show an image, audio 200 on all 7.
  Graded all 7 "Knew it"; FSRS due-count persisted across reload.
- **All 6 lessons, 87 steps**: swept every `?step=N` at 390×844 on the real
  route — 0 console/nav errors, 0 overflow>2px, 0 non-200 TTS post-fix.
  Spot-checked build_sentence, both dialogue_sims, L6's last step visually.
- **Simulator**: `sim-proof.sh` PASS 100/125% for L1's build step and L6's
  sim step (one-shot; build mode doesn't apply to non-build steps). Both
  needed `--allow-fallback-font` (the probe's Noto-Sans-JP check is
  JA-specific, hard-fails any Latin-script language — tooling quirk, not
  fixed).
- **Course map** (`/pt/learn/course`): correct — M1 "Basics," 6 lessons, 42
  words, real vocab samples.
- **Switch-language visibility**: not clicked through (no header selector
  found headless, time-boxed); verified via the allow-list unit suite
  (26/26 green) + `/pt/*` rendering with no redirect.

## NOT fixed — listed only (too large for this lane)

- **Home's "Continue" card shows a fake course** — HIGHEST severity,
  Cris's actual first-load screen: "Portuguese for Beginners / Basics /
  Learn the Portuguese Alphabet / Lesson 1 of 3." `useHomeVariantData.ts`
  calls `getMockCourse(language.id)` directly; `mockCourse.ts` has an
  es/fr branch but **no pt branch**, falls to the generic 8-fake-lesson
  placeholder. `CourseMapPage.tsx` calls the same function but is spared
  for lesson-count/vocab (reads the real content index separately) — its
  module title still leaks "Basics" not "Eu sou Sam." `DevPanel.tsx`'s
  "mark up to module" bypass has the identical bug. Fix needs a `pt`
  branch in `mockCourse.ts` (risks the import cycle `pt/curriculum/
  index.ts` was built to avoid) or auditing ~15 `getMockCourse` call
  sites — real design work, not a 10-line patch.
- **`WordMapStepView.tsx` hardcodes "Tap the Spanish..."** regardless of
  language — misfires for PT and already for FR; pre-existing, wider scope.

Numbers: 1 module, 6 lessons, 42 atoms, 105 clips, 87 steps swept, 7
flashcards, sim-proof PASS×2.
