# 2026-09-15 — TestFlight feedback, builds 14/15 (Spencer) — research doc

Pulled 2026-09-15 21:00 MDT via the App Store Connect API. Crashes: **0**.
Screenshot items: **26** (API rows 117–142 → items **#118–#143**; #118 on
build 14, the rest on build 15; 01:59Z–07:41Z, iPhone 15 Pro Max). Shots at
`2026-09-15-testflight-shots/<item>.jpg`.

Purpose: a working sheet for Fable + Spencer to walk the list. Every item has
the located step (file:line), local + prod links, an answer or root cause, and
a decision flag. Per-item detail (grep evidence, similar-issue counts) is in
§3, produced by two Sonnet agents from the JA scope brief. Cross-session
recurrence analysis is in `2026-09-15-recurring-complaints-rca.md`.

Where he was, in order: m11 review lesson `ja-m11-neo-review-3` (#118–#122),
m32 test-out (#123–#125), m33 test-out (#126–#129), m34 lessons
`ja-m34-neo-1` (#130–#135) and `ja-m34-neo-2` (#136–#143).

Local links assume the dev server (`VITE_DEV_AUTH_BYPASS=true npx vite --port
5399`); `?step=N` is 0-indexed; review lessons carry a dynamic prefix so the
static index shifts. Prod = same path on `https://app.openlingoapp.com`.

## 1. Table

| # | Where | Class | Finding (one line) | Fix / effort | Spencer? |
|---|---|---|---|---|---|
| 118 | m11-neo-12 step 10, build | question | たべる takes an object but Japanese drops it freely; this sentence already names ごはん. House-style preference, not a bug. | none | N |
| 119 | m11-review-3 beat 0, build | sizing/furigana | 今 (distractor) looks squat: hidden reading collapses but the glyph never grows into the freed band. = the build-16 "no visible reading → big word" to-do. | S, BuildTileSurface + index.css | N (already agreed) |
| 120 | m11-review-3 beat 10, listen-build | question | とけい never surfaces 時計: 計 is outside the N5 catalog (`applyKanjiSurfaces.ts:145`). Working as designed; no re-test needed. | none, or widen catalog | Y: keep N5-only? |
| 121 | m11-review-3 beat 11, build | grading gap | は is optional after きょねん; leniency list `TEMPORALS` / `BARE_TEMPORALS` never got m11's later time words (きょねん ことし らいげつ せんしゅう…). His answer should pass. 2 steps affected. | S | N |
| 122 | m11-review-3 beat 2 (review prefix), build | grading gap | うたを いつ きいた is valid; `isMovable()` (`jaAcceptedForms.ts:395`) has no wh-word class so zero variants generate. Same for どこ/なに/だれ. | M + tests | Y: loosen wh-word order globally? |
| 123 | m32 test-out 3/12 | progress/state | Test-out marks only the tested module's lessons complete (`applyPlacement.ts:96`); "resume" goes to the first incomplete earlier module. Needs his module history to tell design from bug. | M–L | Y: which modules were incomplete? |
| 124 | m32 test-out 5/12, listen-build | sizing + kanji | (a) `ListeningBuildStepView.tsx:345` has fixed sizes, never got the dense/bigTiles system. (b) 書ける shown for かける "to call": homograph guard only fires when the colliding atom has a kanji field. 2 more pairs at risk (かえる/帰る, つける/付ける). | M + S | N |
| 125 | m32 test-out 6/12, listen-build | sizing | Bank `gap-3` vs tray `gap-2.5` in the same file (`:331` vs `:294`). | S, with #124 | N |
| 126 | m33 test-out 2/12 | audio | All audio gone for the rest of the session. Best hypothesis: iOS AudioContext parked "interrupted" (memory `ios-audio-session`). Not reproduced. | L, needs device repro | Y: what preceded it? |
| 127 | m33 test-out 2/12 | audio | Stop-on-advance exists in test-out too (`PlacementTestPage.tsx:270`); bleed is a race with the manual play button at submit. Needs a "still current step" guard. | M | N |
| 128 | m33 test-out 8/12, match | review-pool | たなか paired with itself: names are furniture with no atom, `resolve()` falls back to meaningEn = kana (`moduleCompiler.ts:895`). 5 reviewPool entries affected (m10 ×2, m33 ×3). | S: filter NAMES at `:1547` | N |
| 129 | m33 test-out 11/12, cloze | test-out engine | Dedupe is by step id only (`deriveModuleTestOut.ts:154`); m33 authors さいふが おちる/おとす in 4 beats. Add a normalized-sentence key. | M | N |
| 130 | m34-neo-1 step 5, rule card | question | Yes, two rule cards by design (plain form → practice → "ましょう is its polite dress"). Only lesson in m34 with two. | none | N |
| 131 | m34-neo-1 conjugation_transform | sizing + UX | Chip row `flex-wrap` (`TransformRuleTable.tsx:121`) splits う onto a second line at 430px; and the table shows いく's chips while drilling のむ (alternate-example convention). | M | Y: change the alternate-example convention? |
| 132 | m34-neo-1 reactive tip modal | sizing | `ReactiveGrammarTipCard.tsx:43` lacks the `max-h-[85vh] overflow-y-auto` its sibling `RuleHintCard.tsx:51` has. Same gap in `LessonIntro.tsx`, `TestRunner.tsx`. | S | N |
| 133 | m34-neo-1 particle-cloze (`m34.ir.yaml:238`) | kanji-surfacing | Cloze blank + options are kana by design (`types.ts:1041`, 2026-08-15 call); affects all 559 particle-cloze beats. Display-only kanji would need `optionAnnotations`. | M | Y: surface kanji on cloze options? |
| 134 | m34-neo-1 build (`m34.ir.yaml:245`) | question | だす is the idiomatic verb for mailing a letter (手紙を出す); おくる is the general "send". App is right. Side note: てがみ rendered kana at m34 despite having kanji. | none (+ look at てがみ) | N |
| 135 | m34-neo-1 build (`:249`) | authoring | Premise not confirmed: the harder combined sentence is authored two beats AFTER this one (`:251`). Needs the preceding screen. | blocked | Y: which step came before? |
| 136 | m34-neo-2 lesson-complete | sizing | Checkmark `h-20` + stats `py-5` + 4 buttons. Asks: smaller check, drop "XP earned", shorter box, rethink buttons. | M | Y: what does "Return" do; 3 buttons? |
| 137 | m34-neo-2 build | sizing (systemic) | ばんごはん (1 line) and 食べよう (2 lines) differ in height in one tray: no shared tile-height token across the 4–6 tile components. Ask = tile spec + QA page with sliders. | L (QA page) / M (spec) | Y: prioritise |
| 138 | m34-neo-2 cloze (`:265`) | authoring | Same sentence ばんごはんをたべよう as the build beat at `:261`, 4 beats apart, against the "new sentence per step type" doctrine. m34 has 0 `alsoAccept`, 0 `bankExtras`. | M (IR + recompile) | N (Y if full m34 re-audit) |
| 139 | m34-neo-2 listen-build (`:269`) | authoring | Authored sentence is 3 words (あした また こよう), no bankExtras. Ask: never < 5 tiles. Distractor source not fully traced. | M (IR sweep) | Y: floor = answer tiles or bank tiles? |
| 140 | m34-neo-2 build (`:263`) | grading gap | うちを はやく でよう is valid adverb placement; add `alsoAccept`. | S | N |
| 141 | m34-neo-2 listen-comp (`:269`) | kanji-surfacing | こよう never surfaces 来: irregular stem く→こ breaks reading-keyed substitution (hypothesis, not confirmed in `applyKanjiSurfaces.ts`). Also only 3 of 4 authored options rendered. | M | Y: 来よう or leave こよう kana? |
| 142 | m34-neo-2 listen-build (`:272`) | feature | No English ever shown on a correct listening_build; `listeningBuildSentence()` stores only the generic prompt (`grammarHelpers.ts:1865`). Empty band above is the standard stage-center layout. | M | N |
| 143 | m34-neo-2 auto match review | sizing | `MatchPairsStepView.tsx:238` `minmax(min-content,1fr)` stretches every row to the tallest tile. Same design gap as #137. | M | Y: natural height per tile, or uniform with furigana reserve? |

## 2. How to walk it with Spencer

**Answered, no change** (4): #118, #120, #130, #134.

**Mechanical, no decision, ship as one lap** (9): #121, #124, #125, #127,
#128, #129, #132, #140, #142. Plus #119 (build-16 to-do already agreed).

**Needs one call each** (9): #122 wh-word order · #123 module history ·
#131 alternate-example convention · #133 kanji on cloze options · #136
complete-screen buttons · #137/#143 tile-height rule · #139 5-tile floor
definition · #141 来 on irregular forms.

**Blocked on more info** (3): #123, #126, #135.

**Recurring classes hit again this pull** (see the RCA doc): tile sizing
(#119 #124 #125 #137 #143), kanji surfacing (#120 #133 #141), gloss/verb
naturalness (#118 #134), short/duplicate content (#129 #138 #139), audio on
test-out (#126 #127), rule-card fit (#131 #132).

## 3. Per-item detail (agent output, verified against source by grep)

# TestFlight feedback items #118–130 (b14/b15)

### #118 — Does たべる need an object?
- **Shot:** docs/user-feedback/2026-09-15-testflight-shots/118.jpg (build b14, 01:59Z)
- **Quote:** "Does taberu require you to eat something? If so we should make it easier to figure, force a food hey are eating in the translation"
- **On screen:** `build_sentence`, prompt "Say to a friend: I'm eating at Mika's house at four tomorrow," progress bar + "See the rule · 3" chip + "+10 XP." Tray is empty/wrong; feedback banner shows "Not quite — Correct answer: あしたよじにミカのいえでごはんをたべる." No kanji anywhere (pure kana), no English besides the prompt. Bank tiles below (some dim/spent): あした, よじ, に, ミカ, の, いえ, で, たべる, を, みる, のむ, いく.
- **Located step:** module **m11**, lesson **ja-m11-neo-12** ("に or nothing — pointing at the clock and calendar"), 0-based step index **10**, type `build_sentence` (last beat of the lesson). Located via `grep -n "あした よじに ミカの いえで" src/features/languages/ja/curriculum/ir/m11.ir.yaml` → `m11.ir.yaml:618`; confirmed 1 beat = 1 compiled step (`moduleCompiler.ts:1222` loop, `push(step)` per beat) and counted beats 0–10 in `m11-neo-12`'s `beats:` array (rule, 8×sentence/listening, dialogue, then this `challenge` at index 10).
- **Source:** `src/features/languages/ja/curriculum/ir/m11.ir.yaml:618` — `{ kind: challenge, ja: "あした よじに ミカの いえで ごはんを たべる。", en: "Say to a friend: I'm eating at Mika's house at four tomorrow", combines: [ni-time, counter-ji, no-possession, de-action, wo-object], mode: build, pitfall: {...} }`. Atoms: `courseAtoms.ts:387` `{ id: "ja-m7-1-v-taberu", kana: "たべる", kanji: "食べる", meaningEn: "to eat" }`; `courseAtoms.ts:` (ミカ has no atom entry at all — see #128's finding, it's in the `NAMES` furniture list, not the atom registry).
- **Links:** Local `http://localhost:5399/ja/learn/lessons/ja-m11-neo-12?step=10` · Prod `https://app.openlingoapp.com/ja/learn/lessons/ja-m11-neo-12?step=10`.
- **Class:** authoring (question — content is correct; Spencer is asking a linguistic question with a content-design implication).
- **Answer / finding:** たべる (食べる) is lexically transitive (semantically you always eat *something*), but Japanese is pro-drop: the direct object may be, and routinely is, omitted when understood from context or when the sentence is about *when/where* eating happens rather than *what*. The course itself teaches and grades たべる with **no stated object** in several places — `m6.ir.yaml:509` ("いえで たべる。" = "I eat at home", `mode: build`) and `m7.ir.yaml:298` ("きょう たべる。" = "I'll eat today", `mode: build`) — so it is not a bug that some prompts omit the food. This particular step (#118) *does* name the food (ごはんを, "rice/a meal") — Spencer's ask is really a request to always spell out what's eaten in the English prompt when the sentence has an object, so a learner isn't left guessing "eat what?" when building. That's a copy/authoring-style preference, not a grammar error.
- **Similar issues:** grep `'ja: "[^"]*たべ[るた][^をに"]*。"'` across m6–m8 (excluding lines containing を) found 3 object-less graded たべる sentences: `m6.ir.yaml:509` ("いえで たべる。"), `m7.ir.yaml:298` ("きょう たべる。"), plus the info-only example `m11.ir.yaml:72` ("くじに たべる。"). None of these confuse "eat" with an implied object the way #118 worried about, since none of them name a food at all (they're not "translate the object" drills) — different pattern from #118.
- **Effort:** S — no code change; if Spencer wants "always name the food when the sentence has an object," that's a one-line English-prompt-wording note for the authoring playbook, not a step-type change.
- **Needs Spencer:** N (question answered); Y only if he wants a house style rule enforced ("every ごはんをたべる/food-object sentence must name the food in English") — that's a policy call for the authoring guide.

---

### #119 — Kanji tile looks under-sized vs kana
- **Shot:** docs/user-feedback/2026-09-15-testflight-shots/119.jpg (build b15, 09:24Z)
- **Quote:** "Good sizing fix! It looks like the kanji isn't sizing up as well though. If the user is proficient already then we should save space for furigana they aren't seeing, that something we can fix?"
- **On screen:** `build_sentence`, prompt "Say politely: I went at nine yesterday," empty tray ("Tap tiles to build the sentence"), no XP/rule chip yet answered. Bank tiles: あさ, いきました, 今, に, くじ, で, きのう — 7 tiles, all kana except **今** (kanji, "now"), which is a distractor (not part of the correct answer). No furigana ruby is visible above 今 in the screenshot.
- **Located step:** module **m11**, lesson **ja-m11-neo-review-3** ("Review — clock, calendar and past"), static beat **0**, type `build_sentence`. Located via `grep -n "きのう くじに いきました" src/features/languages/ja/curriculum/ir/m11.ir.yaml` → `m11.ir.yaml:626`. **Caveat:** per scope brief §4/§9, static `ja-mN-neo-review-*` lessons get a render-time **dynamic review prefix** (`dynamicReviewPrefix.ts`) prepended based on the learner's SRS due queue, so the *actual* runtime step index Spencer hit is `0 + (however many dynamic steps preceded it)`, not a fixed `?step=0`.
- **Source:** `src/features/languages/ja/curriculum/ir/m11.ir.yaml:626` — `{ kind: sentence, ja: "きのう くじに いきました。", en: "Say politely: I went at nine yesterday", exercises: [masu-past, ni-time, counter-ji], mode: build }`. The distractor 今 atom is not in this beat's target at all (bank padding); no dedicated `courseAtoms.ts` line matched `kana: "いま"` in this grep pass — worth a follow-up read if the exact atom id matters.
- **Links:** Local `http://localhost:5399/ja/learn/lessons/ja-m11-neo-review-3` (index shifts — jump won't land reliably) · Prod `https://app.openlingoapp.com/ja/learn/lessons/ja-m11-neo-review-3`.
- **Class:** kanji-surfacing / furigana (sizing).
- **Answer / finding:** Two candidate mechanisms, and I can't fully disambiguate without Spencer's live FSRS card state for 今 (per scope brief gotcha #3 — furigana visibility is per-learner mastery, not fixed content): (1) If 今's furigana is **hidden** (mastered/`isNew` false), `.kana-helper[data-visible="false"]` (`src/index.css:471-478`) already collapses the ruby band to `font-size:0; line-height:0; margin-bottom:0` — so no reserved dead space should remain; if the tile still "looks small," the glyph itself just isn't up-scaled to fill the freed vertical room (the pill's own font-size is driven by the shared per-step tile class, not per-glyph). (2) If furigana is still **visible** (not yet mastered), the 0.55em ruby band above 今 (`src/index.css:436`) eats into the pill's effective glyph height relative to the pure-kana tiles next to it, which have no ruby band reserved unless they too carry furigana — this would make 今 look comparatively squat. Spencer's ask — "save the space when they aren't seeing it, and let the kanji grow into it" — implies the app currently reserves the space AND doesn't reclaim it for glyph size, which the code doesn't fully support today (tile font-size is a step-level class, not per-glyph-and-furigana-state).
- **Similar issues:** the `.kana-helper` 12px-floor "dead space above non-furigana tiles" defect was previously reported at b13 #87/#88/#95 and partially fixed at b14 #117 (scoping `[data-visible="true"]` more tightly, per `src/index.css:259` inventory note) — this may be a recurrence or an adjacent case the b14 fix didn't cover (single-kanji tokens specifically). Not independently re-verified live in this pass (would need `getCardState('ima'-or-equivalent-atom-id)` for Spencer's account).
- **Effort:** M — needs a live-state check first (confirm whether 今's furigana is actually visible or hidden for Spencer's account), then either a CSS/BuildTileSurface glyph-upscale rule or confirmation this is expected behavior.
- **Needs Spencer:** Y — need to know if he wants kanji GLYPHS to physically grow to fill freed furigana space (a real behavior change, not just a bug fix), since that's a design call, not just a bug.

---

### #120 — Missing kanji, thinks placement/re-test is needed
- **Shot:** docs/user-feedback/2026-09-15-testflight-shots/120.jpg (build b15, 09:26Z)
- **Quote:** "Maybe I am missing things but I think I should be seeing kanji here. Do I have to re take a placement test for it to show them correctly? Do we not introduce these kanji?"
- **On screen:** `listening_build` ("Build what you hear," play button, no visible prompt sentence, audio-driven). Empty tray. Bank tiles: せんしゅう, とけい, を, かった — all pure kana, no kanji at all. No XP-progress chip beyond the top bar, no "REVIEW" tag.
- **Located step:** module **m11**, lesson **ja-m11-neo-review-3**, static beat **10**, type `listening_build` (mode: listening). Located via `grep -n "せんしゅう とけいを かった" src/features/languages/ja/curriculum/ir/m11.ir.yaml` → `m11.ir.yaml:640`. Same dynamic-prefix caveat as #119 applies to the exact runtime index.
- **Source:** `src/features/languages/ja/curriculum/ir/m11.ir.yaml:640` — `{ kind: sentence, ja: "せんしゅう とけいを かった。", en: "I bought a watch last week", exercises: [time-spans, ta-form, wo-object], mode: listening }`. Atom: `とけい` (watch/clock) — no `kanji` field found for it in the courseAtoms grep pass done for this item (時計 uses the non-N5 kanji 計, which `applyKanjiSurfaces.ts`'s eligibility gate explicitly excludes per scope brief §1/§2 — "it still excludes non-N5 glyphs like 計 in 時計"). かった (bought, past of かう) is an inflected form — no kanji-eligible entry confirmed either.
- **Links:** Local `http://localhost:5399/ja/learn/lessons/ja-m11-neo-review-3` · Prod `https://app.openlingoapp.com/ja/learn/lessons/ja-m11-neo-review-3`.
- **Class:** question (Spencer asks; needs an answer) / kanji-surfacing.
- **Answer / finding:** No, Spencer does not need to retake placement — this is **not a bug**. とけい's dictionary kanji form is 時計, and the second character 計 is not in the N5_KANJI catalog (`n5Kanji.ts`), so `buildEligibleMap()` (`applyKanjiSurfaces.ts:126-153`) never adds とけい to `KANJI_ELIGIBLE_ATOMS` at all — "every component kanji must be in the catalog... this... excludes non-N5 glyphs like 計 in 時計" (comment at `applyKanjiSurfaces.ts:145-147`). So とけい is *permanently* kana-only in this app regardless of module/mastery — it is correctly never shown in kanji. This directly answers "do we not introduce these kanji" — correct, 計 is deliberately never surfaced (N5 scope decision), independent of anything the learner did.
- **Similar issues:** the same catalog-gap note calls out 達 in 友達 (ともだち) as another permanently-kana word for the identical reason — 1 other confirmed example from the code comment itself; a full scan of which N5_KANJI-adjacent words have an excluded second glyph would need a dedicated script (out of this item's grep budget).
- **Effort:** N/A (working as designed) — optionally S to add a one-line in-app note ("this word always shows in kana") if Spencer wants to preempt the confusion, but not required.
- **Needs Spencer:** N — this is a "no bug, here's why" answer; flag as answered, not open.

---

### #121 — "Is は required here?" (きょねんは がくせいだった)
- **Shot:** docs/user-feedback/2026-09-15-testflight-shots/121.jpg (build b15, 09:28Z)
- **Quote:** "Is ha required here? Can we get the card to explain why?"
- **On screen:** `build_sentence`, prompt "Say to a friend: Last year I was a student." Submitted/placed tray: きょねん, がくせい, だった (no は used). Feedback banner: "Not quite — Correct answer: きょねんはがくせいだった." Bank below shows all original tiles, spent ones dimmed: にほんじん, は, ともだち / だった, を, きょねん, がくせい (は sits unused/available, in the un-dimmed bank).
- **Located step:** module **m11**, lesson **ja-m11-neo-review-3**, static beat **11**, type `build_sentence`. Located via `grep -n "きょねんは がくせいだった" src/features/languages/ja/curriculum/ir/m11.ir.yaml` → `m11.ir.yaml:641`. Same lesson as #119/#120 (consecutive in the same review lesson, per neighbour cross-check).
- **Source:** `src/features/languages/ja/curriculum/ir/m11.ir.yaml:641` — `{ kind: sentence, ja: "きょねんは がくせいだった。", en: "Say to a friend: Last year I was a student", exercises: [time-spans, desu-past, wa-topic], mode: build }`. Atoms: `courseAtoms.ts:648` `{ id: "kyonen", kana: "きょねん", kanji: "去年", meaningEn: "last year" }`; `courseAtoms.ts:259` `{ id: "ja-m3-2-v-gakusei", kana: "がくせい", kanji: "学生", meaningEn: "student" }`; `courseAtoms.ts:1135` `{ id: "datta", kana: "だった", meaningEn: "was, were (plain)" }` (copula, `blocked: true`, "no concrete referent").
- **Links:** Local `http://localhost:5399/ja/learn/lessons/ja-m11-neo-review-3` · Prod `https://app.openlingoapp.com/ja/learn/lessons/ja-m11-neo-review-3`.
- **Class:** authoring (question, with a confirmed grading-leniency gap).
- **Answer / finding:** Grammatically, は is **not strictly required** here — きょねん (last year) is a relative-time noun that, like あした/きょう, can open a clause bare (「きょねん がくせいだった」 is fine, plain "last year I was a student") or topic-marked (「きょねんは がくせいだった」, contrastive "as for last year…"). The course's own variant engine already recognizes this pattern for the *narrower* daily-temporal set: `translateVariants.ts:53-54` defines `TEMPORALS = "きょう|あした|あす|きのう|いま|けさ|こんばん|ごぜん|ごご|よる|あさ|ばん|まいにち"` and rule 4 (`translateVariants.ts:13-16`) makes は optional on any of those, both directions, exactly citing the same class of bug ("m6 walk 2026-07-23: きょうたべない was marked wrong against きょうは たべない"). **But きょねん is not in that `TEMPORALS` list** — it belongs to the m11 "vocab pack 2" wider-calendar words (きょねん/せんしゅう/らいげつ/ことし/せんげつ/まいとし, comment at `m11.ir.yaml` around the pack-2 lessons) added after `TEMPORALS` was written, and it was never backfilled in. So Spencer's exact answer, きょねん がくせいだった, is grammatically correct but gets marked wrong by the app because the leniency list is stale — a real, confirmed bug, not user error. The step's `exercises` tag does include `wa-topic` (this beat is explicitly meant to exercise topic-marking), so the AUTHORED target intentionally uses は — but the grading should still accept the bare form as an alternate, the way it does for きょう/あした.
- **Similar issues:** `grep -rn "せんしゅうは\|らいげつは\|ことしは\|せんげつは\|まいとしは\|きょねんは"` across all `ir/m*.ir.yaml` found 2 affected graded `build` steps total: this one (`m11.ir.yaml:641`) and `m31.ir.yaml:944` ("おかねが ないから ことしは なにも あげない。", exercises `[ageru, nai-form, kara-because, nai-existence]`, mode build) — both would incorrectly reject the topic-dropped variant today. `scrambleVariants`'s separate `BARE_TEMPORALS` set (`jaAcceptedForms.ts:49-52`) has the identical gap (also missing きょねん/せんしゅう/etc.), so both leniency mechanisms need the same backfill.
- **Effort:** S — add `きょねん|せんしゅう|らいげつ|ことし|せんげつ|まいとし` (and any other m11 time-span words) to `TEMPORALS` in `translateVariants.ts:53` and to `BARE_TEMPORALS` in `jaAcceptedForms.ts:49`; re-run `BuildSentenceLeniency`-style regression tests.
- **Needs Spencer:** N to fix the leniency gap (mechanical, matches an existing precedent rule); the "can we get the card to explain why" ask is a Y — a UI feature request (surface a short "why" note when a build answer is rejected on a topic-marking technicality), separate from the grading fix.

---

### #122 — "Can't I put いつ here?" (word-order flexibility)
- **Shot:** docs/user-feedback/2026-09-15-testflight-shots/122.jpg (build b15, 09:29Z)
- **Quote:** "Can't I put itsu here?"
- **On screen:** `build_sentence`, banner "REVIEW · 3 LEFT" (dynamic-review chip — not present on #119–121's screenshots). Prompt "Say to a friend: When did you listen to the song." Submitted tray: うた, を, いつ, きいた？ Feedback: "Not quite — Correct answer: いつ うたを きいた？" Bank: を, で, うた, に, いつ, きいた？, が.
- **Located step:** the literal sentence いつ うたを きいた？ matches `grep -n "いつ うたを きいた" src/features/languages/ja/curriculum/ir/m11.ir.yaml` → `m11.ir.yaml:630`, beat 2 of `ja-m11-neo-review-3` (same lesson as #119–121). **However**, this screenshot carries the "REVIEW · N LEFT" chip, which per scope brief §4 marks the render-time **dynamic review prefix** (`dynamicReviewPrefix.ts` / `buildSrsReviewLesson.ts`), not the static lesson body — #119–121 (static beats 0/10/11, no such chip) came chronologically *before* this screenshot (09:24–09:28) but #122 (09:29) shows content matching a beat index (2) that would come *before* all of them in the static lesson. The most consistent read: this is a **different, dynamically-composed review lesson** that mined/reused the identical authored sentence from `m11-neo-review-3` beat 2 via `minedSentences.ts`/`grammarReviewIndex.ts`, resurfacing it as SRS review content in a fresh session. I could not confirm which dynamic lesson container without live SRS state — flagging as **NOT FULLY LOCATED** (content source confirmed at `m11.ir.yaml:630`; delivery surface is the dynamic prefix pipeline, exact lesson id unconfirmed).
- **Source:** `src/features/languages/ja/curriculum/ir/m11.ir.yaml:630` — `{ kind: sentence, ja: "いつ うたを きいた？", en: "Say to a friend: When did you listen to the song", exercises: [itsu-when, ta-form, wo-object], mode: build }`. Atoms: `courseAtoms.ts:390` `{ id: "ja-surv-itsu", kana: "いつ", meaningEn: "when", blocked: true, note: "interrogative" }`; `courseAtoms.ts:200` `{ id: "uta", kana: "うた", kanji: "歌", meaningEn: "song" }`; `courseAtoms.ts:1128` `{ id: "kiita", kana: "きいた", meaningEn: "heard (hear, past)" }`.
- **Links:** Local `http://localhost:5399/ja/learn/lessons/ja-m11-neo-review-3` (static source; live dynamic surface unconfirmed) · Prod equivalent.
- **Class:** authoring (grading-leniency gap in the JA variant/scramble engine).
- **Answer / finding:** In real Japanese, いつ (when) is not pinned to sentence-initial position — 「うたを いつ きいた？」 is equally natural (question-adverb placement is flexible; only the verb must stay clause-final). The app's word-order leniency (`scrambleVariants`, `jaAcceptedForms.ts:437-467`) explicitly refuses to move いつ here: `isMovable()` (`jaAcceptedForms.ts:395-398`) only treats a chunk as movable if it ends in a case particle (`CASE_PARTICLES`, line 43) or is in the `BARE_TEMPORALS` set (line 49) — いつ is neither, so the "longest contiguous movable run" starting the sentence is only `うたを` (length 1), which is below the `bestLen < 2` threshold (`jaAcceptedForms.ts:459`), producing **zero scramble variants** for this sentence. So Spencer is right that うたを いつ きいた？ should be accepted and currently is not — this is a genuine gap in the movable-chunk classifier: bare wh-question adverbs (いつ, and likely どこ/なに/だれ by the same logic) aren't recognized as a movable class at all.
- **Similar issues:** `grep -c` for other build-mode sentences opening with いつ across `ir/m*.ir.yaml` was not separately re-run in this pass (budget); `どこ/なに/だれ` share the identical `isMovable()` gap by construction (none are in `CASE_PARTICLES` or `BARE_TEMPORALS`) so any build_sentence opening with a bare wh-word is equally affected — a full count would need one grep per wh-word, deferred to keep this item bounded.
- **Effort:** M — extend `isMovable()`/`scrambleVariants` with a WH_WORDS-style bare-question-adverb class (いつ/どこ/なに/だれ/なぜ/どう), scoped conservatively like `BARE_TEMPORALS` already is; needs its own regression coverage (`jaAcceptedForms.test.ts`) before shipping since it changes what a huge swath of build steps accept.
- **Needs Spencer:** Y — confirm he wants wh-word scrambling accepted broadly (this could have false-positive risk on sentences where a fronted いつ is being specifically taught/tested as the "correct" position, e.g. this exact review beat) before loosening a course-wide grading rule.

---

### #123 — "Continued at a previous module" after test-out
- **Shot:** docs/user-feedback/2026-09-15-testflight-shots/123.jpg (build b15, 09:33Z)
- **Quote:** "Nevermind it looks like kanji was working it just continued me at a previous module. It seems like some modules or lessons aren't being marked as completely correctly even when I test out of things, something to look at"
- **On screen:** `build_sentence`, header chip "Test out · Conditionals I — たら, ... 3/12." Prompt "Build: When you've read that book, please lend it to me." Empty tray. Bank tiles (kanji visible, confirming kanji rendering IS working here): わたし, 電話, 傘, 読んだら, ください, えんぴつ, に, かして, 本, を, その.
- **Located step:** "Test out · Conditionals I" = module **m32** ("Conditionals I: たら (と as the contrast)", confirmed via `grep -n '^title:' ir/m32.ir.yaml`). This is a derived test-out session (`deriveModuleTestOut.ts`), not a fixed lesson — item 3/12, format `build_sentence`. Not resolved to one exact IR beat (test-out items are algorithmically sampled per-session); flagging the underlying complaint (module-completion bookkeeping) as located in code instead (see below).
- **Source:** the "continued at a previous module" behavior traces to `src/features/placement/engine/applyPlacement.ts:96-113` — `applyPlacementResult()` only calls `markLessonCompleted()` for the lessons of modules actually in `passedSet`/`assumedSet` (`for (const mod of course.modules) { if (!passedSet.has(mod.id) && !assumedSet.has(mod.id)) continue; ... }`). A **single-module** test-out (`/learn/test-out/:moduleId`, `PlacementTestPage.tsx`) only credits that one module — it does not retroactively mark earlier, never-visited modules' lessons complete. If Spencer tested out of a later module (e.g. m32/m33) without ever completing or testing out of an earlier one, any course-map "resume where you left off" logic that walks modules in order looking for the first incomplete lesson will legitimately land on that earlier, still-incomplete module — which reads to the learner as "got bumped backwards."
- **Links:** Local `http://localhost:5399/ja/learn/test-out/m32` · Prod `https://app.openlingoapp.com/ja/learn/test-out/m32`. Progress store: `src/shared/domain/mockProgress.ts` (`mergeServerLessonRollups:123`, `hasLessonProgressReset:211`).
- **Class:** progress/state (also confirms #120's kanji answer was correct — Spencer's own retraction).
- **Answer / finding:** Best-supported hypothesis (not fully confirmed without Spencer's actual passed-module history): single-module test-out is, by design, narrow — it only completes the tested module's own lessons (`applyPlacement.ts:96-113`), and deliberately does NOT mark earlier modules complete just because a later one was proven. If the "resume" pointer used by the course map picks the first module with any incomplete lesson, testing out of module N while module N-3 is still open will send the learner back to N-3. This may be working as designed (test-out isn't meant to imply mastery of everything before it) but the UX reads as a regression to Spencer. A secondary possibility per scope brief §3(b) — `mergeServerLessonRollups` overwriting more-advanced local state with a stale server rollup — was not ruled out; would need Spencer's account's actual `mockProgress` / server rollup history to confirm which of the two is happening.
- **Similar issues:** not independently re-audited beyond citing the one code path; this is a structural (not per-sentence) issue so a "count of similar instances" isn't meaningful the way it is for content bugs.
- **Effort:** M/L — either (a) is-working-as-designed and needs a UX affordance ("you tested out of m32, but m29 is still open — want to also mark earlier modules known?"), or (b) a real rollup-merge bug needing a repro with Spencer's account state. Can't size precisely without confirming which.
- **Needs Spencer:** Y — need to know exactly which modules he had/hadn't completed before this test-out, and ideally a repro (which module test-out, which module he landed back on) to distinguish (a) design-as-expected vs (b) a rollup bug.

---

### #124 — listening_build tile sizing fix didn't port over
- **Shot:** docs/user-feedback/2026-09-15-testflight-shots/124.jpg (build b15, 09:34Z)
- **Quote:** "Looks like the tile sizing fix we have for build translate didn't apply here, or am I seeing things wrong?"
- **On screen:** `listening_build`, "Test out · Conditionals I — たら, ... 5/12," play button + "Build what you hear." Empty tray. Bank: 電話, に, かばん, 書ける (with a small furigana か above it), くうこう, ついたら, を, いす, で — 9 tiles.
- **Located step:** module **m32** test-out, item 5/12, `listening_build`, algorithmically sampled — not pinned to one IR beat (closest content match: `m32.ir.yaml:354`, "くうこうに ついたら、でんわを かける。", mode listening — でんわ here is likely rendering as 電話 (kanji) and かける as the buggy 書ける, see below).
- **Source:** confirmed sibling-parity gap. `BuildSentenceStepView.tsx:275,301,339-347` computes `bigTiles`/`hugeBank`/`denseTileClass` **dynamically from `step.tiles.length`** (the recent b12 10% sizing pass). `ListeningBuildStepView.tsx` shares the same `BuildTileSurface` component for rendering glyphs but has **no equivalent tile-count-adaptive logic** — its bank-tile className is a fixed literal: `src/features/lesson/components/steps/ListeningBuildStepView.tsx:345` `"...px-[14px] py-[7px] text-[17px] font-bold leading-tight ... sm:px-[17.5px] sm:py-[14px] sm:text-[25.5px]"` — same for every tile count, no `bigTiles`-style upscale for small banks or `hugeBank`-style shrink for large ones. The in-file comments (`ListeningBuildStepView.tsx:283-292`) show it *did* get the earlier fixed "#69/#75 sibling parity" -15%/-12.5% shrink pass, but never the later *dynamic* count-based system BuildSentenceStepView got.
- **Links:** Local `http://localhost:5399/ja/learn/test-out/m32` · Prod `https://app.openlingoapp.com/ja/learn/test-out/m32`.
- **Class:** sizing/layout (also flags a separate kanji-surfacing bug, below).
- **Answer / finding:** Spencer is correct — the dynamic bigTiles/hugeBank tile-count sizing system added to `BuildSentenceStepView.tsx` was never ported to `ListeningBuildStepView.tsx`, which still uses one fixed size regardless of bank size (9 tiles here). **Separately**, the 書ける tile is a real kanji-surfacing bug: courseAtoms has exactly one atom with `kana: "かける"` — `courseAtoms.ts:421`, `{ id: "kakeru", meaningEn: "to make (a phone call)", ... }` — and it deliberately carries **no `kanji` field** (it should always render as plain かける). But `src/features/languages/ja/secondScript/buildTileKanji.ts`'s `inflectedTileKanjiMap()` (~lines 132-149) separately enumerates every conjugated form of every kanji-eligible verb (including 書く's potential form かける = "can write" = 書ける) and its homograph guard (`if (existing.kanji && HAS_HAN_INFLECT.test(existing.kanji)) return;`, line ~135) only protects the collision when the *pre-existing* atom (here, "to call" かける) has its own `kanji` field — since it deliberately has none, the guard doesn't fire, and 書く's potential-form surface "書ける" gets bound to the "to call" atom's id and displayed for it. This is exactly the collision the authors flagged as a known risk in `courseAtoms.ts:1413` ("かく → かける is 掛ける 'to call by phone' (m14 row)... Those three verbs are simply never put in the potential in this module") — but that note only stopped a *manual* atom registration; it didn't stop this generic conjugation enumerator from independently deriving the same collision.
- **Similar issues:** the other two verbs named in the same `courseAtoms.ts:1413` comment (かう→かえる colliding with 帰る "to go back"; つく→つける colliding with 付ける "to turn on") are exposed to the identical `buildTileKanji.ts` collision path, since that guard's weakness (no-kanji-field homophone) applies generically — not spot-verified live in this pass, but the code path is shared, so all 3 are equally at risk. Sizing-wise, `grep -c "bigTiles\|hugeBank"` confirms 0 hits in `ListeningBuildStepView.tsx` vs the dynamic logic present in `BuildSentenceStepView.tsx` — a full-component gap, not a partial one.
- **Effort:** M (sizing: port `bigTiles`/`hugeBank`/`denseTileClass` logic from `BuildSentenceStepView.tsx` into `ListeningBuildStepView.tsx`, sharing a helper if possible) + S (kanji bug: strengthen the `buildTileKanji.ts` collision guard to also check whether the *inflected* candidate's own dictionary-form atom would collide with ANY atom sharing that kana, not just one that already owns a kanji field — or simply blocklist the three named collision pairs explicitly).
- **Needs Spencer:** N for the kanji fix (clear bug, matches the authors' own documented risk); N for porting the sizing logic (matches Spencer's own prior sizing-fix intent) — both are mechanical once scoped.

---

### #125 — listening_build tile spacing "10% too much"
- **Shot:** docs/user-feedback/2026-09-15-testflight-shots/125.jpg (build b15, 09:36Z)
- **Quote:** "Maybe I am quoting padding wrong BUT now the pacing between the tiles in the selection row seems like slightly too much, 10% all around? Or increase size a bit more still?"
- **On screen:** `listening_build`, "Test out · Conditionals I — たら, ... 6/12," play button + "Build what you hear." Empty tray. Bank (larger, better-spaced tiles than #124): に, 駅, 車, を, 空, ください, 傘, して, 電話, ついたら — 10 tiles.
- **Located step:** module **m32** test-out, item 6/12, `listening_build`, sampled — not pinned to one exact IR beat within grep budget for this item.
- **Source:** `src/features/lesson/components/steps/ListeningBuildStepView.tsx:331` — bank row wrapper `className="relative flex flex-wrap items-stretch gap-3"` (12px gap, Tailwind `gap-3`) vs the tray's ghost/placed rows at `ListeningBuildStepView.tsx:294,296` using `gap-2 sm:gap-2.5` (8px/10px). The bank row's gap is visibly larger than the tray's — and larger than `BuildSentenceStepView`'s equivalent bank gap (not independently re-diffed against `BuildSentenceStepView.tsx`'s bank wrapper in this pass, but the tray/bank gap mismatch within this same file is confirmed).
- **Links:** Local `http://localhost:5399/ja/learn/test-out/m32` · Prod `https://app.openlingoapp.com/ja/learn/test-out/m32`.
- **Class:** sizing/layout.
- **Answer / finding:** Spencer's "10%" instinct lines up with the numbers: bank uses `gap-3` (12px) while the tray directly above it uses `gap-2`/`gap-2.5` (8px/10px) — dropping the bank to `gap-2.5` (10px) would be a ~17% cut and match the tray exactly; a smaller nudge to e.g. `11px` would be closer to his "10% all around" ask. This is the same component gap as #124 (`ListeningBuildStepView.tsx` never received the later, more careful gap tuning `BuildSentenceStepView.tsx` got).
- **Similar issues:** same file/component as #124 — treat as one fix lane (component-level parity pass on `ListeningBuildStepView.tsx`), not two separate defects.
- **Effort:** S — change `gap-3` → `gap-2.5` (or a `clamp()`-based value matching the tray) at `ListeningBuildStepView.tsx:331`; bundle with #124's sizing port since they're the same component.
- **Needs Spencer:** N — mechanical fix, but worth a quick visual re-check on-device after, per the iOS-sizing-harness memory (Chromium metrics differ from WebKit's).

---

### #126 — Test-out audio dropped entirely
- **Shot:** docs/user-feedback/2026-09-15-testflight-shots/126.jpg (build b15, 09:37Z)
- **Quote:** "Also somewhere along the way my previous test out just dropped audio for some reason, any reason why? Can you test a few test out surfaces and all options and see what would cause that? It had completely dropped all audio"
- **On screen:** `build_sentence`, "Test out · Transitivity I — 自動詞/..., 2/12." Prompt "Build: The teacher opens the classroom door." Empty tray. Bank: が, 家族, ドア, 人, あける, 先生, きょうしつ, の, を, 日本人.
- **Located step:** module **m33** ("Transitivity I: 自動詞/他動詞 — が vs を", confirmed `grep -n '^title:' ir/m33.ir.yaml`) test-out, item 2/12, `build_sentence`, sampled.
- **Source:** audio-stop call sites confirmed: `PlacementTestPage.tsx:270` (`stopAllAudio()` on item-advance) and `:303` (on unmount/navigate-away), both imported from `src/shared/tts/index.ts:41`. Core playback/interruption-recovery: `resumeAudioPlayback()` (`src/shared/tts/index.ts:336`).
- **Links:** Local `http://localhost:5399/ja/learn/test-out/m33` · Prod `https://app.openlingoapp.com/ja/learn/test-out/m33`.
- **Class:** audio.
- **Answer / finding:** Best-supported hypothesis (per the `ios-audio-session.md` memory and scope brief §5, not independently reproduced live in this pass — would need on-device repro to confirm): iOS WebKit can park a `.playAndRecord`-shaped `AudioContext` in an "interrupted" state that no `suspended` check catches, after which every subsequent `play()` call silently no-ops for the rest of the session — this would present exactly as "audio completely dropped for the rest of test-out," as opposed to a single-step bleed (that's #127, a different, narrower symptom). A likely trigger is any earlier `speaking`-step mic-permission interaction in the same session parking the session category; test-out itself has no `speaking` steps, but the app-wide AudioContext is shared, so an interruption anywhere earlier in the same app session (a lesson before this test-out, an incoming call, a mic-permission prompt) could carry forward.
- **Similar issues:** this is the same root-cause class flagged generically in scope brief §5 as "a plausible cross-cutting cause for both bleed AND drop-out reports" — #127 is the sibling "bleed" report from the very next screenshot in this same session, so they may share one fix.
- **Effort:** L — needs a repro harness (the "test a few test-out surfaces and all options" ask) plus a fix in `resumeAudioPlayback()`/session-category handling, likely requiring on-device iOS testing (not reproducible in headless Chromium per the sizing-harness memory's general lesson about engine-metric mismatches).
- **Needs Spencer:** Y — need more detail on what he did right before audio dropped (which surface, whether a mic prompt or interruption occurred, whether backgrounding the app was involved) to build a reliable repro.

---

### #127 — Test-out audio bleeding into next step
- **Shot:** docs/user-feedback/2026-09-15-testflight-shots/127.jpg (build b15, 09:38Z, same "Build: The teacher opens the classroom door" screen as #126 — 2/12 of the same session)
- **Quote:** "Another test out nitpick, on sentence build and for lessons too if we haven't already, the audio is bleeding over into the next step. I think we solved this with lessons already, look into it please"
- **On screen:** Identical to #126's screenshot (same step, same tiles) — this is the same screenshot re-referenced for a second piece of feedback.
- **Located step:** module **m33** test-out, item 2/12, `build_sentence` — same as #126.
- **Source:** `PlacementTestPage.tsx:269-270` — `stopAllAudio()` fires inside the effect that advances to `nextItem` on submit, with the comment "Placement auto-advances on submit, so a cloze's correct-answer clip could still be playing as the next item mounts. Cut it off so audio never bleeds into the next question (Spencer QA 2026-07-16)." This looks like it should already prevent bleed. Contrast: `LessonPage.tsx:220` calls the equivalent stop on its own step-advance ("we solved this with lessons already" — confirmed, same pattern exists there too, `LessonPage.tsx:220,701`).
- **Links:** Local `http://localhost:5399/ja/learn/test-out/m33` · Prod `https://app.openlingoapp.com/ja/learn/test-out/m33`.
- **Class:** audio.
- **Answer / finding:** Spencer's belief that lessons already solved this is correct in principle — both `LessonPage.tsx` and `PlacementTestPage.tsx` call `stopAllAudio()` on their own internal advance, and there's also a navigation-level stop (`useStopAudioOnNavigate.ts`, mounted once in `Layout.tsx:93`) — so on paper test-out should behave like lessons. The most likely remaining gap (not independently reproduced live) is a **race**, not a missing call: if the learner taps the manual "play" button for the CURRENT item's clip right as (or just before) they submit, the advance-triggered `stopAllAudio()` at `PlacementTestPage.tsx:270` can run, and then a stale click-handler closure from the OLD item finishes its `.play()` call slightly after, on the new screen — this is exactly the class of bug scope brief §5 flags generically ("suspect a per-step-view component that plays audio but doesn't check whether it's still the 'current' step before firing"). `build_sentence`/listening steps in test-out don't autoplay by default (there's a manual play button), which is a slightly different audio-trigger shape than most lesson steps, so the fix pattern from lessons may not carry over 1:1.
- **Similar issues:** none independently counted in this pass — recommend Fable reproduce on-device with the audio-play button tapped right at submit time, across `build_sentence`, `listening_build`, and `listening_comprehension` test-out formats, since each has its own play-button wiring.
- **Effort:** M — likely needs a "still current step" guard in whichever play-button handler(s) test-out's build/listening views use, mirroring whatever guard the lesson-side step views already have (scope brief notes ~30 files call `.pause()` in cleanup — confirm test-out's step views are among them).
- **Needs Spencer:** N to investigate further (clear enough lead); Y only if a fix requires trading off the manual-replay button's responsiveness for strict "kill audio the instant advance starts" behavior.

---

### #128 — "Tanaka fail" (match_pairs self-pair)
- **Shot:** docs/user-feedback/2026-09-15-testflight-shots/128.jpg (build b15, 09:40Z)
- **Quote:** "Tanaka fail"
- **On screen:** `match_pairs`, "Test out · Transitivity I — 自動詞/..., 8/12," "Match each Japanese word to its meaning (review)." Left column: 時間, きめる, りょこう, 会社, 店, たなか (bottom-left tile shown dim/dark, i.e. already used). Right column: trip, company, **たなか** (highlighted with a red selection border), time, shop, decide. たなか appears in BOTH columns.
- **Located step:** module **m33** test-out, item 8/12, `match_pairs`, pool sampled from one of m33's `reviewPool`s. Located the mechanism via `grep -n "たなか" ir/m33.ir.yaml` → 3 hits at `m33.ir.yaml:408,544,678`, all `reviewPool:` arrays; line 544's pool (`[かいぎ, じかん, りょこう, テスト, かいしゃ, みせ, きめる, たなか, あした, ともだち]`) is the best content match to the 6 visible words (時間/じかん, きめる, りょこう, 会社/かいしゃ, 店/みせ→ wait 店 vs みせ: 店 IS みせ's kanji — kanji surfacing confirms みせ is being shown as 店, たなか).
- **Source:** root cause confirmed in code, not just content. `src/features/lesson/data/moduleCompiler.ts:895-896` — `const resolve = (kana: string): Atom => atoms.get(kana) ?? { kana, meaningEn: kana, fromModule: moduleId as ReviewAtom["fromModule"] };`. たなか is in the character-name furniture list `NAMES = ["トム", "ミカ", "ケン", "たなか", "タナカ"]` (`moduleCompiler.ts:482`) but **has no entry in `courseAtoms.ts` at all** (`grep -n "たなか" src/features/languages/ja/courseAtoms.ts` → 0 hits, confirmed for all 5 NAMES entries). `NAMES` only feeds `priorVocab`/`metBefore` gating (comment at `moduleCompiler.ts:889-892`) — it is never added to the atom registry `atoms` map. So when たなか appears in a `reviewPool` (as it does 4× across m10/m33), `resolve()` falls through to the `?? { kana, meaningEn: kana, ... }` fallback — producing an atom whose "English meaning" is literally its own kana. In `match_pairs`, that renders as a self-referential pair: the kana card AND its "translation" card both read たなか.
- **Links:** Local `http://localhost:5399/ja/learn/test-out/m33` · Prod `https://app.openlingoapp.com/ja/learn/test-out/m33`.
- **Class:** review-pool/FSRS (content pipeline bug — match-pairs data source, not FSRS scheduling itself).
- **Answer / finding:** Confirmed root cause: `moduleCompiler.ts:895-896`'s `resolve()` fallback silently manufactures a self-translating pseudo-atom for any `reviewPool` entry that isn't in `courseAtoms.ts` — and all 5 character names (トム/ミカ/ケン/たなか/タナカ) are in exactly that state (in `NAMES` for gating purposes, absent from the atom registry). Any time a name is included in a `reviewPool`, its match-pairs card is a guaranteed self-pair.
- **Similar issues:** `grep -n "reviewPool:.*\(トム\|ミカ\|ケン\|たなか\|タナカ\)" ir/m*.ir.yaml` → **5 occurrences**: `m10.ir.yaml:196` (ミカ), `m10.ir.yaml:389` (たなか), `m33.ir.yaml:408,544,678` (たなか ×3). All 5 are affected the same way whenever their host lesson's `match_pairs` step samples that reviewPool entry into its 6-word pick.
- **Effort:** S — either (a) filter `NAMES` out of the `matchable` pool at `moduleCompiler.ts:1547` alongside the existing particle filter ("Match pairs are EXCLUSIVELY words... particles drill in cloze/build, never as match tiles" — extend that same guard to proper names), or (b) register real courseAtoms entries for the 5 names with a `meaningEn` like "Tanaka (a name)" so `resolve()` finds them. (a) is simpler and matches the existing "names are course furniture, not vocabulary" framing.
- **Needs Spencer:** N — mechanical, low-risk fix; flag as ready to ship.

---

### #129 — Duplicate sentence/step-type in test-out
- **Shot:** docs/user-feedback/2026-09-15-testflight-shots/129.jpg (build b15, 09:41Z)
- **Quote:** "It gave me this sentence twice, can we make sure we don't get the same exact question and step type in test outs"
- **On screen:** `particle_cloze` ("COMPLETE THE SENTENCE" header), "Test out · Transitivity I — 自動詞/..., 11/12." Quoted prompt "'I drop my wallet'"; sentence frame さいふを ___。 with おちる placed in the blank (dashed outline) and a lock icon below. Options: おちる (highlighted/selected), おとす, だす.
- **Located step:** module **m33**, `particle_cloze` step type — confirmed via `ParticleClozeStepView.tsx:174` (`t("lesson.completeSentence", "Complete the sentence")`, the literal on-screen header string). Content matches `m33.ir.yaml:697` exactly: `{ kind: particle-cloze, stem: "さいふを ", tail: "。", answer: "おとす", options: ["おとす", "おちる", "だす"], en: "I drop my wallet", ... }`, lesson `m33-neo-11`. **Note:** the correct answer is おとす, not おちる — the screenshot shows おちる selected/highlighted, i.e. the wrong option, mid-attempt (pre-submit; no "Not quite" banner visible yet).
- **Source:** `m33.ir.yaml:697` is the only beat with this exact stem+en pair — so the "twice" is not a literal duplicate IR beat. But `m33.ir.yaml` does contain the identical literal sentence **さいふが おちる。** repeated across at least 3 separate beats with different step types: `m33.ir.yaml:694` (`sentence`, mode build), `:701` (`listening-comp`, audio), and `:740` (a *different* lesson, `m33-neo-review-3`, `particle-cloze`, stem "さいふが ", answer "おちる"). Test-out's sampler (`deriveModuleTestOut.ts:154-158`) dedupes candidates by **`step.id` only** (`usedIds.add((it.step as {id:string}).id)`) — there is no dedup on underlying **content** (target sentence / kana string), so two different beats that happen to render the same or near-identical Japanese sentence (different step ids) can both land in the same 12-item test-out session.
- **Links:** Local `http://localhost:5399/ja/learn/test-out/m33` · Prod `https://app.openlingoapp.com/ja/learn/test-out/m33`.
- **Class:** test-out engine.
- **Answer / finding:** Confirmed gap, not fully confirmed as *this exact* session's cause (would need the session's other 11 items to know which second occurrence Spencer saw): `deriveModuleTestOut.ts` dedupes by `step.id`, not by content, and module m33 authors the transitivity minimal-pair さいふが おちる/さいふを おとす across 4 different beats/step-types (build, listening-comp, and two different particle-cloze beats in two different lessons — `m33-neo-11:697/740` and `m33-neo-review-3:740`). Any pair of those could both be sampled into one session since they have distinct `step.id`s, producing exactly the "same sentence twice" feel Spencer describes — especially since the near-duplicate さいふが/さいふを pair is deliberately authored close together (it's the whole point of the transitivity lesson), making an accidental double-sample more likely than for an arbitrary pair of sentences.
- **Similar issues:** `grep -n "さいふが おちる" ir/m33.ir.yaml` returns exactly this: 3 build/listening/cloze beats sharing the literal string, plus a 4th near-variant ("つくえから さいふが おちる。", `m33.ir.yaml:766`). This minimal-pair-dense authoring style (intentional for teaching 自動詞/他動詞) is exactly the shape most likely to trip a content-blind sampler — worth checking `deriveModuleTestOut.ts`'s `sections`/format-diversity logic (lines ~132-195) for whether it can be extended to also dedupe on normalized target-sentence text, not just step id.
- **Effort:** M — add a content-level dedup key (e.g. normalized `targetSentence`/`ja` string) alongside `usedIds` in `deriveModuleTestOut.ts:154-158`, so two steps rendering the same/near-identical sentence can't both land in one test-out session.
- **Needs Spencer:** N — clear engine fix; Y only if he wants a stricter definition of "duplicate" (e.g. same verb pair, not just same literal sentence) which is a judgment call on how aggressive the dedup should be.

---

### #130 — "Are there two info cards?"
- **Shot:** docs/user-feedback/2026-09-15-testflight-shots/130.jpg (build b15, 13:22Z)
- **Quote:** "Are there two info cards?"
- **On screen:** `grammar_rule` step, "GRAMMAR REFRESHER" label, document icon + audio-play button, title "「のもう」— the will behind ましょう." Bulleted body: "Now the reveal. You have said のみましょう since module 24 — and のみましょう is just のもう wearing the polite layer. Same word, same will, dressed up. This is the course's oldest claim about Japanese — politeness is a LAYER over the real sentence... So nothing new to memorise: everywhere you would say のみましょう to your teacher, you say のもう to a friend. The pair 「のもう / のみましょう」..." (continues below the fold). No tiles, no XP shown yet, empty top progress bar.
- **Located step:** module **m34**, lesson **ja-m34-neo-1** ("「のもう」— the will behind ましょう"), 0-based step index **5**, type `grammar_rule`. Located via `grep -n "Now the reveal" src/features/languages/ja/curriculum/ir/m34.ir.yaml` → `m34.ir.yaml:99` (the `grammarPoints` entry `id: volitional, variant: mashou-is-polite`), then cross-referenced its usage in the lesson body: `grep -n "grammarPointId: volitional\|- id: m34-neo" ir/m34.ir.yaml` shows `m34-neo-1`'s `beats:` array has **two** `{ kind: rule, grammarPointId: volitional }` entries — `variant: shape-u` at beat 0 and `variant: mashou-is-polite` at beat 5, with 4 sentence/cloze beats between them (`m34.ir.yaml:235-244`).
- **Source:** `src/features/languages/ja/curriculum/ir/m34.ir.yaml:229-253` (lesson `m34-neo-1`) — beat 0: `{ kind: rule, grammarPointId: volitional, variant: shape-u }` (rule text at `:87`); beat 5: `{ kind: rule, grammarPointId: volitional, variant: mashou-is-polite }` (rule text at `:98-99`, the exact screenshot text). `moduleCompiler.ts:1220-1229` confirms each `rule` beat compiles to its own `grammar_rule` step (disambiguated by `variant` only on an id-collision, per the comment there), so this lesson genuinely renders **two separate grammar_rule cards**.
- **Links:** Local `http://localhost:5399/ja/learn/lessons/ja-m34-neo-1?step=5` · Prod `https://app.openlingoapp.com/ja/learn/lessons/ja-m34-neo-1?step=5`.
- **Class:** question (Spencer asks; content-structure confirmation, not a bug).
- **Answer / finding:** Yes — this is intentional, by design, not a duplicate-rendering bug. `m34-neo-1` is structured as a two-card teaching arc: card 1 (beat 0, "shape-u") introduces the plain volitional のもう as new material; 4 practice beats reinforce it; card 2 (beat 5, "mashou-is-polite") is the pedagogical "reveal" that のみましょう (which the learner has used since module 24) was always just のもう's polite dress — a deliberate two-step teach documented in the IR's own `grammarPoints` comments. For comparison, this module's other volitional lessons only carry ONE rule card each (`m34-neo-2`: `ru-irregular` only; `m34-neo-3`: `invitation` only) — so `m34-neo-1` is the only lesson in this module with 2 stacked info cards, which is exactly why it stood out to Spencer relative to its siblings.
- **Similar issues:** `grep -n "kind: rule" ir/m34.ir.yaml` shows 4 total rule beats across the module's ~10 teaching lessons, only 2 of which (both in `m34-neo-1`) belong to the same lesson — not systemic; a broader course-wide count of lessons with 2+ rule cards was not run in this pass (would need one grep per module, out of budget for a single item).
- **Effort:** N/A — no code/content defect; optionally S if Spencer wants a "part 1 of 2" visual cue when a lesson deliberately stacks two rule cards, to preempt this exact question recurring.
- **Needs Spencer:** N — answered; confirm he's fine with the two-card structure as designed.

---

## Cross-item themes (your range)

- **Stale grading-leniency lists in the JA variant engine (#121, #122):** two independent leniency mechanisms — `translateVariants.ts`'s `TEMPORALS` (topic-は-optional) and `jaAcceptedForms.ts`'s `BARE_TEMPORALS`/`isMovable()` (word-order scrambling) — were both written against an earlier, narrower vocabulary set and never backfilled when m11's "vocab pack 2" time-span words (きょねん/せんしゅう/らいげつ/ことし/せんげつ/まいとし) or bare wh-question-words (いつ/どこ/なに/だれ) were added. Same class of bug, two different files, likely one coordinated fix session.
- **`ListeningBuildStepView.tsx` never got the sizing system `BuildSentenceStepView.tsx` did (#124, #125):** both are literal instances of the same missing parity — the dynamic tile-count-based `bigTiles`/`hugeBank`/`denseTileClass` logic exists only in `BuildSentenceStepView.tsx`; `ListeningBuildStepView.tsx` still uses fixed literal Tailwind values. Fix once, in one place if a shared hook/helper is extracted.
- **Kanji-surfacing collisions from homophones with no registered kanji field (#119 possibly, #124 confirmed):** #124's 書ける/かける collision is fully confirmed in `buildTileKanji.ts`'s inflected-form enumerator; #119's under-sized 今 tile may be an adjacent but distinct furigana-reservation issue (not a homophone collision) — worth re-checking as a separate ticket rather than merging.
- **`reviewPool`/match-pairs entries with no `courseAtoms` registration (#128):** `moduleCompiler.ts`'s `resolve()` fallback silently self-translates anything in a `reviewPool` that isn't a real atom; confirmed for all 5 character names across 5 reviewPool occurrences. Same underlying gap class as the collision above (a generic "not scoped to a real atom" pattern) but a different code path.
- **Test-out audio (#126, #127):** same session, back-to-back screenshots, likely two symptoms of one shared-AudioContext interruption issue per the `ios-audio-session.md` memory — recommend investigating together rather than as separate tickets, but keep them as separate fix verifications since "total drop" and "single-step bleed" are different enough severities to verify independently.
- **Test-out content-dedup is id-only, not content-aware (#129):** `deriveModuleTestOut.ts` dedupes sampled items by `step.id`, with no awareness that multiple distinct step ids can render the same/near-identical sentence — most visible in modules (like m33's transitivity pairs) that deliberately author near-duplicate minimal-pair sentences across several step types.
- **"Continued at a previous module" (#123) is structurally different from the others:** it's a progress/state design question (does single-module test-out retroactively complete earlier modules?), not a rendering/grading bug — needs Spencer's actual module-completion history to resolve, unlike the other items which are fully groundable in code alone.

# TestFlight feedback items #131–143 (build 15, JA course, m34)

All 13 items in this range are build 15, timestamped 07:24–07:41Z 2026-09-15 (one
continuous walk of module 34, "the volitional"). Located step chrome ("See the
rule · 3", "+10 XP" pill, progress bar) confirms normal LESSON surface (not
test-out). Items 131–133 are lesson `ja-m34-neo-1` ("「のもう」— the will behind
ましょう"); items 134–143 are lesson `ja-m34-neo-2` ("「たべよう・しよう・こよう」").

---

### #131 — volitional rule card chip wrap
- **Shot:** docs/user-feedback/2026-09-15-testflight-shots/131.jpg (build 15, 07:24Z)
- **Quote:** "You need to fit the rule card to fit one row per word and then line up the rule with the actual kana ending it's giving an example for or something for the first example?"
- **On screen:** `conjugation_transform` card, STAGE 1 · LEARN, SRS ×1. Prompt: のむ → ? / "nomu · to drink → **let's drink** · volitional form (let's)". Pinned rule table below reads "う-VERBS" then, on its own line, "いく" (the example word), then chips wrapping across two lines: "い ⟵ い こ +" then "う" alone on line 2. Below: 3 MCQ options, のもう (green, selected/correct), のむよう, のよう. CONTINUE button.
- **Located step:** `conjugation_transform` step, form `volitional`, `verbClass: "godan"`, `base: "のむ"` — auto-ramped from lesson `ja-m34-neo-1`'s `introduces: [のもう, いこう, かおう, けそう, だそう]` (`src/features/languages/ja/curriculum/ir/m34.ir.yaml:233`), not an authored IR beat. Confirmed by `grep -n "STAGE 1 · LEARN\|stage1" src/features/lesson/components/steps/ConjugationTransformStepView.tsx` → line 163 stage label, and the base→gloss line format at lines 209–220 matches the screenshot exactly.
- **Source:** Rule table content comes from `TransformRuleTable.tsx` + `src/features/languages/ja/conjugation/transformRulesets.ts`. Canonical godan row for `volitional` (chips の/む→の/も+う, example のむ): `transformRulesets.ts:352-363`. Because the drilled base (`のむ`) equals that row's own canonical example, `getTransformRulesetFor` (`TransformRuleTable.tsx:48-50`, impl at `transformRulesets.ts:1154-1173`) swaps in the **alternate** row from `RULESET_ALTERNATES.volitional.のむ` at `transformRulesets.ts:926-940` — chips い/く→い/こ+う, example いく — "so the table never prints the card's own answer" (comment at `TransformRuleTable.tsx:40-42`). Atoms: のむ (`courseAtoms.ts:388`, kanji 飲む), いく not in courseAtoms.ts (kana-only helper example in the ruleset, not a course atom).
- **Links:** Local: `http://localhost:5399/ja/learn/lessons/ja-m34-neo-1?step=conjugation_transform.1` (form-based jump per devGates.ts). Prod: `https://app.openlingoapp.com/ja/learn/lessons/ja-m34-neo-1?step=conjugation_transform.1`.
- **Class:** sizing/layout (chip wrap) + authoring/UX (alternate-example mismatch).
- **Answer / finding:** Two distinct, both real. (1) **Wrap**: the chip row is `flex flex-wrap` (`TransformRuleTable.tsx:121-146`) with no width reservation, so at 430px width the 4-chip godan row (い/く-out/い/こ-in/+/う-add) overflows onto a second line, splitting the trailing う chip from its row — this is what reads as "not one row per word." (2) **Mismatch**: because のむ is being drilled, the table is *forced* to show a different word's chips (いく) under the same "う-VERBS" label — the small `row.examples.join(" · ")` caption ("いく") is the only signal it's an alternate, and it's easy to miss, which is exactly what produced "line up the rule with the actual kana ending" — Spencer read いく's い/こ chips as if they were meant to explain のむ→のもう.
- **Similar issues:** The mask-and-swap pattern is structural, not specific to this card: `grep -c "group: \"" ` over `RULESET_ALTERNATES` in `transformRulesets.ts` (line 578 onward) → **47** alternate rows across all conjugation forms (nai, ba, te/ta, volitional, …) — every one of them can show this same "example word ≠ drilled word" swap whenever a learner's own base happens to be a form's canonical example. Wrap risk is likewise general to any focused row whose label text is long relative to 430px — not re-verified per-form here (bounded per brief).
- **Effort:** M — reserve width/allow the 4-chip focused row to never wrap (e.g. reduce gap or drop to `text-base` in focused+dense mode), and make the "showing an alternate word" cue more prominent (e.g. "example: いく (same rule)" instead of a bare word).
- **Needs Spencer:** N for the wrap (styling fix). Y for whether the alternate-example convention should change at all (e.g. show のむ's own chips even when it's the drilled word, since kana ↔ kanji styling could still distinguish "prompt" from "rule").

---

### #132 — rule-reveal card clipped behind status bar, no scroll
- **Shot:** docs/user-feedback/2026-09-15-testflight-shots/132.jpg (build 15, 07:25Z)
- **Quote:** "This info card doesn't fit on the screen and has no scroll, need to make these more concise and maybe format better"
- **On screen:** A "Quick fix" modal (title clipped/cut off behind the iOS status bar, only "いっし…よう" fragment with a red strike-through visible at the very top). Below: a ✓ box reading "いっしょにたべよう。". Below that, prose: "the polite suggestion is not ます with よう stacked on top — the layers FUSE: ましょう is the volitional's polite form entire. いっしょに たべましょう。". Then a bordered "The rule again:" box with the full multi-paragraph "mashou-is-polite" rule text ("Now the reveal. You have said のみましょう since module 24…" through "…works exactly like 「のむ / のみます」."). Bottom: "Got it — try again" button.
- **Located step:** `ReactiveGrammarTipCard` modal (not a step itself — an error-triggered overlay), fired while the learner is on a step exercising grammar point `volitional` / variant `mashou-is-polite`. Located by the unique button string: `grep -rn "Got it — try again" src/features/lesson/components/*.tsx` → only hit is `ReactiveGrammarTipCard.tsx:85`. The rule prose is a byte-for-byte match to `src/features/languages/ja/curriculum/ir/m34.ir.yaml:97-100` (grammarPoint `id: volitional, variant: mashou-is-polite`, lesson `ja-m34-neo-1`).
- **Source:** `src/features/lesson/components/ReactiveGrammarTipCard.tsx:37-43` — outer wrapper `fixed inset-0 z-50 flex items-end justify-center … sm:items-center` (line 38), inner card `w-full max-w-md rounded-2xl … p-5` (line 43) — **no `max-h-*` or `overflow-y-auto` anywhere in the file** (confirmed: `grep -n overflow src/features/lesson/components/ReactiveGrammarTipCard.tsx` → no hits). Rule/antiPattern text: `ir/m34.ir.yaml:97-100`. Wrong/right pair rendered at lines 55-66 of the component (icons + `tip.wrongJa`/`tip.rightJa`); the ✗ box is what's scrolled/clipped off the top of the viewport.
- **Links:** Local: trigger by answering a build/particle-cloze step on grammar point `volitional`/`mashou-is-polite` incorrectly in `http://localhost:5399/ja/learn/lessons/ja-m34-neo-1`. No direct deep link to the modal itself (it's error-triggered, not step-addressable). Prod equivalent: `https://app.openlingoapp.com/ja/learn/lessons/ja-m34-neo-1`.
- **Class:** sizing/layout.
- **Answer / finding:** Root cause confirmed by direct code comparison: the near-identical sibling modal `src/features/lesson/components/RuleHintCard.tsx:51` uses `max-h-[85vh] w-full max-w-md overflow-y-auto rounded-2xl …` — it scrolls. `ReactiveGrammarTipCard.tsx:43` uses `w-full max-w-md rounded-2xl …` with **no height cap and no scroll**, so on a long rule (this one is 5 sentences of prose plus a ✗/✓ pair and a "why" line) the card simply overflows the fixed-position dialog upward past the safe area, with the browser/WebView's own page scroll (not an internal scroll region) revealing the top behind the status bar — exactly the clipped-title symptom in the screenshot.
- **Similar issues:** `grep -rLn overflow-y-auto src/features/lesson/components/*.tsx | xargs grep -l "fixed inset-0"` → 3 other `fixed inset-0` overlays with no scroll: `LessonIntro.tsx`, `TestRunner.tsx`, and this file; only `RuleHintCard.tsx` has the fix already. `LessonIntro.tsx` is worth a follow-up look (module intro text can run long too).
- **Effort:** S — copy `RuleHintCard.tsx:51`'s `max-h-[85vh] overflow-y-auto` onto `ReactiveGrammarTipCard.tsx:43`.
- **Needs Spencer:** N — this is a straightforward parity fix against the sibling component's already-approved pattern.

---

### #133 — particle-cloze options never show kanji
- **Shot:** docs/user-feedback/2026-09-15-testflight-shots/133.jpg (build 15, 07:25Z)
- **Quote:** "I should be seeing kanji here for each of these even for the distractions right?"
- **On screen:** `particle_cloze` step, "COMPLETE THE SENTENCE", quote "Let's have a coffee". Sentence frame: コーヒーを [blank]。 with the blank shown as a dashed pill containing the selected answer のもう (red/selected). 3 options below: のむう, のみよう, のもう (selected, outlined red = correct). All three options and the blank are plain kana — no kanji anywhere on the card. CHECK button.
- **Located step:** `particle_cloze` beat, lesson `ja-m34-neo-1`. Found via `grep -n "answer: \"のもう\", options" src/features/languages/ja/curriculum/ir/m34.ir.yaml` → `ir/m34.ir.yaml:238`: `{ kind: particle-cloze, stem: "コーヒーを ", tail: "。", answer: "のもう", options: ["のもう", "のむう", "のみよう"], en: "Let's have a coffee", ... }`.
- **Source:** Compiled by `cloze()` factory, `src/features/languages/ja/grammarHelpers.ts:390-450`, returning `type: "particle_cloze"` (line 443) with `options: rotated` (raw kana strings, line 447) and no per-option annotation field. Confirmed no kanji path exists for options: `grep -n "applyKanjiSurfaces" src/features/lesson/components/steps/ParticleClozeStepView.tsx` → 0 hits. The step type itself documents this as **deliberate**: `src/features/lesson/types.ts:1035-1041` — "*until these existed (2026-08-15) the halves were bare strings… **The blank itself stays kana — it is the answer.**" Only the `before`/`after` frame text (here "コーヒーを") gets a kanji-eligible `beforeAnnotation`/`afterAnnotation` (types.ts:1043-1044) — but コーヒー is a katakana loanword with no kanji form, so the frame shows nothing extra here either.
- **Links:** Local: `http://localhost:5399/ja/learn/lessons/ja-m34-neo-1?step=particle_cloze.1`. Prod: `https://app.openlingoapp.com/ja/learn/lessons/ja-m34-neo-1?step=particle_cloze.1`.
- **Class:** kanji-surfacing (question, and the answer is "by design").
- **Answer / finding:** This is not a bug — it's an explicit, documented design choice: the cloze blank/options are the *grading string* and are intentionally kept in kana at all times (`types.ts:1041`), regardless of module/unlock distance. So "even for the distractors" — yes, none of them will ever kanji-surface under the current design. Whether that's still right this late in the course (m34, long past kanji unlock) is a legitimate product question: the options could show kanji as *display only* (mirroring `word_image_mcq`'s `optionAnnotations` pattern) while keeping the grading string kana-keyed underneath — that's an available pattern, just not wired to `particle_cloze`.
- **Similar issues:** `grep -rc "kind: particle-cloze" src/features/languages/ja/curriculum/ir/*.ir.yaml` totals **559** particle-cloze beats course-wide — every single one has this same never-kanji-surfaces-options behavior.
- **Effort:** M — add `optionAnnotations?` to `ParticleClozeStep` (types.ts:1028-1045), populate via `applyKanjiSurfaces` in `ParticleClozeStepView.tsx`, keep grading on the raw kana `options` array unchanged.
- **Needs Spencer:** Y — decide whether particle-cloze options should kanji-surface (display-only) at all, given the type's explicit "stays kana, it is the answer" invariant was a deliberate 2026-08-15 call.

---

### #134 — verb choice for "send" (だす vs おくる)
- **Shot:** docs/user-feedback/2026-09-15-testflight-shots/134.jpg (build 15, 07:28Z)
- **Quote:** "Isn't there another verb for send that is more commonly used?"
- **On screen:** `build_sentence` step, "Build: I'll send the letter tomorrow" — placed/correct tray: 明日(あした) てがみ を だそう. Bank remainder (used, greyed): てがみ, 写真, 自転車, だそう / 明日(あした), が, を — i.e. 2 distractor tiles (写真, 自転車) plus the 4 used tiles. CONTINUE button.
- **Located step:** `sentence` beat, mode `build`, lesson `ja-m34-neo-1`. `grep -n "てがみを だそう" src/features/languages/ja/curriculum/ir/m34.ir.yaml` → `ir/m34.ir.yaml:245`: `{ kind: sentence, ja: "あした てがみを だそう。", en: "I'll send the letter tomorrow", exercises: [volitional, wo-object], mode: build }`.
- **Source:** `ir/m34.ir.yaml:245`. Atoms: てがみ = `courseAtoms.ts:288` (`kana: "てがみ", kanji: "手紙", fromModule: "m4"`); だそう is an m34-local IR `newAtom` (`ir/m34.ir.yaml:184`: `derivedFrom: だす, verbClass: u`), whose base だす is `courseAtoms.ts:623` (`kanji: "出す", fromModule: "m33", blocked: true`, note: "transitivity pair with でる").
- **Links:** Local: `http://localhost:5399/ja/learn/lessons/ja-m34-neo-1?step=build_sentence.4` (approx — step-type-indexed jump; verify index against the live lesson). Prod: `https://app.openlingoapp.com/ja/learn/lessons/ja-m34-neo-1`.
- **Class:** authoring (question — Spencer asks; answer is the app is correct as authored).
- **Answer / finding:** だす is in fact the standard, idiomatic verb here — 手紙を出す ("tegami wo dasu") is the ordinary way to say "mail/send a letter" in Japanese (literally "put the letter out," i.e. into the mailbox); it's arguably *more* natural for this specific collocation than 送る (おくる), which is the general "send" verb for packages, money, email, or people, and reads slightly more formal/distancing when used with a simple letter. So the authored sentence is linguistically sound; Spencer's instinct that there's "another verb for send" (おくる) is correct that it exists, but it's not the more common choice for 手紙 specifically. No fix needed to the sentence itself.
- **Similar issues:** Not applicable — no code/content defect to search for; this is a one-off vocabulary-choice question. Separately noted in passing: the てがみ tile renders in plain kana (no 手紙 kanji) despite being introduced at m4 and this lesson being m34 (well past the unlock+2 furigana window) — see item #141's kanji-surfacing finding for the likely mechanism; not chased further here since it's outside what Spencer asked in #134.
- **Effort:** N/A (no change needed) — optionally, a short "why だす not おくる" aside could be added to the step's `explanation` field if Spencer wants the distinction taught explicitly.
- **Needs Spencer:** N — question answered; no action required unless he wants the おくる/だす distinction taught as its own point.

---

### #135 — "harder sentence shown before the simple one" (order claim)
- **Shot:** docs/user-feedback/2026-09-15-testflight-shots/135.jpg (build 15, 07:30Z)
- **Quote:** "We were just shown a more difficult version of this sentence, the simple version is useless since they just learned it, go look at lesson order, this shouldn't happen and should be replaced with better sentence variety"
- **On screen:** `build_sentence` step, "Build: Let's go to the shop and buy a drink" — completed correctly: 店(みせ) に 行って(いって) 飲み物(のみもの) を 買おう(かおう)。 Bank remainder greyed: に, こうえん, 飲み物, 買おう / 店(みせ), 行って, を, うち, 空. "WELL DONE! 🔥 3 in a row" toast fading. CONTINUE button.
- **Located step:** `sentence` beat, mode `build`, lesson `ja-m34-neo-1`. `grep -n "みせに いって、のみものを かおう" src/features/languages/ja/curriculum/ir/m34.ir.yaml` → `ir/m34.ir.yaml:249`: `{ kind: sentence, ja: "みせに いって、のみものを かおう。", en: "Let's go to the shop and buy a drink", exercises: [volitional, te-form, wo-object], mode: build }`.
- **Source:** `ir/m34.ir.yaml:249`. Compiled JSON confirms the same order (`ir/m34.ir.json:804-813`, simple build beat *before* the harder combined-clause beat).
- **Links:** Local: `http://localhost:5399/ja/learn/lessons/ja-m34-neo-1?step=build_sentence.5` (approx). Prod: `https://app.openlingoapp.com/ja/learn/lessons/ja-m34-neo-1`.
- **Class:** review-pool/FSRS or authoring (uncertain which — see below) / question.
- **Answer / finding:** I could **not confirm** Spencer's premise from the source order. The harder combined version of this exact sentence — `{ kind: challenge, ja: "えいがが はじまる まえに みせに いって、のみものを かおう。", combines: [volitional, mae-ni, te-form, jidoushi-tadoushi] }` — is authored at `ir/m34.ir.yaml:251`, **two beats after** this simple build beat (line 249), separated by one `listening-comp` beat (line 250). `moduleCompiler.ts:1222` iterates `for (const beat of lesson.beats)` in array order with no shuffle found (`grep -n "shuffle\|sort(\|Math.random" src/features/lesson/data/moduleCompiler.ts` → no reordering hits near the beat loop), so the compiled step order should match source order: simple first, challenge second. If Spencer genuinely saw the harder sentence *first*, the likely explanation is a **different** occurrence entirely — e.g. this exact collocation being drawn into a review/filler step from an *earlier* point in the session (not captured in this item range) — I don't have visibility into the 1–2 steps immediately preceding item #135 to confirm. Flagging as unresolved rather than guessing.
- **Similar issues:** Not chased further given the unresolved premise (bounded per brief) — worth a follow-up walk capturing the step immediately before #135 if this recurs.
- **Effort:** — (blocked on more info).
- **Needs Spencer:** Y — ask which exact step/screen preceded this one (or re-walk m34-neo-1 end-to-end) so the "harder sentence first" claim can be pinned to a specific beat before any lesson-order fix is made.

---

### #136 — lesson-complete screen crowded
- **Shot:** docs/user-feedback/2026-09-15-testflight-shots/136.jpg (build 15, 07:32Z)
- **Quote:** "This screen is a little crowded, do estned takes lot of vertical, remove gained and shrink box height, shrink check mark size and then we need to re think the buttons below. Next lesson, drill, and a return button should be all that's there. With drill below the return button, return button can be grey or something maybe? Whatever fits the color scheme and then drill can stay the buttonless form it is"
- **On screen:** Lesson-complete screen for "「のもう」— the will behind ましょう". Large checkmark circle icon. "Lesson Complete!" heading. Stats card: 94% ACCURACY | +10 XP EARNED | 17/18 SCORE. Streak/level card: 🔥 2 day streak, Level 1, 360/500 progress bar. Buttons stacked: NEXT LESSON → (solid), "Drill what you missed (1)" (outlined), "Review your words →" (outlined), "I'm done — save my XP" (ghost/text) with caption "Locked in — see you tomorrow." below it.
- **Located step:** `LessonComplete` screen, rendered at end of lesson `ja-m34-neo-1`. Found via `grep -rln "Lesson Complete\|Drill what you missed" src/features/lesson/components/` → `src/features/lesson/components/LessonComplete.tsx`.
- **Source:** Checkmark circle: `LessonComplete.tsx:143` (`h-20 w-20` circle) and `:147` (`<Icon name="check" size={40} />`). Stats card: `:185-213` (`px-6 py-5` container with `Stat` sub-components for accuracy/XP/score). Streak/level card: `:217-249`. Button stack (5 elements today): NEXT LESSON `:302-308` (`variant="primary-3d"`), "Drill what you missed" `:310-320` (`variant="outline" accent`, conditional on `missedCount>0`), "Review your words →" `:322-330` (`variant="outline"`), "I'm done — save my XP" + hint caption `:332-343` (`variant="ghost"` + `<span>` hint).
- **Links:** Local: complete any JA lesson, e.g. `http://localhost:5399/ja/learn/lessons/ja-m34-neo-1` and finish it. Prod: `https://app.openlingoapp.com/ja/learn/lessons/ja-m34-neo-1`.
- **Class:** sizing/layout.
- **Answer / finding:** Spencer wants 3 concrete changes, all locatable: (1) shrink the checkmark (`h-20 w-20`/`size={40}` at `:143,147`) and the stats card's vertical padding (`px-6 py-5` at `:185`); (2) drop the "+10 XP EARNED" stat's redundancy with the header checkmark/heading (his "remove gained" reads as "XP earned" is redundant once the checkmark+heading already signal completion) — likely means tightening that middle `Stat` block rather than deleting it outright, needs his confirmation; (3) collapse the current 4-button stack (Next lesson / Drill / Review your words / I'm done+hint) down to exactly 3: **Next lesson** (unchanged), a **grey "Return"** button (folding "Review your words" + "I'm done — save my XP" + the "Locked in…" hint into one exit action), and **Drill** kept in its current "buttonless"/ghost-link form, moved to sit *below* the Return button.
- **Similar issues:** Not applicable — this is a single dedicated screen; no other component shares this button stack.
- **Effort:** M — button-stack restructure (3 buttons instead of 4, re-home "Drill" below "Return", merge "Review words"/"I'm done" semantics into one grey exit action) plus a S-sized icon/padding shrink.
- **Needs Spencer:** Y — exactly what "Return" should do (go to course map? replace both "Review your words" and "I'm done — save my XP" with one action, or just relabel one of them grey?) and whether removing the XP-earned stat number entirely vs. just de-emphasizing it is intended.

---

### #137 — build-step tile sizing QA page request
- **Shot:** docs/user-feedback/2026-09-15-testflight-shots/137.jpg (build 15, 07:35Z)
- **Quote:** "I don't know if these are different lesson types or something, but ANY build step is so inconsistent in sizing on cells. We need to make a QA page displaying every build type step, with a few different sentence examples, and confirm the sizing is standardized. MAKE ATRONG NOTE HERE. the height of every tile should be the same, furigana should not change that, we need to figure out our padding situation better, make me a QA page that can give me a slider for furigana spacing above the kana, kana size, and padding vertically and horizontally"
- **On screen:** `build_sentence` step, "Build: Let's eat dinner" — tray: ばんごはん(large, no furigana, tall tile) を(large) 食べよう(smaller kana, with furigana た above 食, visibly shorter/denser tile than ばんごはん). Bank remainder: を, に, が, 食べよう(furigana, grey/used) / ばんごはん(grey/used), で. CHECK button.
- **Located step:** `sentence` beat, mode `build`, lesson `ja-m34-neo-2`. `grep -n "ばんごはんを たべよう" src/features/languages/ja/curriculum/ir/m34.ir.yaml` → `ir/m34.ir.yaml:261`: `{ kind: sentence, ja: "ばんごはんを たべよう。", en: "Let's eat dinner", exercises: [volitional, wo-object], mode: build }`. (Note: ばんごはん is a courseAtom with a kanji field, `courseAtoms.ts:769`, 晩ご飯, but renders in plain kana here — a furigana/kanji-surfacing question in its own right, not chased further per item scope.)
- **Source (per brief, scope only — no page built):** This is a request for a dedicated QA page; per the task brief I'm scoping what it needs, not building it. Every component that renders build/tile-bank cells, for the page to cover:
  - `src/features/lesson/components/steps/BuildSentenceStepView.tsx` — tile sizing knobs: `bigTiles` (`:275`), `hugeBank` (`:301`), `denseTileClass` (`:339-347`, feeds `bankTileClass`/`placedTileClass`), tray min-height (`:615,649`).
  - `src/features/lesson/components/steps/BuildTileSurface.tsx` — shared tile-rendering surface; `useBuildTileKanji` hook (~`:104-134`) governs kanji/furigana display per tile.
  - `src/features/lesson/components/steps/SortableBuildTiles.tsx` — drag/drop tray, `touch-action` handling (`:111-119`).
  - `src/features/lesson/components/steps/ListeningBuildStepView.tsx` — audio-driven tile-build variant, its own `bankTiles = seededShuffle(step.tiles, step.id)` (`:80-83`) and sizing classes.
  - `src/features/lesson/components/steps/MatchPairsStepView.tsx` (+ `MatchPairsKanjiSurface`) — pair-tile grid, separate sizing system (`clamp()` fonts at `:374-378,464-468`, row-height cap at `:238-247` — see item #143 for a specific bug here).
  - `src/features/lesson/components/steps/MultipleChoiceStepView.tsx` (+ `McqOptionKanjiFurigana`) — MCQ option tiles, fixed 2×2 grid (`:123`).
  - `src/features/lesson/components/steps/TapTheWordStepView.tsx`, `WordMapStepView.tsx`, `RowTestStepView.tsx` (wraps build/MCQ/match via thin adapters) — not yet inspected for tile-sizing knobs in this pass.
  - Shared CSS: `src/index.css` — `.kana-helper` (furigana size/visibility: `:436,471,483,496,819-847`, including the 12px mobile floor), `.kanji-ruby` (`:376,385`), `.build-tile-dense` (`:862`).
  A slider-driven QA page would need controls bound to: furigana-above-kana spacing (the `.kana-helper` line-height/margin rules), kana font size (the `clamp()` calls in each *StepView), and tile padding (the `py-*`/`px-*` classes named above) — ideally as CSS custom properties the page can override live, since today these are hard-coded Tailwind utility strings scattered across ~6 files rather than centralized tokens.
- **Links:** Existing prior-art pattern to reuse: `src/features/lesson/dev/FeedbackB12ReviewPage.tsx` (route `/ja/qa/feedback-b12`) and the general QA hub `http://localhost:5399/ja/qa`.
- **Class:** sizing/layout (feature-request: QA tooling).
- **Answer / finding:** Confirmed root complaint: `ばんごはん` (no furigana, 1 line) and `食べよう` (furigana + kanji, 2 visual lines) sit in the same tray with visibly different tile heights in this screenshot — consistent with there being no single shared height token across the tile-rendering components listed above; each StepView computes its own `dense`/`bigTiles`/`clamp()` sizing independently. Building the requested page is out of scope for this pass (per task instructions); the file list above is the starting inventory for whoever builds it.
- **Similar issues:** N/A — this item IS the "systemic" ask; see #143 for one concrete instance of the same family (match_pairs row-height inconsistency) already root-caused.
- **Effort:** L — the QA page itself (new route + slider UI + CSS-custom-property plumbing through 4-6 step components) is a multi-day build, not a quick fix.
- **Needs Spencer:** Y — this is explicitly his ask; needs prioritization against other queue items given the L effort estimate. **STRONG NOTE per his request**: tile height standardization (independent of furigana presence) is the core ask.

---

### #138 — same sentence retested via a second step type, no kanji
- **Shot:** docs/user-feedback/2026-09-15-testflight-shots/138.jpg (build 15, 07:36Z)
- **Quote:** "This is poor authoring I was just asked this question in a different font, I already get the answer also no kanji"
- **On screen:** `particle_cloze` step, "COMPLETE THE SENTENCE", quote "Let's eat dinner". Frame: ばんごはんを [blank]。, blank shows selected answer たべよう (dashed pill, plain kana, no kanji). Options: たべろう, たべる, たべよう (selected/correct, outlined). CHECK button.
- **Located step:** `particle_cloze` beat, lesson `ja-m34-neo-2`. `grep -n "answer: \"たべよう\"" src/features/languages/ja/curriculum/ir/m34.ir.yaml` → `ir/m34.ir.yaml:265`: `{ kind: particle-cloze, stem: "ばんごはんを ", tail: "。", answer: "たべよう", options: ["たべよう", "たべろう", "たべる"], en: "Let's eat dinner", ... }`.
- **Source:** **Confirmed duplicate**: the identical sentence "ばんごはんをたべよう。" is ALSO an authored `sentence`/`mode: build` beat 4 lines earlier in the *same lesson*: `ir/m34.ir.yaml:261` — `{ kind: sentence, ja: "ばんごはんを たべよう。", en: "Let's eat dinner", mode: build }` (this is item #137's step). Only 3 unrelated beats separate them (しゅくだいをはじめよう build, うちをでよう build, また こよう listening — lines 262-264). No kanji: same mechanism as #133 — particle-cloze blank/options stay kana by design (`types.ts:1035-1041`); the ばんごはんを frame text also has no kanji shown despite ばんごはん having a `kanji: "晩ご飯"` field (`courseAtoms.ts:769`, `fromModule: "m32"`) — m34 is only 2 modules past m32, right at (not clearly past) the documented "unlock+2" furigana window, so this may be exactly on the boundary rather than a bug; not confirmed either way.
- **Links:** Local: `http://localhost:5399/ja/learn/lessons/ja-m34-neo-2?step=particle_cloze.1`. Prod: `https://app.openlingoapp.com/ja/learn/lessons/ja-m34-neo-2`.
- **Class:** authoring (content wrong/weak) — repeated-sentence violation, confirmed.
- **Answer / finding:** Spencer is exactly right and it's traceable to source, not a misperception: CLAUDE.md's own authoring doctrine says "a new sentence per grammar point (don't recycle one sentence through 4 step types)" — this lesson recycles "ばんごはんをたべよう" through both `build` (line 261) and `particle-cloze` (line 265), 4 beats apart. That's the "poor authoring / different font, same question" feeling exactly. Separately: no kanji is expected-by-design for the cloze blank itself (see #133); the frame's kanji absence is a secondary, less certain finding.
- **Similar issues:** Not exhaustively re-grepped across the whole course per the brief's "bounded" instruction, but the doctrine violation is locally clear: within `ja-m34-neo-2` alone there are 2 `particle-cloze` beats (lines 265, and しよう's at the next one) each immediately preceded by a `sentence`/build beat testing a near-identical construction pattern (たべよう then しよう a few lines later, same structure) — worth a dedicated re-audit of m34 sentence reuse across step types if this pattern recurs elsewhere.
- **Effort:** M — swap the particle-cloze's underlying sentence to a fresh one exercising the same grammar point (ru-irregular volitional) that hasn't already appeared as a build beat in this lesson; requires `node scripts/compile-ir.mjs m34` after editing the YAML.
- **Needs Spencer:** N for the fix direction (matches an already-documented doctrine); Y only if he wants the whole m34 lesson re-audited for other repeats before shipping a point fix.

---

### #139 — 3-tile listening-build sentence too short
- **Shot:** docs/user-feedback/2026-09-15-testflight-shots/139.jpg (build 15, 07:37Z)
- **Quote:** "Need a few more distractions here and the sentence needs to be a little longer. At this point they should NEVER be shorter than 5 tiles"
- **On screen:** `listening_build` step, "▶ Build what you hear." Tray (correct): 明日(あした) また 来よう. Bank remainder: 今朝, 明日(grey/used), 今, 来よう(grey/used), 朝, また(grey/used) — i.e. 3 real distractor tiles (今朝, 今, 朝) alongside the 3 used ones. CHECK button.
- **Located step:** `sentence` beat, mode `listening`, lesson `ja-m34-neo-2`. `grep -n "あした また こよう" src/features/languages/ja/curriculum/ir/m34.ir.yaml` → `ir/m34.ir.yaml:264`: `{ kind: sentence, ja: "あした また こよう。", en: "Let's come again tomorrow", exercises: [volitional], mode: listening }`.
- **Source:** Compiled via `listeningBuildSentence()`, `src/features/languages/ja/grammarHelpers.ts:1865-1886` — `tiles: opts.tiles` is passed straight through with **no distractor injection in this factory** (confirmed: no `distractor`/`filler`/`extra` logic in `ListeningBuildStepView.tsx` either — `grep -n "distractor\|Extra" src/features/lesson/components/steps/ListeningBuildStepView.tsx` → only a comment at `:225` noting "the bank carries distractors" with no implementation in this file). `bankTiles = seededShuffle(step.tiles, step.id)` at `ListeningBuildStepView.tsx:80-83` — bank tiles are just a shuffle of `step.tiles`. `m34.ir.yaml` has **zero** `bankExtras` entries anywhere (`grep -c bankExtras ir/m34.ir.yaml` → 0), and this beat authors none. Given that, I could not fully trace where the 3 extra distractor tiles (今朝/今/朝) visible in the render come from — they aren't in the beat's authored text and the factory I traced doesn't add them, so either a mechanism I didn't find at runtime supplies them, or this step is a dynamically-generated review/filler variant of the same sentence rather than the primary m34-neo-2 beat. Flagging as a gap in my trace rather than asserting a specific line.
- **Links:** Local: `http://localhost:5399/ja/learn/lessons/ja-m34-neo-2?step=listening_build.1` (approx). Prod: `https://app.openlingoapp.com/ja/learn/lessons/ja-m34-neo-2`.
- **Class:** authoring (content wrong/weak).
- **Answer / finding:** The part I can assert with full confidence: the **authored sentence itself is only 3 words** (あした/また/こよう) with **no `bankExtras`** authored for it anywhere in m34 — that's the root cause of "the sentence needs to be a little longer," independent of however many distractor tiles the runtime ends up showing. Fixing "never shorter than 5 tiles" means either lengthening this sentence in the IR or adding `bankExtras` to pad the bank (the two are different levers — sentence length vs. bank size — and Spencer's ask conflates them slightly; both point at `ir/m34.ir.yaml:264`).
- **Similar issues:** Not exhaustively counted (bounded per brief) — a quick `grep -c "mode: listening" ir/m34.ir.yaml` shows several other short listening-build sentences in the same module that would need the same review if a 5-tile floor is adopted as a course-wide rule.
- **Effort:** M — either lengthen the target sentence (content change) or author `bankExtras` for it (mechanical addition per the IR field moduleCompiler already supports at `:1358-1359`); needs the distractor-source question above resolved first so the fix targets the right layer.
- **Needs Spencer:** Y — confirm the "never shorter than 5 tiles" floor should be a hard authoring rule (and whether it's tiles-in-the-answer or tiles-in-the-bank he means), then this becomes a straightforward IR sweep.

---

### #140 — alternate word order not accepted
- **Shot:** docs/user-feedback/2026-09-15-testflight-shots/140.jpg (build 15, 07:37Z)
- **Quote:** "Is this not correct?"
- **On screen:** `build_sentence` step, "Build: Let's leave the house early" — Spencer's submitted order: うち を はやく 出よう(でよう). Marked "✗ Not quite — Correct answer: はやく うちをでよう". Bank remainder below (all used, greyed): 池(いけ), こうえん, うち, で / はやく, を, 出よう(でよう). CONTINUE button.
- **Located step:** `sentence` beat, mode `build`, lesson `ja-m34-neo-2`. `grep -n "はやく うちを でよう" src/features/languages/ja/curriculum/ir/m34.ir.yaml` → `ir/m34.ir.yaml:263`: `{ kind: sentence, ja: "はやく うちを でよう。", en: "Let's leave the house early", exercises: [volitional, wo-object], mode: build }`. No `alsoAccept` field on this beat (confirmed: `grep -c alsoAccept ir/m34.ir.yaml` → 0 course-wide in this module).
- **Source:** `ir/m34.ir.yaml:263`. でる/でよう base atom: `courseAtoms.ts:624` (`kana: "でる", kanji: "出る", fromModule: "m33"`). `alsoAccepted` mechanism exists and is supported end-to-end (per memory: 47 ES alternates already ship this way) but is simply unused on this beat.
- **Links:** Local: `http://localhost:5399/ja/learn/lessons/ja-m34-neo-2?step=build_sentence.3` (approx). Prod: `https://app.openlingoapp.com/ja/learn/lessons/ja-m34-neo-2`.
- **Class:** authoring (content wrong/weak) / question.
- **Answer / finding:** Spencer's ordering is grammatically valid Japanese. はやく ("early/quickly") is an adverb, and Japanese adverb placement is flexible — it can sit sentence-initial (はやく うちを でよう, the authored answer) or immediately before the verb it modifies (うちを はやく でよう, Spencer's answer), with essentially the same meaning ("let's leave the house quickly/early"); if anything, adverb-right-before-verb is the more common default placement, so his order is at least as natural as the authored one. This is a straightforward missing-`alsoAccept` gap, not a comprehension error on his part.
- **Similar issues:** Zero `alsoAccept` entries exist anywhere in m34 (`grep -c alsoAccept ir/m34.ir.yaml` → 0), so **every** adverb-bearing build sentence in this module (はやく appears in at least 2 beats: `:263` and `:384`) has the same unaddressed word-order-flexibility gap.
- **Effort:** S — add `alsoAccept: ["うちを はやく でよう"]` to `ir/m34.ir.yaml:263`, recompile with `node scripts/compile-ir.mjs m34`.
- **Needs Spencer:** N — this is a clear-cut fix (adverb reordering is valid); flag for a broader "which adverb-placement alternates need alsoAccept" sweep if he wants one, but this specific instance doesn't need a judgment call.

---

### #141 — missing kanji on irregular-verb volitional
- **Shot:** docs/user-feedback/2026-09-15-testflight-shots/141.jpg (build 15, 07:38Z)
- **Quote:** "Missing kanji"
- **On screen:** `listening_comprehension` step, "LISTEN AND ANSWER". Transcript shown: 来週(らいしゅう, furigana over kanji) またこよう (plain kana, no kanji). "What does this mean?" with 3 answer options visible: "I came again last week", "Let's go there next week", "Let's come again next week" (the correct meaning). CHECK button (disabled/no selection made yet).
- **Located step:** `listening-comp` beat, lesson `ja-m34-neo-2`. `grep -n "らいしゅう また こよう" src/features/languages/ja/curriculum/ir/m34.ir.yaml` → `ir/m34.ir.yaml:269`: `{ kind: listening-comp, audio: "らいしゅう また こよう。", answer: "Let's come again next week", distractors: ["I came again last week", "Let's go there next week", "I won't come next week"], q: "What does this mean?", ... }`.
- **Source:** `ir/m34.ir.yaml:269`. こよう is an m34-local `newAtom` with **no `kanji` field** (`ir/m34.ir.yaml:195`: `{ kana: こよう, ..., derivedFrom: くる, verbClass: irregular }`) — this matches all 17 volitional-form atoms in m34 (lines 180-196), none of which carry a `kanji` field. But the base verb くる **does** have a registered kanji mapping: `n5Kanji.ts:244-253` — `character: "来"`, `kunyomi: ["く.る"]`, `anchorVocab: ["kuru"]`, `introducedAtModule: 10`. My best-supported hypothesis for why 来週 kanji-surfaces but こよう doesn't: くる is one of only two true irregular verbs in Japanese (する/くる), and its volitional mutates the leading kana itself (く→こ), unlike regular godan/ichidan verbs whose stem is preserved across conjugation (のむ→のもう keeps の-; でる→でよう keeps で-, which is why item #140's 出よう correctly kanji-surfaced). If kanji substitution matches against the registered kunyomi reading ("く"), a form that no longer contains that kana string (こよう has no く at all) would never match — a structural gap specific to irregular-verb conjugations, not a general kanji-surfacing failure. I did not trace `applyKanjiSurfaces.ts`'s exact matching algorithm line-by-line to fully confirm this — flagging as strong hypothesis, not certainty.
- **Links:** Local: `http://localhost:5399/ja/learn/lessons/ja-m34-neo-2?step=listening_comprehension.1` (approx). Prod: `https://app.openlingoapp.com/ja/learn/lessons/ja-m34-neo-2`.
- **Class:** kanji-surfacing.
- **Answer / finding:** See hypothesis above. Separately noted: the screenshot shows only **3** answer options (missing the 4th, "I won't come next week", from the authored `distractors` list) — worth a second look, but not what Spencer flagged here so not chased further.
- **Similar issues:** `grep -rn "verbClass: irregular" src/features/languages/ja/curriculum/ir/*.ir.yaml | wc -l` → **31** irregular-verb-derived atoms course-wide (する/くる conjugations) — if the hypothesis is right, all こ-initial/き-initial くる forms (こない, きます, こられる, etc.) share this same never-kanji-surfaces gap; する forms are a non-issue since する has no kanji form in ordinary use.
- **Effort:** M — needs `applyKanjiSurfaces.ts`'s matching logic actually read/confirmed first (not done in this pass), then likely a special-case entry for くる's irregular stem the same way `RULESET_ALTERNATES`/`derivedFrom` already special-case irregular conjugation elsewhere.
- **Needs Spencer:** N to investigate further (engineering task); Y eventually on whether 来 should kanji-surface on ALL くる conjugations or just the dictionary form (product call on N5 authenticity — 来よう/来ます etc. are legitimate but こよう especially is very often left in kana even by native speakers).

---

### #142 — no translation shown on listening-build completion
- **Shot:** docs/user-feedback/2026-09-15-testflight-shots/142.jpg (build 15, 07:39Z)
- **Quote:** "We need to use the space better here, an English translation should populate when they get it right"
- **On screen:** `listening_build` step, "▶ Build what you hear." Tray (correct, all used/greyed in bank below): 来週(らいしゅう) りょこう に 行く(い) つもり だ. Distractor bank tiles: いる, に(dup), 遊ぶ, で, plus greyed 来週/だ/つもり/行く/りょこう. Large empty space between the header chips and the "Build what you hear." prompt. CONTINUE button.
- **Located step:** `sentence` beat, mode `listening`, lesson `ja-m34-neo-2`. `grep -n "りょこうに いく つもりだ" src/features/languages/ja/curriculum/ir/m34.ir.yaml` → `ir/m34.ir.yaml:272`: `{ kind: sentence, ja: "らいしゅう りょこうに いく つもりだ。", en: "I intend to go on a trip next week", exercises: [tsumori-desu, ni-location], mode: listening }`.
- **Source:** `ir/m34.ir.yaml:272`. Confirmed no translation is ever shown for this step type: `src/features/lesson/components/steps/ListeningBuildStepView.tsx:369` — `{submitted && !isCorrect && <Feedback correct={false} />}` — `Feedback` (which is where an explanation/translation would render) is gated to the **incorrect** path only; on a correct answer there is no `Feedback`, no English gloss, nothing beyond the green Continue button (`:381`, `variant={isCorrect ? "correct" : "incorrect"}`). The `ListeningBuildStep` type itself (`types.ts`) carries no learner-facing meaning field beyond the generic `prompt` ("Build what you hear.") — the authored `en` ("I intend to go on a trip next week") is compiled away and never surfaces to the learner in this step type, right or wrong.
- **Links:** Local: `http://localhost:5399/ja/learn/lessons/ja-m34-neo-2?step=listening_build.2` (approx). Prod: `https://app.openlingoapp.com/ja/learn/lessons/ja-m34-neo-2`.
- **Class:** feature-request (also explains the "wasted space" complaint).
- **Answer / finding:** Confirmed gap, and it directly explains both halves of Spencer's comment: the big empty vertical band above the prompt is the step's standard `stage-center` vertical-centering layout (`ListeningBuildStepView.tsx:188`, same pattern used across every step in this lesson, e.g. `ConjugationTransformStepView.tsx`'s documented `mt-auto` centering) — it isn't unique to this step, but because this step never shows *anything* extra on success (no translation), that centered empty space reads as pure waste rather than intentional breathing room the way it does on steps with more content. Adding the English meaning on correct completion would both answer his UX ask and fill the dead space.
- **Similar issues:** The same gate (`submitted && !isCorrect`) is worth checking on other production-modality steps — not re-verified per-component here (bounded per brief), but `ListeningComprehensionStepView.tsx` and `BuildSentenceStepView.tsx` should be spot-checked for whether they already show a translation on success (if they do and `listening_build` doesn't, that's an inconsistency across siblings per CLAUDE.md's "sibling-parity check").
- **Effort:** M — add a success-path translation reveal to `ListeningBuildStepView.tsx` (new UI, plus ensure `promptEn`/a stored `en` gloss is threaded through `listeningBuildSentence()` at `grammarHelpers.ts:1865-1886`, which currently only stores `prompt: opts.promptEn` = the generic "Build what you hear." instruction, not the sentence's actual meaning).
- **Needs Spencer:** N — clear, well-scoped ask; implement directly once sibling-parity is checked.

---

### #143 — match-pairs tile padding/sizing inconsistent
- **Shot:** docs/user-feedback/2026-09-15-testflight-shots/143.jpg (build 15, 07:41Z)
- **Quote:** "Font sizing and formatting issue, look at all the wasted padding"
- **On screen:** `match_pairs` step, "Match each Japanese word to its meaning (review)", 3-dot page indicator top right. Left column (Japanese, shuffled independently of right column — not row-paired): ばんごはん (selected, red outline), 明日(あした, furigana), うち, しゅくだい, へや, (more below fold). Right column (English): dinner, home (my place) [wraps to 2 lines], homework, room, TV, (more below fold). Single-line tiles (dinner, homework, room, TV, うち, しゅくだい, へや) show large empty vertical padding compared to the 2-line "home (my place)" and furigana-bearing 明日 tiles, even though all tiles in the grid appear the same height.
- **Located step:** `match_pairs` review card for lesson `ja-m34-neo-2`. `grep -n "kind: match" src/features/languages/ja/curriculum/ir/m34.ir.yaml` → **0 hits** — no authored `match-pairs` IR beat exists anywhere in m34, so this is an auto-generated end-of-lesson review card (drawing on the lesson's own taught words — ばんごはん、あした、うち、しゅくだい、へや all appear as objects in this lesson's own build sentences at `ir/m34.ir.yaml:261-267` — plus the module's `reviewPool`), not traced to its exact generator function in this pass (bounded per brief).
- **Source:** `src/features/lesson/components/steps/MatchPairsStepView.tsx:238-247` — the grid uses `gridTemplateRows: repeat(${rows}, minmax(min-content, 1fr))` with `maxHeight: calc(${rows} * 4.75rem + ...)` (line 247). With `1fr` row sizing, once the tallest row's content (the 2-line "home (my place)" cell, or the furigana-bearing 明日 cell) sets a floor, the CSS grid's `1fr` distribution stretches every OTHER row — including single-line, no-furigana tiles like "TV"/"room"/うち — to match that same allocated height, producing dead vertical padding inside the short tiles. Font-size `clamp()` values differ by tile type/density at `:374-378` (target-column tiles) and `:464-468` (source-column tiles).
- **Links:** Local: appears at the end of `http://localhost:5399/ja/learn/lessons/ja-m34-neo-2` (review card, not directly step-addressable by index without playing through). Prod: `https://app.openlingoapp.com/ja/learn/lessons/ja-m34-neo-2`.
- **Class:** sizing/layout.
- **Answer / finding:** Root cause is `MatchPairsStepView.tsx:238-247`'s `minmax(min-content, 1fr)` row template: it makes every row in the grid share equal height once total content is under the `maxHeight` cap, so short single-line tiles inherit the same allocated height as the tallest (furigana or 2-line-wrap) tile in the set — exactly Spencer's "wasted padding." This is the same family of issue as #137 (build-tile height inconsistency), just the match_pairs component instead of the build_sentence one.
- **Similar issues:** This is a component-wide behavior (affects the grid on every `match_pairs` step, not just this one instance) — no separate per-lesson grep needed; the fix belongs in `MatchPairsStepView.tsx` itself, not content.
- **Effort:** M — change row sizing so each row's height is driven by its OWN content (`min-content` per row without the shared `1fr` stretch) or, if uniform height is actually wanted, size every tile to a fixed height tall enough for the furigana case and vertically-center shorter content within it deliberately (rather than the current implicit stretch).
- **Needs Spencer:** Y — pick a direction: (a) let each tile be its own natural height (rows won't align across columns), or (b) keep uniform row height but only pad it to accommodate typical content, with an explicit "reserve space for furigana" reservation such as the `.kana-helper` mobile floor already does elsewhere.

---

## Cross-item themes (your range)

- **Kanji/furigana surfacing gaps are NOT one bug — at least 3 distinct mechanisms, confirmed separately:**
  - #133 / #138: `particle_cloze` blanks and options are **deliberately** kana-only by design (`types.ts:1035-1041`, "the blank itself stays kana — it is the answer") — not a gate, a documented product decision. Affects all 559 particle-cloze beats course-wide.
  - #141: irregular-verb (する/くる) conjugated forms likely never kanji-surface because kanji substitution appears to key off the base verb's kunyomi reading string, which doesn't survive くる's irregular stem mutation (く→こ) — a structural gap, best-hypothesis not fully confirmed against `applyKanjiSurfaces.ts`'s source. Affects up to 31 irregular-derived atoms.
  - #134 (aside): てがみ (m4, has a kanji field) renders in plain kana in a build tile at m34 — not diagnosed, flagged only.
  These are **not the same gate** — don't batch a single fix across all three; #133/#138 need a product decision (wire up optional display-only kanji), #141 needs an engineering read of the actual substitution algorithm, and #134 needs its own look.

- **Tile-height/padding inconsistency is one design gap wearing two costumes:** #137 (build_sentence tiles, furigana vs no-furigana height mismatch, general "MAKE A QA PAGE" ask) and #143 (match_pairs grid, `1fr` row-stretch causing dead padding in short cells) are the same underlying problem — no shared height/padding token across the tile-rendering components — surfacing in two different StepView implementations (`BuildSentenceStepView.tsx`/`BuildTileSurface.tsx` vs `MatchPairsStepView.tsx`). #137's requested QA page (file inventory scoped, not built) would need to cover both.

- **Missing `alsoAccept`/word-order flexibility (#140) and same-sentence-repeated-across-step-types (#138) are both m34-wide authoring-doctrine gaps, not isolated slips:** m34 has zero `alsoAccept` entries and zero `bankExtras` entries anywhere in the module (`grep -c` confirms both at 0), and at least one confirmed sentence-reuse-across-step-types violation (#138) against CLAUDE.md's own "don't recycle one sentence through step types" rule. Worth a dedicated m34 authoring re-pass rather than three separate point fixes.

- **The "quick-fix" reactive modal (#132) and the sibling `RuleHintCard` it was copied from diverged on one CSS rule** (`overflow-y-auto`/`max-h-*`) — a one-line parity fix, not a design problem. Two other `fixed inset-0` overlays (`LessonIntro.tsx`, `TestRunner.tsx`) share the same missing-scroll gap and are worth the same check.

- **Two items (#135, #139) hit the limits of what a single screenshot can diagnose:** #135's "harder sentence shown first" claim doesn't match the authored/compiled beat order I could verify, and #139's distractor-tile source didn't fully trace through the code paths I checked. Both would benefit from either a live re-walk with screenshots of the immediately-preceding step, or a `?step=` deep-link capture, rather than further static-code guessing.

- **Two items (#134, #140) are genuine "is the app wrong or is Spencer's Japanese instinct right" questions, and in both cases the answer was independent of any code defect** — #134 the app is correct (だす is the standard collocation for 手紙, more so than おくる), #140 Spencer is correct (うちを はやく でよう is valid word order, just not authored as an accepted alternate). Good pairing to show he's not just finding bugs — his linguistic instincts are also catching a real content gap (#140) and are also sometimes answered by existing correct authoring (#134).
