# 2026-09-15 — TestFlight feedback, build 13 (Spencer)

Pulled via the App Store Connect API
(`GET /v1/apps/6805652204/betaFeedbackScreenshotSubmissions`). Screenshot
submissions in scope: **27** (rows n = 86–112 in the API; item numbers here
are row n + 1, i.e. **#87–#113**, because #86 was already used for the prod
chunk-load bug in the b12 ledger). All 27 from **Spencer**, device
`iPhone16_2`, build 13, 2026-09-15 00:21–00:50Z. Crash submissions: 0.

Screenshots: `2026-09-15-testflight-shots/<item>.jpg` (87.jpg … 113.jpg).

Status legend: `open` / `fixed <sha>` / `in progress (uncommitted)` /
`wontfix (reason)` / `discuss`.

**Working-tree note (read before triaging #92):** `git status` at write time
showed `src/features/placement/engine/deriveModuleTestOut.ts` and its test
already modified and uncommitted — someone (concurrent session) is already
mid-fix on exactly the ask in #92 (`word_image_mcq` removed from
`TESTOUT_FORMATS`, comment literally quotes this feedback). Not yet
committed/pushed. See #92's row.

## Table

| # | Shot | Screen / component | Verbatim | Class | Proposed fix | Needs-Spencer? | Status |
|---|---|---|---|---|---|---|---|
| 87 | 87 | Test-out · Give & receive I, Build step, "commemorative watch" sentence (`BuildSentenceStepView.tsx` via `PlacementTestPage.tsx`) | "Looks like we didn't shrink the furigana small enough here and instead shrunk the other hiragan and kanji, needed to be the other way around, look at all the ugly dead space above the non furigana words" | layout-fit | Root cause confirmed in `src/index.css:819` — the mobile media-query floor `.kana-helper { font-size: max(0.55em, 0.75rem) }` (12px floor, Spencer's 2026-09-09 "keep 12px" call) is now BINDING on the already-shrunk build tiles (tiles cut 15% for b12 #69, furigana cut 15% for b12 #70a/#73 — both landed, see `index.css:436-464`). The floor stops the reading from shrinking further than the tile did, so on short kana-only tiles (で/に/を/から) the furigana row reserves its full 12px band with nothing above it, reading as "dead space." Fix: either lower the mobile floor below 12px for build/listening tiles specifically, or stop reserving the ruby band on tiles with NO furigana (kana-only tiles shouldn't get a `.kana-helper` row at all — check whether `BuildTileSurface.tsx` renders an empty ruby slot for every tile uniformly). Same class as #88/#95. Size: S–M. | Y (12px floor is Spencer's own earlier call — relaxing it needs his sign-off) | open |
| 88 | 88 | Test-out · Give & receive I, Build step, "hospital medicine" sentence, 11/12 | "Another example here, maybe it's a mobile issue but furigana too big, still too much padding, you need to test in Xcode device simulator or something" | layout-fit | Same `.kana-helper` mobile-floor issue as #87 — this is a real device (iPhone16_2, TestFlight), not a simulator-only artifact, so the ask to "test in Xcode simulator" is already satisfied by this being a live device report; no separate repro-tooling gap. Fix bundled with #87/#95. Size: S–M (one shared fix). | Y (same floor-relaxation call as #87) | open |
| 89 | 89 | Test-out · Give & receive I, Build step, "admitted to hospital, kind doctor" sentence, 0/12 — visible scrollbar (11-tile bank), "So close" banner (1 word missing) | "There should be no scroll here, so close should shrink text size and fix the box smaller, scroll needs to be deactivated if someone is moving a tile too" | layout-fit / nav-behavior | Two asks: (a) the "So close" banner (`BuildSentenceSoClose.test.tsx` covers this UI) adds height below an already-tall 3-row, 11-tile bank on a long sentence, forcing a scroll — the tray's `min-h` was already shrunk for b12 #75 (now `min-h-[54px]`/`min-h-[48px] sm:min-h-[61px]`, `BuildSentenceStepView.tsx:615,649`), so the remaining overflow driver on this item is tile COUNT (11) + banner height, not the box floor; worth checking whether the banner should render as a smaller inline toast instead of a full block on tall sentences. (b) `SortableBuildTiles.tsx:111-119` scopes `touch-action:none` to the PLACED tray only, by design ("the bank tiles are tap-to-place and keep native scrolling") — Spencer's ask to kill scroll while "moving a tile" implies extending `touch-action:none` to bank-tile press-and-hold too, trading native scroll for drag-safety on the bank row. Size: M. | Y (banner-height accepted tradeoff already documented once; the touch-action extension is a real behavior tradeoff) | open |
| 90 | 90 | Test-out · Give & receive I, "Build what you hear" step, 1/12 (`ListeningBuildStepView.tsx`) — bank includes both うた「歌」(uta, noun "song") and うた「歌う」(utau, verb "to sing") as separate distractor tiles | "This is a weird sentence, double uta aren't necessary no?" | content-gloss | The play-button size complaint from b12 #69 is already fixed here (`ListeningBuildStepView.tsx:190-206`, compact 44/48px inline button, comment dated 2026-09-14) — not a regression, don't re-open. The actual bug: the build-tile distractor generator (`moduleCompiler.ts`, distractor selection around lines 140/1478-1487) doesn't dedupe candidate distractors by lemma/stem, so 歌 and 歌う (same root, two inflections) both land in one bank. Fix: skip a distractor tile whose stem/kana-root already has a sibling tile in the bank. Size: S–M. | N (mechanical dedupe rule) | open |
| 91 | 91 | Test-out · Give & receive I, Listening comprehension MCQ, 2/12 (`ListeningComprehensionStepView.tsx`) — audio 「ドアがあく」("The door opens"), options "Is it a cat?" / "Is it a horse?" / "The door opens" | "This is a bad question to ask, not sure if it was in the spirit of review but it's bad, find something to replace this and similar 'low module short sentences' with, they don't belong in more advanced course work outside of review" | content-gloss / feature | Same complaint class as an earlier drive's low-level-sentence-in-advanced-review ask. `deriveModuleTestOut.ts` samples a module's own `reviewPool`, which recycles early-module filler sentences (ドアがあく is a basic M5-ish intransitive-verb drill) as MCQ distractor flavor regardless of how far above that level the target module sits. Fix: add a floor to the derivation/selection in `collectGradable`/the picker so a step's SOURCE module can't be more than N modules below the TARGET module being tested out of. Size: M (selection heuristic + `deriveModuleTestOut.test.ts` regression case). | Y (exact "how far below is too far" threshold, and whether to backfill with a same-level item instead) | open |
| 92 | 92 | Test-out · Give & receive I, Image MCQ, 6/12 (`WordImageMcqStepView.tsx`) — "What is the word for 'a birthday'?" (4 image options) | "Image mcq is a bad test for testing out, needs to be black listed and filled with something else for sure" | feature | **Already in progress, uncommitted.** `deriveModuleTestOut.ts:48-56` now excludes `"word_image_mcq"` from `TESTOUT_FORMATS`, with an inline comment quoting this exact feedback verbatim ("Spencer 2026-09-15: image mcq is a bad test for testing out…") and citing coverage math (removes 1–15 items/module across ja/ko/es, no module drops below `TESTOUT_DERIVED_FLOOR`). `deriveModuleTestOut.test.ts` updated in lockstep. Needs `npm run preflight` + push to land. Size: done, just needs shipping. | N | in progress (uncommitted) |
| 93 | 93 | Test-out · Give & receive I, Match-pairs review, 8/12 (`MatchPairsStepView.tsx`) — はなたば paired with "bouquet" (also ケーキ/cake, ノート/notebook, 時計/watch, くすり/medicine, カード/card) | "Bouquet Audio renders as wanataba instead of hanataba, needs kanji generation input into the TTS probably" | audio | TTS for はなたば (hanataba, "bouquet") mis-synthesizes as "wanataba" (h→w). Audio isn't in this repo — pipeline is `scripts/emit-tts-deck.mjs` (this repo, emits the deck spec) → `python -m pipeline.tts.generate` (`../lingo-data/`) → CloudFront, keyed by `getTtsUrl()`'s `sha256("<lang>:<text>")`. Same class as #100/#106 (see lane C) — a kana-only or ambiguous-kanji word getting a bad reading from the synthesis step. Size: S per clip (regen once flagged) — the real cost is DETECTION, which is what the planned ASR sweep is for. | N | open |
| 94 | 94 | Test-out · Give & receive I, Fill-in-the-blank, 9/12 (`FillBlankStepView.tsx`) — "It'll probably rain tomorrow" → 明日 あめだろう, distractor tiles 元気/明日/外国/病院 | "Good lesson type IF this kanji gets taught in this module" | content-gloss | Conditional praise — needs a verification pass, not a fix by default: check 明日 (あした)'s `fromModule`/`introducedByLessonId` in `courseAtoms.ts` against kanji-recognition unlock (`KANJI_RECOGNITION_MODULE=8`, furigana window unlock+2 per CLAUDE.md) and against which module this Give-&-receive-I test-out is actually sampling from — confirm 明日's kanji surface is legitimately unlocked by that point, not just carried along by the derivation. Size: S (verification; no code change unless a mismatch turns up). | N unless mismatch found | open |
| 95 | 95 | Test-out · Give & receive I, Match-pairs review, 11/12 — お金/hat, 傘/dictionary(mismatch shown mid-drag), 帽子/bicycle, 辞書/umbrella, 切符/ticket, 自転車/money (6 pairs, all fit on screen without scroll) | "Yeah good fitting all cards on the screen now but still the furigana issues, let's look into that together for sure, need to make sure mobile and web are covered" | layout-fit | Confirms the CARD-COUNT/grid fix already landed (all 12 match tiles fit without scroll now) — only the furigana-floor issue remains, and Spencer explicitly wants both mobile AND web checked. Same `.kana-helper` mobile-floor root cause as #87/#88. Size: bundled with lane A. | Y (same floor call, now explicitly cross-surface) | open |
| 96 | 96 | Flashcard reviewer, "This session" settings sheet (`ReviewSettingsPanel.tsx`) — Highlight particles, Grading buttons (Simple 2), Show scheduling intervals, New cards per session, Frequency vocabulary | "This settings page is not intuitive at all, look at other mobile app settings page designs to get a better feel of how these should functionally work" | visual-polish / info-density | Vague general-polish ask, same shape as b12 #83's "too flat" — not concrete enough to size without Spencer pointing at specifics (grouping? labels? which control is confusing?). Size: unscoped. | Y (needs him to name what's unintuitive before this can be scoped) | open |
| 97 | 97 | Flashcard reviewer, Recognition front — いかが, "Tap to reveal" | "Can we correctness check the meaning of how here I'm not sure if it's accurate" | content-gloss | `courseAtoms.ts:409` — `ikaga`, gloss "how", `blocked: true`, note "interrogative adverb — abstract grammar", m21. Directionally correct but register-flat: いかが is the POLITE/formal register of どう ("how"), used chiefly in offers/inquiries (いかがですか = "how is it / would you like"), not the general-purpose "how" a learner reaches for first. Fix: tighten the gloss (e.g. "how (polite, when offering/asking)") or confirm どう is taught as the primary "how" elsewhere. Size: S (one gloss string). | Y (register nuance is a wording call) | open |
| 98 | 98 | Flashcard reviewer, Recognition front — かける, "Tap to reveal" | "Does this mean to call? Do we teach that over Denwa Suru or something, we need to make sure our Japanese is based on currently used verbiage, maybe a local model search the web can do better checks for us there and for all languages, we ideally want people speaking like your average 30 year old if we can lol" | content-gloss | `courseAtoms.ts:421` — `kakeru`, gloss "to call by phone", `blocked: true`, m32, note "no picture debut… でんわ's 📞 owns the glyph (m19/m24 ruling: a debut distractor the picture names equally well)". Confirms かける (電話をかける) and でんわする (電話する) both exist as ways to express "to call" — same pattern as b12 #74's げんご/ことば precedence question. Needs a decision on which is the course's PRIMARY taught path for "to call" and whether かける should stay a secondary/blocked exposure atom. Size: S. | Y (explicit ask; naturalness/precedence call, same shape as b12 #74) | open |
| 99 | 99 | Flashcard reviewer, Recognition front — たくさん (kana only, no kanji shown), "Tap to reveal" | "We need to show them the kanji anyways for flash cards even if they haven't learned it yet, it's the freest form of passive exposure and they can always read the furigana" | feature | たくさん has a kanji form (沢山) not rendered on the flashcard front, even though the lesson-side kanji surface substitution (`ja/secondScript/applyKanjiSurfaces.ts`, live from `KANJI_RECOGNITION_MODULE=8`) exists elsewhere. This is a deliberate policy ask: always show kanji + furigana on flashcards regardless of the learner's unlock window, since furigana makes it passively readable either way. Files: `src/features/flashcards/components/CardFront.tsx` (front-render path — currently doesn't appear to call `applyKanjiSurfaces`), cross-check `applyKanjiSurfaces.ts`. Size: M (new always-on render path + interaction with the furigana-visibility toggle). | Y (deliberate cross-surface policy change — kanji-always vs current unlock-gated display) | open |
| 100 | 100 | Flashcard reviewer, Meaning-side card — "chopsticks" (English front + icon), Recognition state, Didn't know / Knew it | "The audio generated as washi, kanji or creative regen?" | audio | TTS for はし (chopsticks, 箸) mis-synthesizes as "washi". Same audio class as #93/#106 — see lane C. Word is part of the はし homophone set (箸/chopsticks, 橋/bridge — #106, 端/edge). Size: S per clip. | N | open |
| 101 | 101 | Flashcard reviewer, Recognition front — ほんとう (本当, "truth" / "really" — dual noun/adverb sense), "Tap to reveal" | "For words like this with clear other definitions, including a few is good, and then we also need an option to read example sentences on flash cards, we can very easily mine an occurrence for every word and deliver it IF they want it, along with audio, let's people have a more whole flash cards experience if they want" | feature | Endorses the multi-definition display pattern seen in #103 (extend it consistently — ほんとう's noun "truth" / adverbial "really" senses). Second, NEW ask: on-demand example sentences + audio per flashcard, mined from real course occurrences. `src/features/lesson/data/minedSentences.ts` already mines a target-sentence occurrence per atom for `listening_build` steps (`if (s.type === "listening_build" && typeof s.targetSentence === "string")`) — a plausible existing source to extend into an opt-in flashcard section, paired with `getTtsUrl()` for audio. Files: `CardFront.tsx` / `ReviewCard.tsx` (new expandable section), `minedSentences.ts` (extend as the sentence source). Size: L (new feature, cross-surface, needs a UI slot). | Y (feature scope + priority — this is a real backlog item, not a bug) | open |
| 102 | 102 | Flashcard reviewer, Recognition front — ゆっくりと, "Tap to reveal" | "Is this correctly done for slowly or is the と an artifact" | content-gloss | Not a rendering artifact — `courseAtoms.ts:484` (`yukkurito`) has the と baked directly into the atom's `kana` field ("ゆっくりと"), not appended at render time. Judgment call: the more common citation/dictionary form is bare ゆっくり (と is an optional adverbial-emphasis particle attached in use, not part of the headword); registering the LEMMA as ゆっくりと conflates headword with a common collocation. Recommend re-registering as ゆっくり unless there's a specific pedagogical reason to teach the と-marked form as citation. Size: S (one atom edit), but check other references to atom id `yukkurito` before renaming/re-kana'ing (sibling-parity). | Y (citation-form call) | open |
| 103 | 103 | Flashcard reviewer, Meaning-side card — "(1) one day, (2) first of month" (likely ついたち), Meaning | "Good display of the multiple definitions here but maybe we use like other 1 and 2 inline text icons to display, take less vertical space or we bullet them in a list? Something to think about" | info-density | Praises the multi-def content (see #101) but wants a tighter LAYOUT — replace the "(1)/(2)" inline text prefixes with small numbered chip icons, or a bulleted list, to cut vertical space. Locate the definition-list renderer in `src/features/flashcards/components/` (likely inside `CardFront.tsx`'s back-face branch — not pinned to an exact line this pass). Size: S–M (pure display change). | Y (exact visual treatment — chips vs bullets) | open |
| 104 | 104 | Flashcard reviewer, Recognition front — べんきょうする (small reading, WITH する) above 勉強 (large kanji, WITHOUT する), "Tap to reveal" | "Missing suru below and shouldn't be up top" | content-gloss | The card splits the suru-verb across two rows: the small reading line keeps する, the large kanji headword drops it (bare 勉強, the noun stem). Spencer wants する on the PROMINENT line, not just the small reading. This is a kanji/reading-split bug in the card-front render for suru-verb atoms (`conjugation.class: "irregular"` + a kanji field), not a content question. Files: `CardFront.tsx`'s kanji/reading split logic, cross-check `applyKanjiSurfaces.ts`'s suru-verb handling. Size: M. | N (mechanical correctness — Spencer states the fix, not a question) | open |
| 105 | 105 | Flashcard reviewer, Recognition front — 大きな, "Tap to reveal" | "Should there be a distinction between い ending and な" | content-gloss | Data is already correct: `courseAtoms.ts:688` tags 大きな `pos: "determiner"` (NOT `na-adjective`) — the course already knows 大きな is a rentaishi (fixed pre-noun form, never conjugates, no だ/です), distinct from true い-adjective 大きい. So this isn't a mistagging bug; it's a learner-facing surfacing question — does the flashcard (or a linked rule card) show anything that communicates "this doesn't conjugate like a な-adjective" to the learner, or does it look identical to a real な-adjective card with no distinguishing marker? Size: S (a POS/attributive-only badge or note) if Spencer wants one added. | Y (whether/how to surface a distinction the DATA already has) | open |
| 106 | 106 | Flashcard reviewer, Meaning-side card — "bridge" (English front + bridge icon), Recognition state, Didn't know / Knew it | "Same as chopstick audio break, pick one kanji if either have and then use that audio" | audio | Same はし homophone set as #100: bridge (橋) and chopsticks (箸) share the reading はし. "Pick one kanji if either have" = TTS should key audio generation off the spoken READING (はし), not per-kanji-meaning, since 箸/橋/端 are pronounced identically — right now at least two of the surfaced instances (chopsticks, bridge) are producing bad/inconsistent audio. Size: S per clip, same lane as #93/#100 — the real fix is the ASR sweep identifying every broken reading in one pass rather than word-by-word. | N | open |
| 107 | 107 | Flashcard reviewer, Recognition front — 疲れる (つかれる, "to get tired"), "Tap to reveal" | "Small ask, it looks like it's dynamically resizing the word as the card open and the kanji sized up very slightly between switching over, small defect see if you can reproduce it see why" | visual-polish | Reproducible small defect, not a judgment call. Likely a 1-frame reflow when the card mounts — the furigana/ruby row (`.kana-helper`) settling in after the kanji text first paints, similar in shape to the line-height/margin tuning already done for lesson furigana (`index.css:436-464`) but possibly not ported to the flashcard `CardFront.tsx` path. `ReviewCard.tsx:141` has a bare `transition` class on the card container worth checking too. Size: S–M (repro + fix). | N | open |
| 108 | 108 | Flashcard reviewer, Recognition front — 皆さん (みなさん, "everyone"), "Tap to reveal" | "We have two of this" | feature (dedupe) | `courseAtoms.ts:847` — `minasan` (皆さん, "everyone"), note says explicitly **"synonym of みんな"** — and `courseAtoms.ts:480` — `minna` (みんな, "everyone") — both carry `introducedByLessonId: "ja-m19-4-2"` (the SAME lesson). Two near-identical "everyone" atoms both go live off one lesson and both surface as independent flashcards. See lane D root cause below (109-111 share the same mechanism). Size: S (courseAtoms edit — merge or suppress one) but sibling-parity check any lesson step referencing `minasan` directly before dropping it. | Y (which of minna/minasan is the one that stays reviewable) | open |
| 109 | 109 | Flashcard reviewer, Recognition front — 私 (わたくし reading), "Tap to reveal" | "Same here do we need multiple watashi for beginner levels, im only at m31" | feature (dedupe) | `courseAtoms.ts:855` — `watakushi` (私, わたくし, "(humble) I, myself", `blocked: true`, note "pronoun — rubric blocks pronouns", `fromModule: "future"`, `introducedByLessonId: "ja-m19-3-2"`) coexists with `courseAtoms.ts:280` — `watashi` (私, わたし, the standard taught pronoun, m1). `watakushi` is marked `blocked: true` but STILL carries a real `introducedByLessonId`, and `blocked` doesn't appear anywhere in the card-building path (`grep -rn "\.blocked\b"` across the flashcards/ja data pipeline returns nothing) — so `blocked` isn't actually suppressing this from becoming a reviewable card. Root-cause candidate for the whole 108-111 dedupe class: either the deck builder needs to start filtering on `atom.blocked`, or these near-synonym atoms shouldn't carry a live `introducedByLessonId` at all if they're meant to stay dormant. Files: `src/features/flashcards/data/courseDeck.ts`, `courseAtoms.ts:855`. Size: M (mechanism fix — a repro pass to confirm exactly which unlock path fired is the first step). | Y (is watakushi supposed to be reviewable pre-N4/N3 at all, and how to handle SRS state on cards already graded if it's retired) | open |
| 110 | 110 | Flashcard reviewer, Recognition front — 練習 (れんしゅうする, "to practice"), "Tap to reveal" | "Same as benkyou" | feature (dedupe) | `courseAtoms.ts:869` `renshuusuru` (練習する, verb, m34) vs `courseAtoms.ts:1530` `renshuu` (れんしゅう, noun, `blocked: true`, m30) — same dictionary-form/suru-verb dual-registration pattern Spencer is flagging by comparison to べんきょう(する) (#104). Same root cause and same fix as #109 (the `blocked` filter question). Size: bundled with #109. | N (mechanical once #109's filter mechanism is resolved) | open |
| 111 | 111 | Flashcard reviewer, Recognition front — 警官 (けいかん, "police officer"), "Tap to reveal" | "Why so many for police officer too? Are they all relevant?" | feature (dedupe) | Three police-related atoms cluster at/near the same intro point: `courseAtoms.ts:899` `keikan` (警官, "policeman", `introducedByLessonId: "ja-m17-8-2"`), `courseAtoms.ts:418` `omawarisan` (おまわりさん, "friendly term for policeman", SAME `introducedByLessonId: "ja-m17-8-2"`), and `courseAtoms.ts:1732` `keisatsu` (警察, "police", `blocked: true`, m40 — whose own note says it "would collide with courseAtoms' existing おまわりさん/けいかん stubs regardless"). At minimum keikan+omawarisan go live off the same lesson and both surface. Same mechanism/fix as #109. Size: S once #109's filter question is settled. | Y (which police word is the course's primary teaching form) | open |
| 112 | 112 | Flashcard reviewer, Recognition front — 切手 (きって, "stamp"), "Tap to reveal" | "Is this correct, is tegami just this swapped" | content-gloss | Gloss IS correct — 切手 (kitte) = "postage stamp." The confusion is real and pedagogically interesting though: 切手 and 手紙 (tegami, "letter") share the kanji 手 in swapped positions (切+手 vs 手+紙), making them easy to visually confuse — apparently even briefly for Spencer. Not a bug to fix in the gloss; the ask is whether the app has (or should build) a "confusable kanji-reuse pair" callout mechanism. No such mechanism located this pass. Size: S if a lightweight note suffices, M if a new confusable-pair feature is wanted. | Y (confirm no mechanism exists, and whether to build one just for this pair or fold into a broader pass) | open |
| 113 | 113 | Flashcard reviewer, Meaning-side card — "rice bowl" (English front + icon), "Synced 15 cards" toast | "It's showing me cards I already first try marked today, remember how Anki works I should have infinite reviews" | feature | `reviewQueue.ts`'s `buildQueueFromSubscriptions` (~line 188) explicitly backfills the queue from `notYetDue` — cards with SRS state that aren't due today — as "fuel for a free (extra-practice) session when nothing is otherwise scheduled," gated by `options.free`. If Spencer graded a batch same-day (rescheduling those cards into the future, no longer due) and then started a fresh session, this backfill can resurface just-graded cards as "free" extra practice — which reads as "showing me cards I already marked" if he didn't knowingly ask for free/practice mode. Files: `src/features/flashcards/engine/reviewQueue.ts` (`notYetDue` backfill), wherever the reviewer passes `free: true` (`ReviewShell.tsx`). This is SRS-queue-composition, not a new write surface (no state gets written extra), but touches scheduling semantics — read the SRS doctrine in CLAUDE.md first. Size: M (session-boundary UX decision). | Y (should "free" review ever auto-trigger on session start, or only on an explicit "Practice anyway" tap) | open |

## Screens seen

- **Test-out · Give & receive I** (`PlacementTestPage.tsx`, a 12-question derived test-out): #87, #88, #89 — Build step (`BuildSentenceStepView.tsx`); #90 — "Build what you hear" (`ListeningBuildStepView.tsx`); #91 — Listening comprehension MCQ (`ListeningComprehensionStepView.tsx`); #92 — Image MCQ (`WordImageMcqStepView.tsx`); #93, #95 — Match-pairs review (`MatchPairsStepView.tsx`); #94 — Fill-in-the-blank (`FillBlankStepView.tsx`).
- **Flashcard reviewer** (`ReviewShell.tsx` + `CardFront.tsx`, Recognition-mode session): #97, #98, #99, #100, #101, #102, #103, #104, #105, #106, #107, #108, #109, #110, #111, #112, #113.
- **Flashcard reviewer settings sheet** ("This session", `ReviewSettingsPanel.tsx`): #96.

## Fix lanes

**Lane A — furigana/tile sizing (test-out surfaces).** Items: 87, 88, 95, 89. Files: `src/index.css` (`.kana-helper` mobile floor, line ~819), `src/features/lesson/components/steps/BuildSentenceStepView.tsx`, `BuildTileSurface.tsx`, `SortableBuildTiles.tsx`, `BuildSentenceSoClose.test.tsx`. Root cause for 87/88/95 is one shared CSS floor — fix once. #89's scroll/touch-action half is separable (SortableBuildTiles) but small enough to land in the same pass.

**Lane B — test-out formats.** Items: 92, 91. Files: `src/features/placement/engine/deriveModuleTestOut.ts`, `deriveModuleTestOut.test.ts`. #92 is already uncommitted and done, just needs shipping; #91 needs a new module-distance floor in the same selection logic — do both in one pass since they touch the same file.

**Lane C — TTS readings (audio class).** Items: 93, 100, 106. All three are the same defect shape: a kana-only or homophone-ambiguous word (はなたば, はし×2) getting a wrong reading out of synthesis. Not fixable from this repo alone (audio lives in `../lingo-data/`) — this repo's role is `scripts/emit-tts-deck.mjs` + the manifest. Real fix is the planned ASR sweep to find every broken reading in one pass, then a batch regen.

**Lane D — flashcard dedupe + correctness.** Items: 97, 98, 102, 104, 105, 108, 109, 110, 111, 112, 90. Files: `src/features/languages/ja/courseAtoms.ts` (97, 98, 102, 105, 108, 109, 110, 111, 112 are all courseAtoms-level content/registration questions), `src/features/flashcards/data/courseDeck.ts` (the `blocked`-filter mechanism question shared by 108-111), `src/features/flashcards/components/CardFront.tsx` (104's suru-verb split), `src/features/lesson/data/moduleCompiler.ts` (90's distractor dedupe — different surface, same "near-duplicate word forms" theme, bundled here for triage convenience). 109 is the anchor item — resolve its `blocked`-filter question first, then 108/110/111 are largely mechanical follow-through.

**Lane E — flashcard features.** Items: 99, 101, 103, 107, 113. Files: `src/features/flashcards/components/CardFront.tsx` / `ReviewCard.tsx` (99's always-show-kanji ask, 103's multi-def layout, 107's resize defect), `src/features/lesson/data/minedSentences.ts` (101's example-sentence mining — reusable source already exists), `src/features/flashcards/engine/reviewQueue.ts` (113's free-review backfill). Five independent asks on the same surface; 107 (the defect) is the only one safe to ship without Spencer, the rest are feature/UX decisions.

**Lane F — settings polish.** Item: 96. File: `src/features/flashcards/components/ReviewSettingsPanel.tsx`. Unscoped until Spencer names specifics — same shape as b12 #83.

**Lane G — content verification.** Item: 94. File: `src/features/languages/ja/courseAtoms.ts` (明日/あした atom) + the test-out's source module. Verification only, no code change expected unless a mismatch turns up.

Lanes A, C, D, E, F, G touch mostly disjoint file sets (D and E share `CardFront.tsx` — sequence rather than parallelize those two if run as separate agents). Lane B is small and already half-done. Lane C is blocked on external tooling (ASR sweep, `../lingo-data/` regen), not this repo.

## Corrections after verification (Fable, 2026-09-15)

- **`blocked: true` is NOT a deck-exclusion flag.** Rows #109–#111 and lane D
  above hypothesise that a `blocked` filter is missing from deck building.
  Checked: `blocked` is the "no image MCQ / shared-glyph" flag documented at
  the top of `courseAtoms.ts` (538 of 1116 atoms carry it, e.g. たくさん, いかが,
  かける) — filtering on it would drop half the deck. The real mechanisms
  behind "we have two of this": (a) register pairs authored as separate
  atoms on purpose and taught in the same lesson (わたし/わたくし, おまわりさん/
  警官, みんな/皆さん) — a content decision for Spencer, not a bug; (b) new
  cards are built per subscribed deck and never sibling-deduped
  (`reviewQueue.ts`, lane D2 fix); (c) four suru-verb atoms whose `kanji`
  field held only the noun run (勉強/練習/掃除/散歩 vs …する kana) — #104/#110,
  lane D1 fix.
- **#92 was not "a concurrent session mid-fix"** — it was this wave's local
  coder lane (qwen3-coder-next via `claude-local`), which wrote its diff into
  the main checkout instead of its worktree; the diff was moved here.
- **#99 (たくさん):** the atom has no `kanji` field at all (沢山 is normally
  written in kana), so nothing is being hidden on that card. The general ask
  ("show kanji even if untaught") needs an audit of atoms missing a `kanji`
  field where kanji is the common orthography — Fable follow-up, not this pass.
