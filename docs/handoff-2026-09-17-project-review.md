# Project review 2026-09-17 — ledger

Scope set by Spencer 2026-09-17 10:55: ten areas (primitives, optimization, data storage, testing, lesson authoring, accessibility, observability, deps/platform, learning science, cost). Heavy Sonnet web-research lanes per area (12–20 sources each, contrarian views required). Implementation runs FULLY UNATTENDED after research, user-facing areas first, most impactful first, ≤3 implementation lanes at a time, each finished end to end before the next starts. One TestFlight build per landed area. Allowed without a separate decision: server schema/endpoint changes, major dependency upgrades, tool-driven content re-shaping, Android work. Authoring block lifted for this review. Search first even where a solution exists; say "I don't know" plainly.

Standing rules still apply: explicit-path staging, commit checklist (fetch+rebase, preflight for code/content, watch run conclusions, verify prod by chunk content), one push per lap, no bare stash, ledger every step with `date`.

## Ledger

- 2026-09-17 10:55 — Scope agreed (see above). Baseline: main + worktree at 73b627ba, worktree clean. Facts: content JSON ja 8.3 MB / es 2.9 / fr 1.2 / ko 0.9; src/pub 35 MB; build 25 IPA 29.5 MB; 750 test files; 223 docs; React 19.2, Vite 6, Vitest 4, Capacitor 8.5, dnd-kit core 6.3 / sortable 10, TS 5.6. Three inventory agents (content/sync, UI/tests/platform, docs index) running; eight research lanes dispatched next.
- 2026-09-17 11:00 — Inventory landed (3 agents); facts written to docs/project-review-2026-09-17.md §1; research lanes primitives + storage returned (§2). Dispatching implementation lanes P1 (huge-bank 125 % + prompt shift, opus), P2/P3 (12 option views → Tile primitive, sonnet). Shared brief: scratchpad/review/lane-common.md; simulator mkdir-lock.
- 2026-09-17 11:03 — Research lanes 6/8 returned (primitives, storage, testing, authoring, learning, a11y+obs, perf); deps/cost lane still running. Perf lane hit the WebSearch quota — rerun later if its findings are thin. Ranked queue written to review doc §3. P1/P2/P3 running.
- 2026-09-17 11:03 — All 8 research lanes returned; review doc §2 complete, §3 queue has areas 1–9. Waiting on P1/P2/P3 (primitives) → build 26.
2026-09-17 11:08 — P2 — ConjugationClozeStepView options row onto Tile (kind=options-row, size=particle, tone=success); zero colour drift (already pixel-identical to particle/success). Commit a2f17460. Tests: 10/10 (src/features/lesson/components/steps/ConjugationClozeStepView.test.tsx). No live curriculum route today (conjugation_cloze authored in 0 lessons per STEP_COVERAGE_EMIT walk); no simulator capture possible, code+render-test verified only.
2026-09-17 11:09 — P2 — PretestMcqStepView options onto Tile (kind=grid gap=tight, size=pick); preserved the safe-guess warning-tone contract via !important className overrides (no Tile tone supports amber — flagged as a gap). Commit e1d5e8e3. Tests: 8/8. No live curriculum route today (pretest_mcq authored in 0 lessons); code+render-test verified only.
2026-09-17 11:10 — P2 — SelfExplanationMcqStepView options onto Tile (kind=grid, size=sentence, fr on 4-opt); zero colour drift (matches default option palette exactly). New test file (5 cases, none existed before). Commit 60cf8fae. Tests: 5/5. No live curriculum route today (self_explanation_mcq authored in 0 lessons); code+render-test verified only.
2026-09-17 11:13 — P2 — AgreementClozeStepView + AspectChoiceClozeStepView: LEFT UNMIGRATED (reasoned). Their per-blank option chips are inline in running prose (multiple blank-groups per sentence, ~36px chip on the text baseline); the only existing TileSize tier close in shape (particle) forces a 56-72px standalone-row height and 25%-flex-basis, which would break inline sentence flow, not fix it. No TileSize tier exists for a small inline chip. Gap flagged for the Tile.tsx owner. No code change; existing tests unaffected (11+9 passing).
- 2026-09-17 11:14 — P2 DONE: 4 views onto Tile (a2f17460 e1d5e8e3 60cf8fae ddb54ef4), 70 scoped tests green, DialogueListen sim PASS 100/125; AgreementCloze/AspectChoiceCloze left (inline chips need a `chip` TileSize tier — queued for the Tile owner after P1); conjugation_cloze/pretest_mcq/self_explanation_mcq have ZERO authored lessons (content note). Dispatching A3 observability lane as the third slot.
2026-09-17 11:20 — P3 — StressPatternStepView syllable row onto Tile (variant=option size=particle tone=success) + TileTray (kind=options-row); zero colour drift (tone=success maps exactly onto the 4 hand-rolled states). Commit 4175fe04. Tests: 17/17 (StressPatternStepView.test.tsx). No live curriculum route (stress_pattern authored in 0 lessons, grepped + confirmed via STEP_COVERAGE_EMIT missing list); code+render-test verified only.
2026-09-17 11:20 — P3 — SymbolRecognitionStepView 2x2 kana grid onto Tile (size=word-glyph, byte-for-byte transcription of the old py-9 text-5xl/6xl class string) + TileTray (kind=grid cols=2, dynamic gridAutoRows via style). State mapping switched to the sitewide idle/selected/correct/wrong convention (was solid-fill on pre-submit select; that look is now correct-only everywhere, disclosed as a deliberate non-pixel-parity change). New test file (6 cases, none existed before). Commit 0013100e. Simulator PASS 100/125 on ja-m1-b-1?step=10 (4 equal 192x136->188x151 tiles, 0 wrap/clip/overflow).
2026-09-17 11:20 — P3 — SymbolToSoundStepView icon+romaji grid onto Tile (size=reveal, 32px padding matches old py-8) + TileTray (kind=grid cols=2). Every shipped producer emits exactly 4 options (grepped) so the old dynamic 2/3-col branch is dead; TileTray's cols only supports 1|2 so a true 3-col grid isn't expressible — flagged as a gap, unreachable in practice. New test file (6 cases). Commit c7fa5809. Simulator PASS 100/125 on ja-m1-b-1?step=9 (0 wrap/clip/overflow).
2026-09-17 11:20 — P3 — TapTheWordStepView sentence chips onto Tile (size=particle tone=success) + TileTray (kind=options-row). 3/4 states map onto tone=success exactly; the 4th ("missed target", unselected-but-was-a-target, dashed outline) has no TileState — used state=selected + a bang-prefixed className override, per Tile.tsx's own documented escape hatch. GAP flagged: a dedicated missed/outline TileState would drop the override. tap_the_word authored in 0 lessons (grepped + STEP_COVERAGE_EMIT); /lesson-preview QA fixture confirmed incompatible with sim:capture (3/3 VALIDATION MISMATCH, no [data-lesson-stage]) — unverified on device, same class as the documented fill_blank gap. Commit fc8e8790. Tests: 8/8 (2 rewritten off literal colour classes onto data-state, 1 new).
2026-09-17 11:20 — P3 — WordMapStepView chip+gloss pairs onto Tile (size=particle, wrapped in a caption div so flex-grow is moot by design) + TileTray (kind=options-row). Solved/wrong/reveal-only map onto the built-in placed/wrong/spent states exactly; gender tinting (shared GENDER_STYLE, out of lane) stays a bang-prefixed className override — no tone in the primitive's accent/success/card vocabulary carries a 3rd grammatical-gender hue. Commit d0faeab9. Tests: 9/9 (1 rewritten onto data-state, 1 new).
2026-09-17 11:20 — P3 — WordImageMcqStepView grid container (already on Tile since 2026-09-16) moved onto TileTray (kind=grid) — data-tile-tray hook + --option-gap token replace the literal gap-3; the adaptive cols/rows/width/height budget math stays inline style on the same element (TileTray's own documented pattern for bespoke per-view geometry, same as match-grid). Commit 387748e9. Tests: 4/4 new assertions added to tileFont.test.tsx (12/12 file total). Simulator PASS 100/125 on es-m1-1?step=1 (4 equal 168x168->158x158 square cards, 0 wrap/clip/overflow; --allow-fallback-font — the Noto-Sans-JP check is JA-only, correctly WARNs on Spanish content).
2026-09-17 11:20 — P3 — RowTestStepView/TestRunner.tsx ASSESSED, no edit: row_test IS a live content step type (2 JSON files, ja/m1 + ja/m2, per grep of src/pub/content/v1). TestRunner.tsx renders no option markup itself — it dispatches every item to MultipleChoiceStepView / MatchPairsStepView / BuildSentenceStepView, all three already on the Tile primitive per mobile-sizing-spec.md §3. No work item; only mistake-dots (icon-based) and header chrome are TestRunner's own markup, neither is an option tile.
2026-09-17 11:20 — P3 DONE: 6 views onto Tile/TileTray (4175fe04 0013100e c7fa5809 fc8e8790 d0faeab9 387748e9), 50 scoped tests green (+ renderSmoke 3 passed/5 skipped), tsc clean. 4/6 simulator-verified PASS at 100%+125% (symbol_recognition, symbol_to_sound, word_map, word_image_mcq); 2/6 (stress_pattern, tap_the_word) have zero authored lessons in any course and the QA fixture page doesn't work with sim:capture's probe — code+render-test verified only, same disclosed class as fill_blank. 2 gaps flagged for the Tile/TileTray owner: no TileTray grid cols>2, no TileState for an unselected-but-target 'missed' look.
- 2026-09-17 11:22 — P3 DONE: 6 views onto Tile (4175fe04 0013100e c7fa5809 fc8e8790 d0faeab9 387748e9), 59 scoped tests green, sim PASS 100/125 on the 4 views with live routes; stress_pattern + tap_the_word have ZERO authored lessons (render tests only). Tile gaps queued: `chip` inline tier, grid cols=3, `missed` TileState, third tone family. row_test is live but delegates to primitive views (no work).

- P1 — 2026-09-17 11:25 b2e3a3dc: build-step layout decisions closed in code. ROOT CAUSE (Chromium DOM probe + the b24 device captures): (1) the 125% shrink was reserve-blind — the stage overflows with an EMPTY tray at 125%, so `planStageFill`'s shrink branch fires first, caps `rec.cap`, and b24's phantom reserve is never read; each new tray row re-triggered it (0.82 → 0.72 at tap 9). (2) the prompt shift has TWO mechanisms, both driven by tray growth: the step column is `flex min-h-0 flex-1 flex-col justify-center`, so any change in content height moves the content by half of it (device 100%: prompt 239.8 → 203.0 at tap 7 when the tray took a row, then back to 238.5 at tap 11 when the spent-tile collapse dropped a BANK row), and when the stage overflows the first tap's scroll-into-view moves everything (Chromium 125%: scrollTop 0 → 66). FIX: the huge-bank tray reserves the full answer in its VISIBLE ghost row (phantom deleted end-to-end: view, CSS, tileFit `phantomReserve`/`reservePx`, simProbe filter), and the huge-bank spent-tile collapse became a fade in place (no inline width/height) so the bank keeps its footprint. Harness: `promptStable` (1 px) + `chromeStable` (bank top + CTA top) verdicts + 3 table columns; unsampled fields report instead of passing silently. Tests: 2 BuildTrayRowNesting pins + 1 collapse pin fail on the b24 structure (proven), 2 tileFit cases each contrasted against the growing tray, +3 simProbe, +6 node. Device captures next.
2026-09-17 11:36 — A3 — Observability lane DONE end to end. Client: src/shared/telemetry/errorReporter.ts (dedupe/cap-20/backoff/offline-queue/sendBeacon, never throws) + src/shared/api/telemetry.ts, wired from window.onerror, unhandledrejection, AppErrorBoundary, RouteErrorBoundary, vite:preloadError (chunk-load), boot-guard firstError, installed once from main.tsx after providers. Server: lingo-core POST /api/core/v1/telemetry/errors (app/telemetry/{schemas,router,guard}.py, no auth, pydantic hard caps 1KB msg/4KB stack/20 items, body-size ASGI guard 413 + per-IP token bucket 429 BEFORE body parsing, one JSON line/item on lingo.client_error), X-Request-Id echoed on every error response (app/shared/request_id.py + 3 exception handlers in main.py), mounted in both full and beta SURFACE_MODE. Tests: client 33 (26 errorReporter + 7 telemetry.ts), server 17 (tests/test_telemetry_errors.py) — both scoped runs green, full suites green (client 118/118 scoped incl. pre-existing; lingo-core 369/369 full suite incl. new). tsc -b clean. Proved one client test (dedupe) and one server test (20-item cap) fail for real by breaking the guarded behavior, rerunning, then reverting — see report. Committed lingo 8cf4f637 (not pushed); lingo-core 9bf5cd1 on main (not pushed, per brief). Doc: docs/observability-2026-09-17.md (payload contract, Insights query, alarm CLI — not run, deploy recipe read off lingo-core's own deploy.yml, guard math, privacy statement). Three things left for Spencer: create the CloudWatch alarm (no ops SNS topic exists yet), push/merge the lingo-core commit to deploy (deploy.yml auto-deploys on push to main), confirm the already-declared 30-day log retention (lingo-infra/observability.tf) is actually applied to the live /aws/lambda/lingo-core group. Lesson/step context and lastRequestId are wired as opt-in setters but NOT called anywhere (LessonPage/ApiClient are outside this lane's owned files) — real end-to-end but unpopulated until someone wires the call site.
- 2026-09-17 11:38 — A3 DONE: client error reporter 8cf4f637 (33 tests), lingo-core telemetry endpoint 9bf5cd1 on its main (17 tests, 369/369 suite; NOT pushed/deployed). Spencer: alarm (no ops SNS topic exists), deploy by pushing lingo-core main, confirm 30-day retention applied. Unwired hooks setLessonContext + setLastRequestId → A3b.
- 2026-09-17 11:44 — A4 — Perf lane DONE. IPA anatomy (unzipped build 25 App.ipa, 29,271,821 bytes compressed): 72.3% (21.18 MB) is Kuromoji/Sudachi dict + ONNX WASM, both lazy-loaded (kanji-ASR fallback + opt-in Whisper), zero cold-open cost but shipped to every install; App Thinning removes at most ~0.3% (Assets.car only — no ODR tags, single-arch already). Bundle report: added rollup-plugin-visualizer@7.1.1 behind ANALYZE=1 (verified no-op on a normal build), read the treemap — only the i18next locale-bundle win (119.2 KB gzip eager, ~77 KB/user avoidable) clears the ≥50 KB/critical-path bar, and it's outside this lane's owned files (src/shared/i18n/i18n.ts) plus risks the protected 0.36 s cold-open number, so flagged not landed; markdown-vendor (586 KB gzip) already correctly lazy (forum-only); lucide-react/dnd-kit both below the 50 KB bar. Learn-map jank audit (TransitLearnPage.tsx + VerticalNetworkMap.tsx): already heavily perf-tuned (3 prior PERF-pass fixes found); remaining candidates (SVG setAttribute transform vs CSS style.transform, box-shadow infinite-loop pulses bounded to ≤2 elements) listed file:line but NOT landed — unmeasurable (Task 4) or unverifiable this session (WebSearch budget exhausted mid-lane). Frame measurement: real landscape capture on /ja/transit-preview via the XCUITest rotator succeeded (innerWidth=1180 innerHeight=820), but confirmed by reading simProbe.ts/sim-capture.mjs that the ONLY frame-trace mechanism (recordTapFrameTrace) is tap-triggered and lesson-tile-scoped — no dropped-frame number exists for map scrolling; manual Safari Web Inspector Timelines recipe given instead. DecompressionStream: confirmed TRUE live via a safaridriver WebDriver session against Mobile Safari on the booted iPad Air simulator (iOS/Safari 26.5, build 23F77) — simProbe.ts was dirty from another lane so used the documented fallback instead of editing it. gzip-at-rest verdict: DO (flagged, not landed) — ~11.6 MB on-device storage saved (13.3 MB content JSON × 87.2% measured ratio) for +0.46 ms/module CPU (Node proxy timing); IPA download size already near-optimally compressed by the zip container itself (cross-checked m26.json: 32,317 IPA-zip bytes vs 31,872 standalone gzip -9). Commits: 949f022f (vite.config.ts + package.json + package-lock.json, visualizer wiring), c2534b7e (docs/perf-2026-09-17.md). Zero src/features/learn code changes — every candidate that cleared the size/impact bar was outside owned files; everything owned was already optimal or unmeasurable. One process note: an early `node scripts/ux-loop/sim-capture.mjs --help` ran OUTSIDE the simulator lock (invalid flag was silently ignored and it ran a real default capture instead of printing help) — no evidence of collision with another lane, flagged per C14 for the record. Tests: npx vitest run src/features/learn 159/159 passed (25 files); npx tsc -b clean; npm run build clean with no visualizer output. Full report: docs/perf-2026-09-17.md.
- 2026-09-17 11:44 — A3b — Observability wiring DONE. Wired both context setters A3 (8cf4f637) left unwired: (1) lastRequestId — ApiClient._request (src/shared/api/client.ts) reads resp.headers?.get("X-Request-Id") after every fetch (success or error), calls setLastRequestId; ?. is defensive, found+fixed a real crash against progressMe.coalesce.test.ts's bare-object fetch mocks. lingo-core already exposes the header (CORSMiddleware expose_headers=["*"], predates this lane) — no server change needed. (2) lessonId/stepIndex/stepType — new hook src/features/lesson/useLessonErrorContext.ts (set on mount/step-change, cleared on unmount), called from LessonPage.tsx (lesson.id/currentStepIdx/currentStep.type) and PlacementTestPage.tsx (src/features/placement/, same StepRenderer shell: placement:<currentModuleId>/adaptive-engine totalServed/currentStep.type — no stable lesson id there). Tests: src/shared/api/client.test.ts +3 (header read on success/error, absent-header no-op), src/features/lesson/useLessonErrorContext.test.ts new (4 cases: set on mount, no-op without lessonId, clear-then-reset on step change, clear on unmount). Proved the lastRequestId wiring for real: temporarily commented out the setLastRequestId call, reran, both new client tests failed with the expected assertion, reverted, reran green. Scoped run: npx vitest run src/shared/api src/features/lesson/LessonPage* src/features/lesson/*.test.tsx src/shared/telemetry src/features/lesson/useLessonErrorContext.test.ts src/features/placement -> 23 files / 179 tests green (includes the placement suite as the PlacementTestPage sibling check). npx tsc -b clean. No sim:capture needed (no visual/layout surface touched). Commits: 53a30313 (client.ts + client.test.ts), 75471efe (useLessonErrorContext.ts + .test.ts + LessonPage.tsx + PlacementTestPage.tsx), 3f01cfaf (docs/observability-2026-09-17.md). Not pushed. Gap flagged: no test renders LessonPage/PlacementTestPage itself to prove the two call sites stay wired if edited later (both are god-files per CLAUDE.md, same rationale lessonPageBuildStepAudioAdvance.test.tsx uses) — hook-level coverage only, per the lane brief's own "hook-level" allowance.
- 2026-09-17 11:45 — A4 DONE: visualizer 949f022f, docs/perf-2026-09-17.md c2534b7e. IPA: 72 % (21.2 MB) = kuromoji/sudachi dict + ONNX WASM (ASR fallback) shipped to every install; i18next eager locales 77 KB/user flagged; DecompressionStream true on iOS 26.5; gzip-at-rest 11.6 MB on-device recoverable, not landed. → A4b IPA slimming.
- 2026-09-17 11:46 — A3b DONE: lastRequestId + lesson/step context wired (53a30313 75471efe 3f01cfaf), 179 scoped tests green; CORS already exposes X-Request-Id. NOTE lane commits carry a "Claude Sonnet 5" co-author trailer (the subagent harness sets its own); left as-is, honest attribution. Dispatching A6 sync hardening.
- P1 — 2026-09-17 11:56 DONE (a1fd391e engine, b291aa98 spec; b2e3a3dc earlier). DEVICE, `--simulate build`, 15 Pro Max: **ja-m15-neo-6?step=15 (13-tile huge bank) 8/8 verdicts PASS at 100% AND 125%**, every per-tap number identical on all 14 samples (100%: fit 0.92, font 22.26, rowH 60.5, tray 220.2, bank 192.5, prompt 181.3, bankTop 489.5, CTA 724.3 — was fit 1.00 with prompt 239.8→203→238.5→201.8 and tray 88.2→235.2; 125%: fit 0.638, font 19.29, rowH 53, prompt 189.4, bankTop 502, CTA 702.5 — was 0.83→0.81→0.71). **ja-m34-neo-7?step=5 (normal bank) 8/8 PASS at both** (fit 1.25 / 1.05). maxH2Jump 0 over the FULL rAF trace in all four. 6 plain captures (3 routes × 2 scales): 0 clipped / 0 wrapped / 0 overflow / stageOverReportPx 0. Price: tiles 8% smaller than b24 at 100%, 10% smaller than its END state at 125%; blank stage at step start 143px → 42px (100%) / 30px (125%). Third mechanism found and fixed by the frame trace: a tile that mounts mid-build was measured before its scale was written, so `--tile-row-h` (a MAX over the cohort) inflated for ONE painted frame (rowH 53→80→53, prompt 5.4px at 125%; 53.5→54→53.5, 1.2px at 100%) → `cohortSizes` + `naturalHeightAtScale`. **OPEN FOR THE LEAD: listening_build has the same class and is NOT fixed** (ja-m34-neo-5?step=12: 100% bank top 459.9→490.9 at tap 4, chromeStable FAIL; 125% fit 1.05→0.92, font 33.94→29.73, three verdicts FAIL) — its tray ghost is clamped at a stale `max-height: 92px` that no longer equals the "two rows" its comment claims, and unclamping it is the 690px-of-743px case the clamp exists for. Untouched by this lane; needs its own lane + sweep.
- 2026-09-17 11:58 — P1 DONE: b2e3a3dc a1fd391e b291aa98 — huge-bank tray reserves the full answer visibly (phantom deleted), spent tile fades in place, tiles born at cohort size; promptStable+chromeStable verdicts; 13-tile + normal build_sentence 8/8 PASS at 100 and 125 (tiles 8–10 % smaller, blank stage 143→42/30 px). OPEN: listening_build fails the new verdicts (pre-existing: ghost clamped at stale 92 px; 125 % font 33.9→29.7) → P1b now; fit stopping point path-dependent (~10 % between runs) → follow-up; 125 % 13-tile sits on the fill floor → Spencer floor dial. h2Stable was vacuous on listening routes (no h2).
- 2026-09-17 12:10 — A6 — Storage/sync correctness DONE. Audit (docs/progress-sync-contract-2026-09-17.md): neither hot write path uses raw BatchWriteItem (progress = TransactWriteItems per attempt + conditional UpdateItem per rollup; SRS = conditional UpdateItem per card fanned out under a semaphore) so the BatchWriteItem/UnprocessedItems bug class this lane was seeded to look for doesn't exist here — but found two real ones instead. (1) Progress: put_attempt and its rollup writes (update_lesson_rollup/update_day_rollup) are non-transactional; a failure between them left the attempt durably logged with XP/streak/rollup PERMANENTLY and SILENTLY dropped on every retry (the old idempotency check treated "CLIENT# row exists" as "fully processed"). Fixed with a rollupApplied marker (defaults True on pre-migration rows so no legacy attempt gets silently re-rolled-up) + a repair path that reuses the durable row and reapplies rollups; residual risk (marker-write itself failing) is a bounded one-time over-count, not the old unbounded silent loss. (2) SRS: upsert_cards fanned out per-card writes with a bare asyncio.gather — no return_exceptions=True — so one card's exception raised immediately WITHOUT cancelling sibling tasks already scheduled; on Lambda a response can go out and the execution environment can freeze before those orphaned tasks finish, silently dropping writes that never even got to fail. Fixed: return_exceptions=True, upsert_cards/delete_cards now return (merged, failed_ids), SRSSyncResponse gets additive failedCardIds, and review_completed no longer credits a card whose write failed. Also: submit_attempt_batch's per-item loop now isolates one item's unexpected exception (was: 500s the WHOLE batch, discarding results for every other item already processed in the same request). Client side already handled all of this correctly (per-card/per-item guards pre-date this pass) — only updated two now-stale "this can't really happen" comments and added regression tests that would have caught the server-side gap. ES compiled-vs-source staleness gate (src/test/esCompiledStaleness.test.ts): recompiles all 36 ES IR modules (m3-m38) to a scratch dir via an additive ES_COMPILE_OUT_DIR env override on compile-ir-es.mjs (never writes the real committed .ts) and diffs byte-for-byte; 0 stale as of this run; full sweep takes ~2.6s (well under the 10s slow-gate threshold), so it runs ungated in the normal suite. Proved every new test fails without its fix by reverting the source in the working tree, rerunning, then restoring (git checkout HEAD -- <path> / git checkout <parent-sha> -- <path>) — 5 server pytest + 2 client vitest + the ES gate's own hand-edit probe, all confirmed red then green again. Tests: lingo-core scoped (test_progress.py + test_progress_dynamo.py + test_progress_unlocks.py + test_srs.py + test_srs_dynamo.py + test_srs_sync.py + test_admin.py) 55/55; full suite 373/373 (no other failures). Client: npx vitest run src/shared/api src/shared/domain src/features/lesson/engine src/features/flashcards/engine + esCompiledStaleness.test.ts — all green (counts in report). npx tsc -b clean. Commits: lingo-core 44bff4e (progress fix), 86cd159 (SRS fix), 7a5d6fe (tests) on main, not pushed. lingo (this worktree): pending, listed in the report. Not landed / flagged for the lead: day-rollup date attribution on a delayed repair (§4 of the contract doc, pre-existing, low-probability) and the fully-atomic rollup+marker TransactWriteItems (would close the residual risk fully at 1.5x WCU cost on every attempt — judged not worth it per Spencer's cost-quantification rule).
- 2026-09-17 12:12 — A6 DONE: two real server data-loss bugs fixed on lingo-core main (44bff4e 86cd159 7a5d6fe; 373/373): (1) attempt logged but XP/rollup permanently dropped if a rollup write failed after put_attempt → rollupApplied marker + repair path, per-item isolation; (2) srs/sync asyncio.gather without return_exceptions 500ed the batch and orphaned sibling writes → partial success + failedCardIds. Client already kept unconfirmed items dirty (tests added 7f80633c); ES compiled-vs-IR staleness gate fdd4a15a (currently clean); contract doc 6de3118c. Lead decisions: bounded over-count on marker failure accepted (documented); day-rollup date attribution left as documented debt. NOT pushed/deployed — Spencer deploys lingo-core.
- 2026-09-17 12:13 — A4b — IPA slimming DONE. Usage map corrected A4's task-1 framing: the kuromoji dict is NOT what feeds furigana (that's the separate, dict-free `romajiLexicon.ts`; readings are precomputed as kana in content JSON, kanji substituted at render time) — its only live caller is `convertToHiragana` (JA speaking-step ASR-transcript kanji→kana, real/learner-facing/offline for BOTH native and web engines); `LanguageModule.romanizer` is dead code (zero callers). (a) Whisper/ONNX (5.75 MB wasm + ~160 KB JS, ~20% of IPA) — DONE, landed: `vite.config.ts` resolve.alias swaps `useWhisperRecognition` for a same-shape stub under `--mode native` only (native never reaches the Whisper hook regardless — `usingNative` short-circuits before the dial is read); web/dev/vitest unaffected (confirmed: plain `npm run build` still ships the wasm byte-identical). `scripts/build/prune-native-assets.mjs` (wired into `build:native`) is a regression guard that fails the build if any Whisper/ONNX filename ever reappears in a native `dist/assets` — caught a real ordering bug in my own draft (a prefix `@` alias checked before the specific one silently swallowed it) before it shipped. (b) Kuromoji dict (15.44 MB / 52.7% of IPA) — code landed, SAFE-BY-DEFAULT, NOT active: `kuroshiro.ts`'s DICT_PATH now follows `VITE_ASSET_BASE_URL` (same convention as TTS's ASSET_BASE) with the bundled `/dict/` as fallback; native fetches route through `fetchBinaryNative`/CapacitorHttp (confirmed via curl that the CDN sends no ACAO header — same CORS gap TTS had) via a monkey-patch of kuromoji's `BrowserDictionaryLoader.prototype.loadArrayBuffer` (verified by reading the compiled output that this reaches the SAME shared module instance kuromoji's internal loader uses, not a duplicate), gunzipping with `DecompressionStream`. The prune script only deletes `dist/dict` when `VITE_ASSET_BASE_URL` is set at build time — unset today, so this is a complete no-op until Spencer/Trevor publish `src/pub/dict/` to the CDN (exact `aws s3 cp` command in the script header + docs/perf-2026-09-17.md §1a) and wire the env var into the native build. Measured (`dist/`, native mode): web-baseline-equivalent 75,936 KB → alias-only (today's safe default) 52,352 KB (−31.1%) → alias+CDN-dict-pruned 32,536 KB (−57.2%, not shippable yet). Projected IPA: 29.27 MB → ≈23.4 MB today, → ≈8.1 MB once the CDN dict ships. Sim: speaking step (`ja-m10-neo-1?step=7`) PASS — native `SFSpeechRecognizer` permission dialog fired (proves native engine selected, not Whisper); `kanji_reading` step (`ja-m18-neo-8?step=1`) PASS via DOM probe (screenshot obscured by the same carried-over OS dialog). Hit and fixed a shared-dev-server HMR staleness crash unrelated to this lane (reproduced on 2 unrelated routes, restarted `:5399` under the lock — C14). Tests: 7 vitest (`kuroshiro.native.test.ts`) + 6 node:test (`prune-native-assets.test.mjs`, includes 4 individual-pattern + 1 false-positive case) + existing 160/160 scoped suite green. tsc clean. `npm run build` and `npm run build:native` both succeed. Doc: docs/perf-2026-09-17.md §1a.
- 2026-09-17 12:15 — A4b DONE: Whisper/ONNX excluded from native (a872b1cf, IPA 29.3→≈23.4 MB projected); kuromoji dict is a REAL runtime dependency (JA speaking-step transcript normalisation, native too), CDN loader landed inactive (7d9d8e0b) pending a live /dict/ copy; docs a5f57704. jaRomanizer dead code flagged.
- 2026-09-17 12:16 — LEAD DECISION on A4b: dict prune SKIPPED — every JA learner hits a speaking step, so a CDN dict = 15 MB per install over the network (no persistent cache) instead of 15 MB in the IPA: a wash, plus offline breakage. Whisper exclusion KEPT (−5.9 MB compressed for every install). RISK FOUND: kuroshiro.ts keys the CDN path on VITE_ASSET_BASE_URL, which the native bundle already sets (host literal https://app.openlingoapp.com in b25 assets), while the prune keys on process.env — build 26 would fetch the dict from the network with the bundle still carrying it. → A4c re-keys both on an explicit VITE_DICT_FROM_CDN flag, default off.
- 2026-09-17 12:22 — A4c DONE: dict-prune CDN path was keyed on VITE_ASSET_BASE_URL alone, which build-25's shipped .env.native already sets (TTS host, unrelated reason) — would have silently network-fetched the 15 MB dict with no cache on every native install while the bundle still shipped it too. Re-keyed both kuroshiro.ts (DICT_PATH + native loader patch) and prune-native-assets.mjs on an explicit VITE_DICT_FROM_CDN="1" AND VITE_ASSET_BASE_URL, both required, both unset today (no-op, verified live: build:native compiles DICT_PATH to "/dict/", dist/dict ships 19,880 KB; npm run build identical; Whisper/ONNX still absent from native dist/assets). Tests extended for all 3 flag/base combinations in both files (kuroshiro.native.test.ts: 12 cases; prune-native-assets.test.mjs: 8 cases), each combo proven to fail when its flag check is reverted. docs/perf-2026-09-17.md §1a records the SKIP with the lead's numbers + revisit precondition (persistent on-device cache + versioned /dict/v1/ CDN prefix, since deploy.yml's root sync uses --delete with no dict/ exclude). Commit 31170516 (kuroshiro.ts, kuroshiro.native.test.ts, prune-native-assets.mjs, prune-native-assets.test.mjs, docs/perf-2026-09-17.md). tsc -b clean; 21/21 vitest + 8/8 node:test scoped green.
- 2026-09-17 12:23 — A4c DONE (31170516): dict CDN path + prune both gated on VITE_DICT_FROM_CDN=1 (default off); native build verified DICT_PATH "/dict/", dist/dict present, Whisper absent. Dispatching A7 procedural QA question set + JA lexical sidecar.
- 2026-09-17 12:44 — A2 — dnd-kit accessibility wiring DONE (d1a6cbe5): DndContext accessibility.announcements/screenReaderInstructions on SortableBuildTiles.tsx, aria-label "{{word}}, {{state}}, position N of M" on every tray tile (confirmed already real <button>s, tabIndex=0), role=status/aria-live=polite live region for tap add/remove (dnd-kit's own announcer never sees a plain click), prefersReducedMotion() fixed to also honour the in-app [data-reduced-motion] toggle (was OS-only — silently ignored for useSortable's transition + DragOverlay's dropAnimation, both Web-Animations-driven). 9 new tests against the real BuildSentenceStepView + real i18next; C4-checked (broke the aria-label wiring, confirmed the test fails, restored). Patch note for the bank-tile aria-label gap (BuildSentenceStepView.tsx/ListeningBuildStepView.tsx, not owned) in docs/accessibility-2026-09-17.md.
- 2026-09-17 12:44 — A2 — axe-core smoke gate DONE (33bd572c): @axe-core/playwright@4.13.0 + tests/e2e/axe.a11y.spec.ts on a new "a11y" Playwright project (playwright.config.ts, reuses the existing dev-bypass MOBILE_URL server — test:mobile's project can't include a tests/e2e/ file by tag, checked against the config). npm run test:a11y currently 4/4 FAIL with real findings: nested-interactive (serious, learn map's transit SVG role="img" wraps real interactive controls, TransitLearnPage.tsx:1508) + heading-order/landmark-unique/page-has-heading-one/region (moderate, region hits all 4 routes — Layout.tsx's skip-link). None live in an A2-owned or shared/components file, so none fixed; full table + owners in docs/accessibility-2026-09-17.md. CI wiring left as a recommendation (gate is red).
- 2026-09-17 12:44 — A2 — Dynamic Type 140% audit DONE (report-only, no commit needed beyond the doc): sim:capture --font-scale 140 on 5 routes (real 15 Pro Max WebKit). build_sentence/MCQ/match_pairs on ja-m34-neo-7: PASS, no clip/wrap, ~1px overflow (negligible). Learn map: obscured full-duration by a stray native Speech-Recognition permission dialog (simulator/harness artifact, not app content) — not fully verified, visible edges clean. Settings modal: REAL finding — "Accessibility" tab label clipped flush against the right edge at 140% (src/features/settings/SettingsNav.tsx, not owned). Table + screenshots referenced in docs/accessibility-2026-09-17.md.
- 2026-09-17 12:44 — A2 — reduced motion + doc DONE (03f63033): confirmed via inspection the fix above closes the only gap found (index.css's universal [data-reduced-motion] rule already covers CSS transitions everywhere; the JS-driven dnd-kit paths were the actual gap). docs/accessibility-2026-09-17.md published: axe table, 140% table, 5-step VoiceOver manual script for Spencer, honest Accessibility Nutrition Label read today (VoiceOver No, Larger Text Partially, Reduced Motion Partially/Yes-for-build-tiles, Sufficient Contrast not evaluated). regression-classes checked (C4 proven, C11 named for the map + settings capture artifacts); npx vitest run (scoped) 72/72 green; npx tsc -b clean.
- 2026-09-17 12:45 — A2 DONE (d1a6cbe5 33bd572c 03f63033 88291341): dnd-kit announcements + per-tile aria-labels + tap live region + reduced-motion toggle honoured; axe gate added (RED: 1 serious nested-interactive on the learn map SVG, 4 moderate); 140 % audit: build/MCQ/match PASS, settings tab label clipped. Honest Nutrition Label today: VoiceOver No, Larger Text Partial, Reduced Motion Partial. → A2b fixes the findings.
- 2026-09-17 12:46 — P1b — listening_build ROOT CAUSE confirmed + corrected. P1's read was right about the clamp and wrong about its size: the ghost row's `max-height: 92px` is not "two rows" AND not 1.26 rows — Chromium DOM probe (artifacts/ux-loop/probeP1b.mjs, 430x932, ja-m34-neo-5?step=12): the ghost row's own scrollHeight is 126px against a clamped clientHeight of 92px (2 rows of 59px + an 8px gap), so it under-reserves by exactly 34px, and the tray goes 120 -> 154px at tap 4 — the clamped-away remainder, to the pixel. The movement is HALF that each way because the step column is `stage-center`: prompt/tray up 17px, bank down 17px (device: 62px growth -> -31/+31, which is P1's bankTop 459.9 -> 490.9). At 125% the same growth pushed the stage past the fill floor and re-triggered the shrink.
- 2026-09-17 12:46 — P1b — FIX (25869e43): `clamp` dropped from the listen ghost row; the `[data-clamp="true"]` max-height rule + its `sm` reset DELETED from index.css. The reservation is now the answer's own tiles wrapping into the rows they need (the build_sentence way) — no px literal to go stale (C2). NO tileFit change was needed: planStageFill's shrink half already triggers on the reserved height at mount, so the shrink lands once at step start instead of mid-build, which is what the brief asked for.
- 2026-09-17 12:46 — P1b — VERDICT HYGIENE (same commit): an unsampled verdict field now prints N/A, never PASS (`na: true`; it still cannot fail a run). `h2Stable` had been green on every listening route since it was written — that view renders no `<h2>` — and `noFlicker` (h2Top deltas) had the same hole. The live prompt verdict on listening routes is real now: the view marks its prompt `data-lesson-prompt` and simProbe's PROMPT_SELECTOR matches either shape. layoutTrace.ts deliberately untouched (shared with the on-device Sync panel).
- 2026-09-17 12:46 — P1b — EVIDENCE, 15 Pro Max `--simulate build`, every applicable verdict PASS at 100% AND 125%, every sampled field identical on every tap: ja-m4-neo-3?step=16 (2 tiles, character) fit 1.25 both scales; ja-m34-neo-5?step=12 (6) fit 1.25 / 0.90; ja-m24-neo-9?step=4 (13) fit 0.86 / 0.638; ja-m42-neo-challenge?step=listening_build (21 = the course's longest of 698) fit 0.798 / 0.638, all 22 samples identical. 6 plain captures (3 routes x 2 scales): 0 wrapped / 0 clipped / 0 tile overflow / stageOverReportPx 0. build_sentence regression re-run at 125%: ja-m15-neo-6?step=15 8/8 and ja-m34-neo-7?step=5 8/8, numbers byte-identical to P1's table (h2Stable/noFlicker still REAL there, so the N/A change neutered nothing).
- 2026-09-17 12:46 — P1b — PRICE. 6-tile at 100%: ZERO tile-size cost (fit 1.25, rowH 73, font 25.25-32.32 = the pre-fix numbers), 62px of tray reserved at step start — the room the tray took at tap 4 anyway. 6-tile at 125%: lands on the pre-fix END state from tap 0 (fit 0.90 vs 0.92, font 29.09 vs 29.73) = -14% against the pre-fix START, -2% against its END. DECISION FOR THE LEAD: the sticky CTA leaves 565px (100%) / 519px (125%) of column above it; tray+bank for the 21-tile answer is 613/619px, so 137px of the bank at 100% and 212px at 125% sit behind the sticky CHECK at rest, a 157/237px scroll away with the native bar as the cue. 13 tiles is clear at 100% (21px slack) and 32px under at 125%; <=6 tiles clear at both. Scope: 7 of 698 steps have >=13-tile answers (1.0%), 14 have >=12 (2.0%). Not new debt (pre-fix those steps hid the bank by the END of the build while moving everything), but it is where "nothing moves" and "everything visible" stop both holding at today's floors. Levers: Spencer's font/fill floor, or a row-expressed tray cap with an INTERNAL scroll. Also disclosed: `stageFits` measures against the stage BOX (which extends behind the sticky CTA), so it does not catch this — only the 21-tile route at 125% trips it, and at EVERY tap including tap 0.
- 2026-09-17 12:46 — P1b — HARNESS ARTIFACTS handled, not filed as defects (C11): (1) the :5399 dev server was serving a broken module graph (`Invalid hook call` / `resolveDispatcher() === null` in NativeAuthBridge on every route, so every lane's capture would have failed validation-blind) — a fresh server on :5401 was clean, so I restarted :5399; it is healthy again for all lanes. (2) The stray native Speech-Recognition dialog A2 also hit: `simctl privacy` has no speech-recognition service, so I inserted kTCCServiceSpeechRecognition auth_value 2 into the OL-15ProMax TCC.db (+ `grant all`) and killed tccd; it still appears over screenshots on lessons containing `speaking` steps — DOM readings are unaffected (JS runs, 21/21 taps landed), but m42 screenshots are partly obscured and I say so rather than claiming a clean read. (3) `?step=<N>` DRIFTS on `*-neo-challenge` lessons (runtime step derivation): `ja-m42-neo-challenge?step=11` landed on a 4-tile MCQ and reported `simulation: null`. `?step=listening_build` (devGates' type jump) is the reliable form, at the cost of resolveAnswerLen falling back to 20 taps — pass `--max-taps` explicitly.
- 2026-09-17 12:55 — P1b DONE (25869e43 a95ebb5e 5fc3cd77 af4fe259): listen tray reserves its answer in rows (stale 92 px clamp deleted), promptStable live via data-lesson-prompt, unsampled verdicts print N/A; 4 listening routes incl. the longest (21 tiles) all sampled fields constant at 100+125; build_sentence regression re-run byte-identical. Cost: 6-tile 100 % zero; 125 % −2 % vs old end state. DECISION (lead, agree with lane): 21-tile answers at 125 % leave ~212 px of bank behind the sticky CTA at rest — 1 % of steps; leave, levers = floor dial or an internal tray scroll. Follow-ups: bank-bottom-vs-CTA verdict; layoutTrace h2 selector; TileTray clamp prop dead → P4. AREA 1 COMPLETE.
- 2026-09-17 12:58 — A7 — procedural QA question set + JA lexical sidecar DONE. `scripts/qa/procedural/` (run.mjs CLI + measure.mjs whole-course sweep + 10 checks Q1-Q10, each named/tool-mapped per the lead's design, `checks.test.mjs` 11/11 node:test — every question proven to say "no" on a planted defect). `scripts/lexical/ja/` sidecar (fugashi+unidic-lite via uv venv, installed and working; kanji-reconstruction mitigation for the kana-only over-segmentation pitfall, measurably helps but doesn't fully close it). Reused, not reinvented: gate.ts's gateResidual/jaSurfaces (stepTaxonomy.ts) via a new `lib/tsBridge.mjs` (vite.createServer().ssrLoadModule — no new dep, loads any repo TS read-only from a plain Node script), moduleCompiler.ts's diagnoseModule (Q8) + its v2 shrapnel retokenize test (Q2, promoted from IR-only to a runtime-JSON callable check), particleTileSeparation.test.ts's rule (Q4, ported — can't import a .test.ts), manifest.ts's resolveTtsPath algorithm (Q7, re-implemented against the committed manifest JSON directly, plus DialogueListenStepView.tsx's per-sentence playback fallback), fr-quality.test.ts's step-variety rule (Q9, ported to JA using the shared SELECTION_TYPES). MEASURED (not assumed): Q2 and Q3 run over ALL 4,125-4,140 JA build/listen steps; found + fixed two real category-error contaminations along the way (character-granularity kana drills, picker:true register-choice steps) before measuring. Q2: 9 hits, hand-audited all 9, 2 true (a derivedFrom verb-form atom used before its own registration — the やめて class, repeat incident in m27/m36) + 7 coincidental atom-collisions — precision ~22%, marked INFORMATIONAL. Q3: 653 hits, hand-audited random 60, 0 true positives (three false-positive classes: legitimate compounds, grammatical aux constructions the POS scheme can't exclude, kana-only tagger parse failures) — precision 0%, marked INFORMATIONAL; the real fix (JMdict compound lookup + richer aux table) is named as follow-up, out of scope here. Q1/Q6 also marked informational (gate.ts models vocabulary not conjugation morphology — measured false-positive on every conjugated verb form, e.g. m34's own grammar_rule teaching のもう). Q4/Q5/Q7/Q8/Q9/Q10 enforced=true. Real findings surfaced (not fixed, per this lane's file-ownership rule): a kanji leaked into a kana-graded dialogue_listen line (m42), 655 dialogue/listening steps with no TTS clip anywhere in the manifest (independently confirmed via a separate Python sha256 check, not just this tool — pattern matches CLAUDE.md's documented "TTS emitter silently skips an unmatched factory shape" risk against dialogue_listen's lines[].kana specifically), one particle-glue candidate (m46, きゅうに) flagged for triage since the shipped particleTileSeparation.test.ts currently passes (possible stale allowlist or bundle drift, not confirmed). `src/test/proceduralQa.test.ts` wired (enforced questions only, full 46-module course, ~19s — under the 30s budget, so no 5-module fallback needed in practice; fallback path still implemented and documented). **This gate is currently RED** (surfaces the findings above) — expected per this lane's report-don't-fix mandate, not a runner bug. `docs/procedural-qa-2026-09-17.md` published: protocol, Q-table, precision tables, top-10 findings, how-to-add-a-question, KO/ES/FR scoping. `.gitignore`: added `scripts/lexical/*/.venv/` (250MB, was uncovered) and confirmed `artifacts/` already covers the sidecar cache. `package.json`: one new script `qa:procedural`. No new npm deps. Tests: `node --test scripts/qa/procedural/checks.test.mjs` 11/11; `npx tsc -b` clean. Commits to follow, explicit paths, trailer.
- 2026-09-17 13:01 — A7 DONE (4958f4ca 9c770cff 14fa6482 5ba9b790): procedural QA runner Q1–Q10 + fugashi sidecar; Q2 precision 22 %, Q3 0 %, Q1/Q6 false-flag conjugations → informational; Q4/5/7/8/9/10 enforced. GATE src/test/proceduralQa.test.ts is RED (655 Q7 "no TTS clip" on dialogue/listening lines, 1 Q10 kanji leak m42, きゅうに m46). BLOCKS preflight → A7b: verify Q7 against the runtime clip resolution (likely wrong field hashed), convert the gate to a ratchet (counts may never rise), triage m42/m46.
- 2026-09-17 13:08 — A2b DONE (9aa85375 470a8e2d df7949c8 e98b9b66 ae972519 fd7be84c 6e3d4ada 97bdc590 e2fc6586): all 5 axe findings from A2's audit fixed + root-caused against the installed axe-core bundle's own rule logic (node_modules/axe-core/axe.js), not guessed. region (all 4 routes): skip link moved above SidebarNav in Layout.tsx — axe's own `_isSkipLink` exemption only holds when the link document-order-precedes every real page link, and SidebarNav's nav links were beating it on every authed route. nested-interactive (serious, learn map): TransitLearnPage.tsx svg role="img"→"group" (ARIA img implies childrenPresentational, which is what the rule actually checks — confirmed by reading nestedInteractiveMatches). landmark-unique: distinct aria-labels on SidebarNav's and LearnSidebar's asides. heading-order: one sr-only h2 added to both LearnSidebar layout branches (not a re-level of the shared h3 components — those also render on Home/LearnPage with their own valid chains). page-has-heading-one: one shared sr-only h1 in LessonShell.tsx, covering every surface built on it for free (lesson player, placement/test-out, grammar review, flashcard review) — did not touch any step view's own prompt heading (P1's promptStable samples it). Also: A2's disclosed bank-tile aria-label patch applied to BuildSentenceStepView.tsx AND ListeningBuildStepView.tsx (P1b had since committed it clean — git log/status checked per the brief's conditional). Settings 140% tab clipping (docs/accessibility-2026-09-17.md §3): root cause was the trailing w-6 edge-fade painting over real label text because container padding (px-3=12px) was half the fade's width (24px) — bumped to px-6/scroll-px-6, fade now also hides once nothing's left to scroll; verified on-device at 100% AND 140% (capture-15-pro-max-{100,140}-settings.attempt3.png). npm run test:a11y: 4/4 GREEN (was 4 failed before this lap) — wired into .github/workflows/ci.yml's mobile-e2e job right after test:mobile, same job env/bypass server. Tests: 3 new files (TransitLearnPage.test.tsx +2 cases, LearnSidebar.a11y.test.tsx, LessonShell.test.tsx +1 case, SettingsNav.tabStrip.test.tsx, BuildSentenceStepView.bankTileLabel.test.tsx) — every one hand-verified to fail on the pre-fix markup before committing (regression-classes C4). Scoped vitest: 212/212 passed across 35 files (src/routes src/features/learn src/features/settings src/features/lesson/components/LessonShell* src/features/lesson/components/steps/BuildSentenceStepView*); npx tsc -b clean. One transient failure hit mid-run (emitContent.test.ts EINVAL on a shared content JSON, via another lane's concurrent predev/content:emit) — not caused by this lane's changes, resolved on retry. docs/accessibility-2026-09-17.md updated: all 5 findings marked fixed with commit SHAs + pinning tests, §3 settings row updated, §6 Nutrition Label paragraph updated only where evidence changed (VoiceOver and Larger Text each had one of two blocking conditions resolved — verdicts stay No/Partially, §5's manual VoiceOver script and the map's 140% capture are still outstanding). Open items for the lead: §5's VoiceOver manual script still needs an actual run; learn-map 140% capture still blocked on the simulator's stray Speech-Recognition permission dialog (same artifact P1b already worked around for a different route — same TCC fix would likely clear it here too, not attempted this lap as out of scope).
- 2026-09-17 13:09 — A2b DONE (10 commits 9aa85375..5f557a6a): all 5 axe findings fixed (learn-map SVG role=group so stations are reachable; skip-link order; landmark labels; sr-only h1/h2), settings tab strip no longer clips at 140 %, bank-tile aria-labels in both build views (ListeningBuild edit confirmed by lead), axe gate GREEN on 4 routes and wired into CI mobile-e2e. Still nobody has run VoiceOver. AREA 2 COMPLETE (pending Spencer VoiceOver pass).
- 2026-09-17 13:20 — P4 — Tile/TileTray primitive gap closure (e458ced4): removed the dead `clamp` prop/JSDoc/`data-clamp` from TileTray.tsx (grepped — no remaining reader; index.css's dead-clamp comment updated to say so) + fixed Tile.tsx's `collapse` doc (still said "~150ms shrink-to-zero", now describes the shipped b25 fade-in-place). `TileTray kind="grid"` gains `cols={3}`/`cols={4}` (columns-only, no assumed item count unlike the fixed 2x2; combines with `fr` for equal rows) — no shipped view needs it yet, added as primitive capability per P3's gap. `TileState` gains `missed`, `TileTone` gains `warning`/`neutral`. New TileTray.test.tsx (7 cases incl. a type-level pin that clamp cannot return). Tests: tiles scope 81/81.
- 2026-09-17 13:20 — P4 — TapTheWord/PretestMcq/WordMap adopt the new state/tone props (bd0a9711): TapTheWordStepView's missed-target tile is now `state="missed"` (was `state="selected"` + `!bg-surface border-dashed`). PretestMcqStepView's wrong-but-safe guess is `tone="warning"`; its reveal/pre-submit-pick states move onto `tone="card"` (already pixel-identical, closing that half of the gap with zero new CSS). WordMapStepView's solved grammatical-NEUTER chip is `tone="neutral"` (masculine/feminine keep the bang-override — genderColor.ts is out of primitive scope, documented in Tile.tsx's TileTone doc). Tests rewritten off literal Tailwind classNames onto data-state/data-tone (3 new cases). Scoped run 478/478, tsc clean.
- 2026-09-17 13:20 — P4 — AgreementCloze/AspectChoiceCloze migrated onto `Tile size="chip"` (532f98fa), the last 2 of the 12 option views. No TileTray — the prose paragraph / each blank's own `role="group"` span IS the container, per the brief's call. All four graded looks (idle/selected/correct+success/wrong/spent) mapped onto EXISTING state/tone combos pixel-for-pixel — the new `chip` TileSize tier was the only addition needed (geometry only, ~36px baseline height, no row-height floor). AgreementCloze keeps its shipped text-lg/sm:text-xl size verbatim; AspectChoiceCloze's private `text-[0.94em]` (~15-17px) is NOT preserved — deliberately unified onto the same 18/20px tier, disclosed in index.css. Tests updated off className color assertions onto data-state/data-tone (both files, all pre-existing cases + 1 structural pin each still pass — actual pre-existing counts were 4 and 19, not the brief's "11+9"). Scoped run 480/480, tsc clean.
- 2026-09-17 13:20 — P4 — Simulator evidence for the chip tier (fc393a90's docs cite these): `/es/learn/lessons/es-m12-4?step=12` (AgreementCloze) on the 15 Pro Max, both scales, validated clean (no VALIDATION MISMATCH after fixing the route to the full `/:lang/learn/lessons/:id` path — the brief's bare `es-m12-4?step=12` 404'd to /home, reproduced 3x before diagnosing as a malformed route, not a harness bug). 100%: 4 chips (el/los/an/a), fontPx 23, boxH 36px uniform, 0 wrap/clip/overflow. 125%: fontPx 28, boxH still 36px (fixed line-height holds the box constant across FIT/FILL growth, as designed), 0 wrap/clip/overflow; "an"/"a" wrap to their own prose line at 125% (expected paragraph reflow, not a chip defect). Screenshots read at both scales: chips sit cleanly on the sentence baseline, readable, no visual regression. AspectChoiceCloze has 0 authored lessons anywhere in the course content (grepped es/fr curriculum + compiled content bundles) — no route exists to capture; render-test-only coverage stands, matching the tap_the_word/P3 precedent. `FAIL: Noto Sans JP did not load` on both runs is the harness's CJK-font-load check firing on an all-Spanish route — unrelated to this work, not chased (mobile-ui-verify §7 names `--allow-fallback-font` for exactly this).
- 2026-09-17 13:20 — P4 — docs/mobile-sizing-spec.md updated (fc393a90): chip tier + new states/tones documented in §2; primitive-coverage paragraph — 24/36 step views on Tile/TileTray now (was 13 this morning), 12 with no tiles (all checked, all fine by design; LiaisonListen/SilentLetter looked tile-shaped but are deliberately bespoke per their own comments). Flags a 1-off drift against the morning inventory's "13/12/11" ballpark (this lane's is a fresh grep-every-file count taken after the work landed) — not chased further, noted for the lead.
- 2026-09-17 13:20 — P4 DONE (e458ced4 bd0a9711 532f98fa fc393a90): all 5 tasks — dead clamp prop removed, TileTray cols 3/4, TileState `missed` + TileTone `warning`/`neutral`, AgreementCloze/AspectChoiceCloze migrated (12/12 option views now on Tile), docs updated. Scoped vitest 480/480 (tiles + steps), npx tsc -b clean. Open question for the lead: is the primitive's tone vocabulary (accent/success/card/warning/neutral) the right long-term shape, or should `card`/`neutral` collapse — both are "organic drift preserved as a transcription" per the file's own doctrine, a visual decision the brief left to the owner and I did not make.
- 2026-09-17 13:22 — P4 DONE (e458ced4 bd0a9711 532f98fa fc393a90): chip tier + missed state + warning/neutral tones + cols 3/4 + clamp removed; AgreementCloze/AspectChoiceCloze migrated (es-m12-4 clean at 100/125); ALL 24 tile-bearing views on the primitive, 12 no-tile by design. Build 26 now waits only on A7b (gate green). Dispatching A8 learning-science (flagged off).
- A7b — 2026-09-17 13:24 c86f8651: Task 1/verification prereq — m42 dialogue_listen line carried raw kanji 人 in its kana-graded `ja` field (plain authoring slip, confirmed in IR). Fixed 人→ひと, recompiled m42 (only line changed, no drift) + m43-m46 (no ripple), re-emitted content. Q10 finding 1→0. JA curriculum suite 8053/8053 green.
- A7b — 2026-09-17 13:24 9ef006e6: Task 3b — Q4's 5 findings (よんで/なんで×2/きゅうに) were the port using a DIFFERENT atom source than particleTileSeparation.test.ts (getNormalizedCourseAtoms's kana-normalized .display vs. the real gate's courseAtoms.surface ??kana), not a stale allowlist — the allowlists were byte-identical. Number atoms register with kanji surface (よん's is 四, no kana field), so the real gate never matches them as stems; the port did. Added getCourseAtomSurfaces() mirroring the real source exactly. Q4 findings 5→0, real gate unaffected (0 violations, unchanged). Re-pointed the plant() fixture off わたしは (no bare courseAtoms entry — only whole-sentence expression atoms) onto あなたは.
- A7b — 2026-09-17 13:24 544afc97: Task 1 — Q7's reported 655 missing-TTS-clip findings were 654 checker false positives: hasTtsClip mirrored only manifest.ts's bare resolveTtsPath, not the full getTtsUrl fallback chain (trailing-punctuation strip, internal-punctuation strip, JA hiragana-twin) every real call site uses. Verified 5 sample findings resolve fine at runtime by hand-computing the fallback hash. True course-wide count: 1 (ja-m42-neo-challenge-dlg-4, genuinely no clip under any path — its play button renders disabled, autoplay resolves silently since JA never falls back to synthesis). Fixed ttsCoverage.mjs, pinned against the real getTtsUrl with src/test/ttsCoverageParity.test.ts (20 real lines); proved the pin can fail and restored.
- A7b — 2026-09-17 13:24 5c82b912: Task 2 — converted src/test/proceduralQa.test.ts from a single anyEnforcedFail boolean into a per-question ratchet against a committed baseline (src/test/proceduralQa.baseline.json: Q4:0, Q5:0, Q7:1, Q8:0, Q9:4, Q10:0), never blocking on pre-existing (Q9's 4, out of this lane's scope) or fixed (Q4/Q7/Q10) debt, only on a rise. Split informational Q1/Q2/Q3/Q6 into a separate always-passing report test (full run ~34s, over the 30s budget the blocking gate must meet). Blocking ratchet test alone measured 19.3s. Proved the ratchet trips: set Q7's baseline to 0, watched it go red naming the exact finding, restored, watched it go green. docs/procedural-qa-2026-09-17.md §4/§6 corrected (be1edf3e).
- 2026-09-17 13:26 — A7b DONE (c86f8651 9ef006e6 544afc97 5c82b912 be1edf3e): Q7 654/655 were checker false positives (getTtsUrl strips trailing 。); true missing clips = 1 (m42 dlg-4 line 2, silent gap — TTS queue); m42 人→ひと fixed in IR; gate is a per-question RATCHET, GREEN (baseline Q7:1 Q9:4). BUILD 26 LAP STARTS.
- 2026-09-17 13:29 — b26 preflight on fdf60a21 (detached worktree, npm ci): 679 files / 18,455 tests / build OK, exit 0 in 1 m 50 s. Pushed fdf60a21:main; ci+deploy watch and the archive/export (no upload) running in parallel; upload gated on CI green + prod content check.
- 2026-09-17 13:31 — b26 NEAR-MISS: first archive (25 s, IPA byte-identical to b25) shipped STALE dist — build:native failed instantly in the main checkout (rollup-plugin-visualizer not installed there; `| tail -2` masked the exit) and cap sync copied the 03:00 dist. Caught by the bundled-content check (Whisper still present, no data-lesson-prompt, no telemetry). Fixed: npm ci in the main checkout; release-b26.sh now has pipefail, rm -rf dist, dist/public freshness asserts, Whisper-absent assert. Rebuilding.
- 2026-09-17 13:33 — b26 archive OK on the third run: IPA 23,600,233 B (b25 29,508,147; −5.9 MB, Whisper/ONNX absent, dict kept), StepRenderer-CnBiLzer.js carries data-lesson-prompt, entry carries telemetry/errors. Whisper assert narrowed (the 362-byte native stub name matched). Upload waits on CI.
- 2026-09-17 13:39 — CI RED on fdf60a21: only src/test/proceduralQa.test.ts (ENOENT src/pub/content/v1/manifest.json — content is generated, CI ran test:run without content:emit; preflight emits first so it was green locally). Fix 977d10f2: both workflows emit before unit tests. Re-verifying in the detached worktree, then push. IPA content unaffected (workflow-only change).
- 2026-09-17 13:41 — A5b DONE (e9e94e71 d56e3b1d 3a65d622 b73acea5): pixel baseline diff on sim-capture.mjs — odiff-bin 4.5.0 (exact-pinned, one devDependency; AA mode + ignoreRegions masking), crop scoped to [data-lesson-stage] via simProbe.ts's new stageLeft/stageWidth; noise measured 0% (26 pairwise diffs, 2 routes, AA on/off) -> threshold 0.1%; --compare-baseline/--update-baseline wired in, proved PASS (0.0000%) and FAIL (1.59% on a planted red-rectangle patch, diff image + non-zero exit) on real device captures, not just synthetic tests; 16 baselines (8 routes x 100/125) committed, 2.2MB. P1b open item 2 (bankVisible, bank-vs-sticky-CTA, informational default / --enforce-bank-visible gate) and item 3 (layoutTrace.ts h2 selector widened to match simProbe's, h2Stable/noFlicker now live on listening_build, LayoutTracePanel proved still renders) both closed. 158 node:test + 86 vitest (src/shared/dev + src/features/sync) green, tsc -b clean. docs/mobile-sizing-spec.md §9 documents the flow/threshold/routes. One harness artifact hit + cleared during baseline capture (a concurrent lane's mid-edit dev-server error on an unrelated import), not an app defect.
- 2026-09-17 13:42 — A5b DONE (e9e94e71 d56e3b1d 3a65d622 b73acea5 f266021e): odiff-bin pixel baselines for 8 routes × 2 scales (2.2 MB), stage-cropped, noise 0.00 % over 26 pairs, threshold 0.1 %, planted 1.6 % change FAILs; bankVisible verdict; layoutTrace prompt selector widened (h2Stable/noFlicker live on listening). Lead: --compare-baseline stays recommended for now. b26b preflight on 977d10f2 green (18,458 tests); pushed; watching.
- 2026-09-17 13:50 — A8 — learning-loop DONE (980e9578 f18311e1 60f68b31 53a1c299): task-1 read/write table corrects the research-lane premise — module-end review lessons' vocab portion (dynamicReviewPrefix.ts/buildSrsReviewLesson.ts) and matchPairsFloor.ts's meaning-fill ALREADY rank by FSRS due-ness (RULE 3, shipped 2026-09-15, same day as the research pass); the real unaddressed gap is inside moduleCompiler.ts's filler/match-grid pools, NOT wired here because compileModule() runs eagerly at JS-import time (45+ call sites) before any async flag fetch could resolve — documented, not faked. Shipped instead: always-on review_grid_served telemetry (sessionLog.ts + dev Sync-panel summary), reviewGridsFromFsrs flag (default false) wired into buildTileFloor.ts's non-sibling-preferred distractor tier via a pure permutation selector (dueRanking.ts + reviewGridFsrsSelection.ts, gate-tested — a never-introduced atom structurally cannot be selected, proven by injecting a bug and watching the test fail), firstExposureBlockedWarmup flag reserved but design-only (its home is the grammar review session, an SRS write surface, off-limits). Doctrine paragraph added to CLAUDE.md. docs/learning-loop-2026-09-17.md has the table, event shape, flag semantics, A/B design. Verification: tsc -b clean; 1312/1314 scoped tests pass (2 skipped, pre-existing); two consecutive content:emit runs produced byte-identical per-module JSON (only manifest.json's generatedAt timestamp differed, pre-existing/unrelated). Open: moduleCompiler.ts's filler-pool FSRS ranking is real but needs an architectural fix (lazy compile or a post-pass), not a flag — left for a future lane.
- 2026-09-17 13:52 — A8 DONE (980e9578 f18311e1 60f68b31 53a1c299 e601d659): two of four review surfaces ALREADY rank by FSRS due-ness (RULE 3, 2026-09-15); real gap = moduleCompiler filler/match pools, which compile eagerly at import so a runtime flag cannot reach them (architectural: lazy compile or post-pass) → decisions doc §2. Shipped: always-on review_grid_served telemetry + Sync-panel summary; experimental.reviewGridsFromFsrs (default off) wired into buildTileFloor distractor tier; doctrine paragraph in CLAUDE.md. Flag-off content:emit byte-identical.
- A5c — 2026-09-17 13:56 04445ef4: Task 3 (sim-capture verdicts) — confirmed fitScaleStable/rowHStable/h2Stable/promptStable/chromeStable/bankVisible already N/A-wrapped by A5b/P1b; found trayBankFontEqual + stageFits were NOT wrapped — computeBuildVerdicts([]) printed PASS with zero comparisons (verified live, exact C4 pattern). Wrapped both in the existing na() helper (evaluated-count tracked per loop); formatBuildVerdictFailure already ignores na so N/A never counts as PASS in the exit code. 3 new node:test cases (empty array, taps-with-no-fields, mixed-taps-still-judged); planted-failure proof: reverting the wrap fails exactly those 2 new tests, restored. 161/161 node:test green.
- 2026-09-17 13:57 — CI RED again on 977d10f2: proceduralQa.test.ts timed out (43 s / 65 s budgets; runner ≈4× slower than local 19 s / 34 s). Fix: full-course budget 150 s / 240 s, report-only test skipped on CI (it prints counts for a human). Local re-run confounded by A7c uncommitted checks (Q3 enforced, 877 hits) — preflighting the committed state in the detached worktree instead.
- 2026-09-17 14:00 — b26 IPA rebuilt from 8cf756c8 (main): 23,601,603 B, Whisper absent, StepRenderer-YDQxrcob.js has data-lesson-prompt, entry has telemetry/errors + review_grid_served. Upload gated on ci 35268145290 + deploy 35268145306.
- A5c — 2026-09-17 14:13 04445ef4 50b31c48 fa0eab36 1877baa7 9f9bd2de c0b1dabc 265e78fb 58f41d67: A5c DONE — vacuity sweep. sim-capture trayBankFontEqual/stageFits N/A-wrapped (P1b's h2Stable/noFlicker fix, same class); 12 gate test files fixed (moduleConformance x3, kanaWordIntroOrder, grammarReviewPools, particleClozePlacement, particleTileSeparation, destinationParticle, manifestCoverage, fr-quality, frDistractorProvenance, es/fr atoms.generated + structure) with a toBeGreaterThan(0) floor per unguarded collector loop, none weakened, no ratchet raised; 6 files (sceneVocabGate, esCompiledStaleness, both particleCueAnswerability, moduleCompiler.diagnostics, ja/irDiagnostics, ko/moduleConformance) already hardened, no change. New scripts/qa/vacuity-lint.mjs + npm run test:gates-nonempty + ci.yml gates-nonempty job + 8 node:test cases. docs/gate-vacuity-2026-09-17.md has the full table. Every fix has a planted-failure proof (emptied the set, watched it fail, restored). Scoped runs: 179 vitest + 169 node:test green, tsc -b clean.
- 2026-09-17 14:14 — A5a — Preflight measurement + tuning DONE (docs/preflight-2026-09-17.md). Headline: local preflight is ~1m51s median (content:emit 3.75s + tsc -b 14.69s + vitest run 83.31s + vite build 9.14s), not the informal "~5 min" — that number is real, but it's CI's: gh run view shows unit tests alone at 435-459s (73-75% of the job) on ubuntu-latest, justifying the 2-way --shard matrix added to ci.yml (4319144b, UNVALIDATED against a real run — this lane does not push). src/test/proceduralQa.test.ts alone is ~83s, essentially the whole local wall-clock — flagged to its owning lane as the real lever; no pool/maxWorkers/isolate change was applied to vite.config.ts because every experiment (pool=threads, maxWorkers=17) swung >15 points in opposite directions on repeat runs under this machine's concurrent-lane load, below the required 10% signal. isolate:false NOT applied to the app project: the --sequence.shuffle order-dependence hunt found failures only in files lane A8 was concurrently editing (confirmed via git status + a no-shuffle control run that still failed on an A8-named test), not genuine ordering sensitivity. Added test:related (vitest run --changed origin/main) for lane use, proven to select exactly the right 4 files for a one-file change (uncommitted-diff form) — preflight itself untouched. Added fast-check 4.10.1 + @fast-check/vitest 0.5.0 (exact-pinned, b606fbc3) + 3 property test files (tileFit, progressReconcile, srsSync mergeStates), each with a real empirically-captured shrunk counterexample pinned as a regression test (2 via real edit+revert on owned files, 1 via a throwaway non-committed broken reproduction for lane A8's file). Final verification: npx tsc -b clean; npx vitest run 687 passed/15 skipped (702 files), 18525 passed/28 skipped (18553 tests), 0 FAIL, 91.28s wall. regression-classes C4/C7 checked in the doc §7.
- A7c — 2026-09-17 14:14 6db184b9 219524a9 5bcc30a5 36989404 0bb2b3a4: Q2/Q3 v3 (dictionary-first, JMdict + course atoms) — PROMOTED both from informational to enforced. JMdict (jmdict-simplified 3.6.2+20260914172325, EDRDG CC BY-SA 4.0, sha256-pinned) fetched + compact-indexed (artifacts/lexical/jmdict/, gitignored). Q2 re-measured 2/2 (100%) true positives (v2: ~22%, 9 hits) — both the same pre-existing たべすぎた-before-m36-registration finding (m27), baselined not fixed (out of file-ownership scope); the 7 v2 false positives (そうです/そうだ/んです/なんだ substring collisions) all resolved by the new independent-word escape (lib/irLexicon.mjs chunkBoundaryHits). Q3 re-measured 0/4,125 hits course-wide (v2: 0%, 653 hits, 0/60 true) via a 5-step dictionary-first resolution (lib/tileMorphology.mjs decomposeTile: whole-tile JMdict/atom/course-furniture match -> deconjugation (lib/jaDeconjugate.mjs) -> te-form host+aspectual-aux split -> content-prefix+function-suffix split -> tagger fallback); precision reported honestly as vacuous (0/0, nothing to hand-audit) with recall backed by a synthetic glued-word capability check + checks.test.mjs's planted-defect case, not a course scan. Baseline Q2:2, Q3:0 (src/test/proceduralQa.baseline.json). Performance: run.mjs now pre-warms the JA sidecar cache with one batched spawn per invocation (36.5s -> 20.2s cold); vitest ratchet measured 20-25s (under the 30s budget) across two runs. node --test scripts/qa/procedural/*.test.mjs 11/11 green; npx vitest run src/test/proceduralQa.test.ts 2/2 green; npx tsc -b clean. docs/procedural-qa-2026-09-17.md §3/§6/§7/§8a updated; m42's one true Q7 TTS finding added as an explicit queue line in docs/handoff-2026-09-11-mobile-qa.md (no dedicated TTS-queue doc existed).
- 2026-09-17 14:16 — CI RED (3rd) on 8cf756c8: procedural gate now passes; reviewSplit.test.ts + switchoverBeatIntegration.test.ts timed out at 20 s. BISECTED on clean checkouts: test time 6.88 s at 980e9578 (A8 telemetry) → 13.12 s at f18311e1 (A8 FSRS ranking, flag OFF) — the flag-off path is not free. → A8b. A5a DONE (b606fbc3 4319144b 421dc410 42160d05): local preflight median 1 m 51 s (proceduralQa ≈ 83 s of it), no vite.config change survived the 10 % bar under lane noise, fast-check property tests ×3 with real shrunk counterexamples, CI 2-way shard matrix (unvalidated until a push). A5c DONE (12 commits): 12 gate files given non-empty floors, trayBankFontEqual/stageFits now N/A when unsampled, vacuity lint + CI job gates-nonempty.
- 2026-09-17 14:17 — A7c DONE (6 commits): JMdict (jmdict-simplified, CC BY-SA 4.0, sha-pinned under artifacts/) + course lexicon resolve tiles first, tagger only for residuals. Q2 9→2 hits, 2/2 true (100 %) → ENFORCED baseline 2 (both = たべすぎた used in m27 before its m36 registration — content bug → A7d); Q3 653→0 hits → ENFORCED baseline 0 (vacuous precision, accepted by lead as a ratchet against new introductions). Ratchet 20–25 s locally. NEW RULE from here: lanes work in their own worktree+branch (decisions doc §2 item 7, lead adopts) so the pushed SHA is always the preflighted one.
- A7c — 2026-09-17 14:24 a6dc76d4: perf follow-up per lead note (proceduralQa.test.ts ~83s local pole). Per-module verdict cache (lib/verdictCache.mjs, artifacts/qa/procedural/verdicts/ gitignored, keyed on module content + all checker source files + JMdict fingerprint — verified auto-invalidates on a checker edit, restores on revert): full-course enforced-only 22.4s cold -> 1.0s warm. Moved the informational report (already skipIf(CI)) out of vitest entirely -> npm run qa:procedural -- --informational-summary (verified same Q1:3880/Q6:737 counts). Combined: npx vitest run src/test/proceduralQa.test.ts 22.0s cold -> 1.15s warm (was ~57-63s combined). CI timeout UNCHANGED (150s) — cache cannot help a cold CI checkout, only repeated local/warm runs; said so per instruction. node --test 11/11, tsc -b clean.
- A7d — 2026-09-17 14:32 bf09c91e c00b847e: Procedural QA Q2's only content finding fixed — たべすぎた was tiling as たべ|すぎた in m27 (m27's own すぎる worked example, correct by m27's IR law that verb+すぎる is always two compositional tiles) but Q2 flagged it because m36 separately re-registered たべすぎた as a competing whole-word derivedFrom atom nine modules later (over-applying the そう/やすい/にくい/ながら/がる/たがる attachment-surfaces-are-atoms rule to a form that doesn't need it — m27's すぎる/すぎた atoms already cover it). Removed the redundant m36 registration; m36's own beat now tiles compositionally too. Recompiled m27–m46 (lexiconKanas-only ripple, hand-verified), content:emit re-run, taughtVocab.generated.json regenerated. Baseline Q2 2->0. Tests: 8053/8059 JA curriculum suite green (6 pre-existing skips), moduleConformance/particleTileSeparation/kanaWordIntroOrder/sceneVocabGate/manifestCoverage/moduleCompiler.diagnostics all green, proceduralQa.test.ts 2/2 green. No new TTS clip needed (no wording changed, only which module registers an existing surface).

- 2026-09-17 14:33 — A7d DONE + MERGED (bf09c91e c00b847e ac12b4e7): たべすぎた was a REDUNDANT m36 atom (m27 teaches verb+すぎる compositionally) — removed from m36 newAtoms/introduces; m27–m46 recompiled, content re-emitted, no wording change, no new clip; Q2 baseline 2→0; JA curriculum 8053 green. Ledger conflict union-merged.

- A8b — 2026-09-17 14:33: root cause of the reviewSplit.test.ts/switchoverBeatIntegration.test.ts slowdown (6.88s→13.12s at f18311e1) was NOT the reviewGridsFromFsrs flag machinery (already correctly 0-cost off — pinned in buildTileFloor.test.ts's "flag-off cost invariant" block) but f18311e1's UNCONDITIONAL `recordReviewStepsServed()` wiring in mockLessons.ts, which fired real telemetry (localStorage JSON.stringify per event) on every internal/background `getMockLessonContent` read too — confirmed by counting spy + stack trace: minedSentences.ts's whole-course miner (25,883 calls in switchoverBeatIntegration.test.ts alone), plus grammarReviewIndex.ts/grammarReviewPools.ts's memoized whole-course harvests, plus lessonAtomIndex.ts's per-lesson atom-attribution cache — none of these are learner-facing serves. Fix: gated telemetry on `floors` (already the "real serve vs internal read" signal, see its own doc comment) in mockLessons.ts, plus a new orthogonal `recordTelemetry` option for the one caller (lessonAtomIndex.ts) that needs real padded content (reads `step.tiles`) so can't use `floors:false` without changing behavior — passes `{recordTelemetry:false}` instead, content byte-identical, only the side effect suppressed. Two extra files touched outside the original file-ownership list (grammarReviewIndex.ts, grammarReviewPools.ts, lessonAtomIndex.ts) — flagged for the lead's review; changes are additive/optional-param only, verified safe (grammar/rule-index scans never read padded fields; the one caller that does keeps floors:true, byte-identical output, only telemetry silenced). Local (this Mac): tests phase 13.12s → 5.90s median (3 runs: 6.03/5.87/5.90s), clean-worktree measurement below. New pin: mockLessons.telemetryGate.test.ts (0 calls under floors:false / recordTelemetry:false, >0 under a real serve) + buildTileFloor.test.ts's FSRS-store-never-touched-when-off spy pin (task 3's literal ask — was already true, now pinned). Both proven to fail on the pre-fix code (reverted, ran red, restored). tsc -b clean; `npx vitest run --project app src/features/lesson/data src/features/practice src/features/flashcards src/shared/telemetry` — see report-A8b.md for counts.
- 2026-09-17 14:37 — A8b DONE (4a3f6499): cause was recordReviewStepsServed firing on every INTERNAL getMockLessonContent read (25,883 calls in one test file); gated on real serves (recordTelemetry defaults to floors), harvesters pass floors:false; two files 13.49 s → 5.52 s clean (below the pre-A8 6.88 s); spy pins for both invariants. FREEZE #4 for build 26.
- 2026-09-17 14:40 — b26 freeze #4 = 1884a798: preflight 688 files / 18,531 tests green; pushed; IPA rebuilt 23,601,478 B with all markers (StepRenderer-CCBLXX_f.js). Watching ci 35272109297 (first run of the 2-way shard matrix + gates-nonempty job) + deploy 35272109261.
- 2026-09-17 14:54 — 1884a798: deploy GREEN, prod verified by content (data-lesson-prompt, telemetry/errors, chip css all present in the live bundle). ci: both unit-test shards + gates-nonempty GREEN; typecheck job failed on npm ci (nuget ETIMEDOUT fetching onnxruntime-node GPU binary — network, not code) → rerun in progress. Build 26 UPLOAD started (release-b26.sh UPLOAD=1 → asc-post26).
- 2026-09-17 14:57 — BUILD 26 UPLOADED (1884a798, IPA 23.6 MB, Delivery UUID f3029703-afc4-4382-be69-e1f61250dc06). asc-post26.sh had VER=25 left over from the copy → it stamped build 25 with the build-26 notes and re-submitted build 25 (422, already approved). Fixed: build 25 notes restored, VER=26, script re-run (asc-post26b.log). ci rerun: typecheck/unit-tests/gates/build GREEN, mobile-e2e in progress.
- 2026-09-17 14:58 — BUILD 26 APPROVED (1884a798, build id f3029703-afc4-4382-be69-e1f61250dc06): VALID on poll 2, what's-new set, group attached, beta review APPROVED. Areas 1–4 (primitives, a11y, observability, native slimming) + testing/storage/procedural-QA/learning-loop are on TestFlight.
- 2026-09-17 14:58 — A9 — deps/platform/cost DONE (lane/A9, 9 commits: 97864997 6bb5148f 69fa716a e6b63a4b 5561b0f9 66058f0c fa49be14 8f34e775 41d825d7). In-major bumps behind tsc -b + scoped tests: react-router-dom 7.0.1->7.18.4 (50 files/246 tests), react-query 5.62.0->5.103.1 (31/118), @playwright/test 1.60.0->1.63.0 (config `--list` verified 3050 tests/27 files; full e2e run not attempted — needs a dev server this lane wasn't set up to hold), i18next 25.8.10->25.10.10 + react-i18next 16.5.4->16.6.6 (in-major only, per brief; 102 files/994 tests each), @auth0/auth0-react 2.15.0->2.26.0 (BROKE tsc — 2.25.0 narrowed getAccessTokenSilently() to `Promise<string|undefined>`; fixed with a throw-on-undefined guard in src/shared/api/provider.tsx, outside this lane's file list but a direct necessary consequence, flagged; 30/101). lucide-react 0.575.0->1.47.0 ATTEMPTED + REVERTED: 1.x removed ALL brand/logo icons (Lucide's own policy, GH #3557) — broke the `Github` import in iconRegistry.ts (2 call sites); picking a replacement icon is a design call outside this lane's scope, net package.json change: none. Lazy-loaded non-English UI locales (i18next-resources-to-backend + useSuspense:false, main.tsx untouched): entry chunk 247,198 -> 168,583 bytes gzip (-76.8 KB), matches lane A4's ~77KB prediction; entryChunkWeight.test.ts extended (proved-can-fail) + new lazyLocale.test.ts (real i18next instance, proved-can-fail). On-device sim verification partially blocked: English confirmed on 15 Pro Max; Korean blocked because the shared :5399 dev server was serving a DIFFERENT worktree's pre-change code (curl-confirmed root cause) — did not force-restart it (would silently redirect every other concurrent lane's captures); added a `?lng=` querystring detector for future use, substituted lazyLocale.test.ts as real functional proof of the same code path. Android 16KB page-size: NOT AFFECTED — zero `.so`/`.aar`/native-code anywhere in android/ or any of the 4 Capacitor Android plugins (grepped + found, both empty); full `:app:dependencies` Gradle listing blocked by a pre-existing missing generated scaffold file (capacitor-cordova-android-plugins/cordova.variables.gradle, needs `npx cap sync android`, not run since android/ is read-only for this lane) — manual check already conclusive. Removed dead `jaRomanizer` (zero callers, independently re-verified beyond A4b's finding; KO's own romanizer left untouched, shared/romanization/Romanizer.ts left untouched — turned out to be an unrelated, entirely-unwired same-named type, not what jaRomanizer implemented, flagged rather than guessed at). Q9 kana-row exemption: JA module 1's ya/wa kana-row micro-lessons (6-9 steps, 4 findings) judged exempt by design — FR's density doctrine targets multi-item teaching lessons, JA's kana-row family deliberately teaches one symbol per sub-lesson (documented in each row file's own header); `appliesTo` now skips the `ja-m1-<row>-<n>` pattern (57 lessons), baseline 4->0 (ratchet only allows lower, regression-classes C7), full justification + findings table in docs/procedural-qa-2026-09-17.md §4a. Majors (TS 7/Vite 8/Vitest 5) explicitly left alone per the brief. Final verification: npx tsc -b clean; npx vitest run 703 files (687 passed/1 failed/15 skipped), 18,559 tests (18,530 passed/1 failed/28 skipped) — the 1 failure is tileFit.property.test.ts, a fast-check floating-point-epsilon edge case on one random seed, file untouched by this lane (outside ownership, explicitly DO-NOT-TOUCH), reran in isolation 10/10 pass, attributed to its owner; npm run build succeeds. docs/platform-2026-09-17.md published with the full bump table, locale-lazy numbers, 16KB verdict, and what was deliberately left. regression-classes checked: C4 (both new tests proved-can-fail), C7 (Q9 baseline lowered with stated cause), C14 (own worktree/branch, explicit-path commits throughout, chose not to disrupt the shared dev server), C15 (verified the jaRomanizer/Romanizer.ts mismatch before acting, didn't delete the wrong file).
- 2026-09-17 15:01 — lane/A9 MERGED (8dfe96b8): react-router-dom 7.18.4, react-query 5.103.1, Playwright 1.63.0, i18next 25.10.10 / react-i18next 16.6.6, lazy non-EN UI locales (entry chunk −76.8 KB gzip), dead jaRomanizer removed, Q9 kana-row exemption (baseline Q9 4→0), Android 16 KB page size: no native .so anywhere. @auth0/auth0-react bump DEFERRED (53b8d7a9): 11 minors on the login path incl. an IPSIE session-lifetime ceiling — needs a real native login on the sim first; token guard kept. lucide-react 1.x reverted by the lane (brand icons removed). ci rerun on 1884a798: all jobs green except mobile-e2e still running.
- 2026-09-17 15:02 — Lane A2d LAUNCHED (golden-learner tap replay: tile_tap events + panel export, sim:capture --replay by label, 3 golden recordings, prove-it-can-fail). Lanes active: A7e, A5d, A2d.
- 2026-09-17 15:17 — ci rerun on 1884a798: mobile-e2e FAILED (public render gate 141/141 green; axe a11y gate 3/4 red). Root causes: (1) mobile-e2e job never ran content:emit → lesson routes fetch /content/v1/manifest.json → 404 → "This lesson could not be loaded" (Playwright error-context DOM proves it); (2) settings modal has REAL a11y defects the lane never caught: htmlFor on a <div> (locale select + volume slider had no accessible name, critical ×2) and 11 px meta text on the accent-muted home row at 4.3:1 (needs 4.5). Fixed: emit step in the job, SettingRow renders a real <label for>, primary-row meta uses text-secondary (5.57:1). Local a11y run 4/4 green, tsc clean.
- A7e — 2026-09-17 15:15: Procedural QA extended to KO/ES/FR (JA untouched — committed baseline was already Q2:0/Q3:0 post-A7d, confirmed unchanged, 11/11 checks.test.mjs green, ratchet 22.2s). KO: kiwipiepy (LGPL v3, subprocess-only) sidecar; Q2 redefined as a whitespace-stripped reconstruction check (KO particles/copula attach without a space — the word-joined ES/FR rule false-flagged 5/58 correct particle/copula splits before the fix), 0 hits, enforced baseline 0; Q3 via Kiwi content/function tags, measured 0/8 true on a full hand audit (grammaticalized constructions Kiwi POS alone can't resolve — 고 나서/을 거예요/줄 알다 — plus legitimate idioms) — kept INFORMATIONAL for KO only via a new per-language `enforced` function on the check (index.mjs's resolveEnforced), baselined at 8/Q9 191 (structural: many short KO sub-lessons per module, not per-lesson slips). ES: simplemma sidecar (MIT); Q2/Q3 both 0/0 vacuous across 1,101 applicable steps, enforced. FR: simplemma + Lexique 3.83 (CC BY-SA 4.0, sha256-pinned fetch under artifacts/) for Q3's function-word fallback; Q3 measured 13/13 TRUE on full hand audit (100%) after 4 false-positive classes found+fixed (atom-priority-over-function-table bug, number+unit-noun phrases, avoir/être auxiliary forms, capitalized-proper-noun, and a phrase-choice-bank exclusion mirroring JA's picker exclusion) — enforced, baseline 13 (coherent-but-compositional tiles like "mangé de gâteau", m18, reported not fixed); Q9 baseline 6 (module-closing recap lessons the isTeaching regex misses). Q4/Q8/Q10 stay n/a for ko/es/fr with an explicit naReason (Q8's JA-hardcoded loadIr would have silently cross-loaded JA's IR for a colliding "m1" moduleId — caught before it ran). Q1/Q6 ported generically (lib/surfaces.mjs, field-based not script-based) except FR, which stays n/a — normalizedAtoms.ts has no fr adapter (out of file-ownership scope). baseline JSON reshaped to {ja,ko,es,fr}; proceduralQa.test.ts now 4 it()s, one per language, combined 25.1s cold (well under the 150s CI budget). Lexique fetch+attribution in scripts/lexical/fr/fetch-lexique.mjs + doc §12. All verification green on branch lane/A7e: node --test scripts/qa/procedural/*.test.mjs 11/11, npx vitest run src/test/proceduralQa.test.ts 4/4, npx tsc -b clean.
- 2026-09-17 15:19 — lane/A7e MERGED: procedural QA Q1–Q10 for KO/ES/FR (kiwipiepy LGPL subprocess, simplemma MIT, Lexique 3.83 sha-pinned); baseline now per-language {ja,ko,es,fr}; JA Q9 kept at 0 (A9 exemption); FR Q3 = 13 true compositional-tile hits (100 % precision, reported not fixed, doc §11); KO Q3 informational (0/8 true); KO Q9 191 = course structure. Gate 4/4 in 25 s. NOTE: `git rebase origin/main` on a branch with merge commits flattens them and re-conflicts — skip the rebase when origin/main is already an ancestor.
- 2026-09-17 15:21 — VACUITY FOUND: procedural-QA "enforced" questions go n/a when their sidecar is missing; CI has no Python venv → JA Q3 (4,134 applicable steps locally) reports 0/0 on CI and passes. FR counts identical with/without Lexique (Q3 13, Q9 6). Lane A7f LAUNCHED: applicable-step floors in the baseline + sidecars installed on CI + prove-it-fails. Lanes active: A5d, A2d, A7f. Preflight of 08475df6 running in b26-wt.
- 2026-09-17 15:23 — preflight of 08475df6 in b26-wt: 18,539 passed, 1 FAILED: tileFit.property.test.ts seed -151753230 (widthRatio 0.43999999999 quantizes to 0.43, below an unquantized floor 0.4300000005 → resolveTileScale returns the floor; the property asserted quantize(widthRatio) at 1e-9). Test contract corrected to max(quantize(width), floor) + fixed regression example; 3×11/11 green. No runtime change.
- 2026-09-17 15:25 — 35b6243d PUSHED to main (deploy 35276428122, ci 35276428181 running). Build 27 dry-run archive OK from the main checkout at 35b6243d (IPA 23.6 MB); bundled checks: Korean UI locale is its own lazy chunk (ko-OdqaPBWV.js, 26 strings; 0 in the entry), settings label fix present. Upload waits for ci+deploy green and prod content verify.
- 2026-09-17 15:40 — 35b6243d: deploy GREEN, prod verified by content (entry has 0 Korean UI strings, ko-*.js lazy chunks referenced, SettingsContent chunk carries the new <label for> row). ci: typecheck/unit-tests ×2/gates GREEN, build job FAILED on npm ci (onnxruntime-node postinstall ETIMEDOUT to a Microsoft CDN — second time today) → rerun; root fix: ONNXRUNTIME_NODE_INSTALL=skip env in ci.yml + deploy.yml (flag proven locally, exit 0). A7f parked on a background monitor → told to watch in the foreground.
- 2026-09-17 15:59 — ci 35276428181 rerun: build job green, mobile-e2e RED again — settings a11y now passes on CI, but both lesson-step routes still "could not be loaded". Playwright trace: /content/v1/manifest.json answered 200 with the Vite SPA shell (6,195 B HTML), i.e. not in Vite 6.4's publicFiles Set at request time. ROOT CAUSE: playwright.config starts THREE dev servers at once, each `npm run dev` → predev → content:emit → three concurrent rmSync(OUT)+rewrite; on the Linux runner one server's public-file scan ran during another's delete window. FIX in emitContent.test.ts: mkdir lock (serialize), skip when the tree already matches the manifest (version + every file), stage in v1.tmp and swap by rename. PROOF in b26-wt: 3 concurrent emits on an empty tree → 3/3 pass, 137 modules, 0 missing files; a run on a complete tree → same manifest mtime + same dir inode (NO-OP). Build 27 UPLOADED (Delivery 80dbaaf6-b301-49db-b7a4-5d3cb776760f), ASC processing.
- 2026-09-17 16:00 — BUILD 27 APPROVED (35b6243d, build id 80dbaaf6-b301-49db-b7a4-5d3cb776760f, IPA 23.6 MB): A9 platform lane + settings a11y labels + home contrast + lazy UI locales. Next push: 902f5475 (content:emit race fix + ONNX skip env + proposals) after preflight in b26-wt.
- A7f — 2026-09-17 15:51 ee181901 740053cb: procedural-QA gate was vacuous on CI (no Python venv, no JMdict/Lexique — JA/KO sidecar-backed questions answered n/a for every step and passed with 0 findings). Fixed both halves. (1) proceduralQa.baseline.json reshaped {question: count} -> {question: {max, minApplicable}}; minApplicable = floor(measured applicable-steps x 0.95) per enforced question (ja Q2 3916/Q3 3927/Q4 4028/Q5 3656/Q7 7740/Q8 515/Q9 547/Q10 8156; ko Q2 55/Q5 820/Q7 786/Q9 256; es Q2 1045/Q3 1045/Q5 2378/Q7 4058/Q9 360; fr Q2 428/Q3 266/Q5 1342/Q7 2009/Q9 246) — no ceiling changed. proceduralQa.test.ts now tallies applicable counts itself from report.rows (no run.mjs output-shape change needed) and fails with a message naming the likely missing sidecar/artifact. Deliberate-failure proof: LINGO_LEXICAL_PYTHON_JA=/nonexistent/python (new env override on all 4 sidecar.mjs, falls back to the existing .venv default) collapsed ja.Q3's applicable count 4134->0 and failed the floor while ja.Q2 (JMdict-only, no sidecar dep) stayed green; renaming artifacts/lexical/jmdict dropped both ja.Q2 and ja.Q3 — both reverted, green again. (2) ci.yml's unit-tests job (both shards) + deploy.yml's build job now install the JA (fugashi+unidic-lite) and KO (kiwipiepy) sidecar venvs and fetch JMdict+Lexique before the vitest run: actions/setup-python + astral-sh/setup-uv@v10.1.0 (pinned exact — no bare v10 floating tag exists, confirmed via gh api and the first CI attempt failing on it) with enable-cache keyed on new scripts/lexical/requirements-{ja,ko}.txt pin files, actions/cache for artifacts/lexical/{jmdict,lexique} keyed on the fetch scripts' own content. Verified for REAL on CI, not just locally: pushed lane/A7f + feedback-2026-09-14 (neither existed on origin before) and opened draft PR open-lingo/lingo#10 (feedback-2026-09-14 base since ci.yml's pull_request trigger has no base filter). First cold-cache run (35277581223, commit 740053cb): unit-tests both shards GREEN, sidecar-install block 23.8-26.1s wall, gate log shows every language's applicable counts bit-identical to local measurement and above every floor (ja Q2 4123/Q3 4134/.../Q10 8586, ko/es/fr likewise) on the actual ubuntu-latest runner. Both pinned Python deps installed cleanly (prebuilt wheels for the runner's platform) — no KO wheel gap to work around, floor stays at the full measured value. typecheck + gates-nonempty green same run. Local: npx vitest run src/test/proceduralQa.test.ts 4/4 green (sidecars present), npx tsc -b clean. docs/procedural-qa-2026-09-17.md §13 "Vacuity on CI" has the full writeup + floor table + CI log excerpts.
- 2026-09-17 16:02 — lane/A7f MERGED (45b83034): baseline now {max, minApplicable} per enforced question (floors = 95 % of measured); CI installs JA/KO sidecars (uv, ~25 s cold) + JMdict/Lexique (cached); proven on a real runner (35277581223): applicable counts bit-identical to local; deliberate failure caught with a message naming the missing sidecar. A7f also pushed feedback-2026-09-14 + lane/A7f to origin (PR #10 draft) — delete after this push. Preflight of 45b83034 running.
- 2026-09-17 16:04 — preflight of 45b83034 in b26-wt: 18,540 passed, 1 FAILED = the NEW floor gate (ja Q2/Q3 applicable 0 < floors 3916/3927) because the detached worktree had no JA venv/JMdict — the gate working as designed; sidecar installed there (uv venv + requirements-ja.txt + fetch-jmdict). Rule: every preflight worktree needs the sidecars now (docs/procedural-qa-2026-09-17.md §13).
- 2026-09-17 16:24 — 0600c439 PUSHED: ci 35280213327 ALL GREEN (typecheck, unit-tests ×2 with sidecars + floors, gates-nonempty, build, mobile-e2e incl. axe on lesson routes — the emitter race fix confirmed on the Linux runner); deploy 35280213174 GREEN; prod serves manifest version cbf711c0bf + 3 ko lazy chunks. No build 28 for this lap (binary content unchanged). Remote lane refs deleted, PR #10 closed, A7d/A7f worktrees removed. Lanes active: A5d, A2d.
- 2026-09-17 16:25 — Lane A5e LAUNCHED (gate mutation sweep: inventory every gate, mutate each in a temp copy, CAUGHT/MISSED table, weekly workflow). Local CI-order repro stopped (root cause already proven on CI). Lanes active: A5d, A2d, A5e.
- 2026-09-17 16:31 — A2d — golden-learner replay DONE (lane/A2d, 8 commits: 9c8fbd21 63d71bbb dccc61d6 1ac3567b 2f577349 [+goldens/runner/docs] 1e178451 ab2568fd): `tile_tap` session-log event (src/shared/telemetry/sessionLog.ts, capped 60/step) fires from BuildSentenceStepView/ListeningBuildStepView's addTile/removeTile; "Copy tap replay" export in the Sync panel; `npm run sim:capture -- --replay <file> --speed 0|1` drives the same route/viewport/font-scale via a real DOM `.click()` per recorded LABEL (BANK_TAPPABLE_SELECTOR/TRAY_TILE_SELECTOR, exact-then-CONTAINS match for concatenated kanji+furigana textContent), hard-fails with visible-labels-listed on a missing tile, runs the existing 9 build verdicts + pixel-compares against a co-located baseline PNG; `npm run sim:replay` (scripts/ux-loop/replay-runner.mjs) runs all `tests/visual/golden/*.replay.json` and prints name|taps|verdicts|pixelDiff|PASS/FAIL, non-zero exit on FAIL. 3 goldens recorded live via the simulator (`--record-golden`, not by hand): normal-build-100 (6 taps, build_sentence, 100%), huge-bank-125 (21 taps, listening_build, 125%), listening-build-100 (6 taps, listening_build, 100%) — all 3 PASS at pixelDiff=0.0000%. Proved it can fail: a deliberate `--tile-font: 18.3px→24px` break in src/index.css FAILED ALL THREE goldens (0.12%/12.58%/5.59% pixelDiff — the token is a shared :root ratio base, stronger proof than the one-golden prediction), clean PASS on revert; both runs pasted in docs/golden-replay-2026-09-17.md. Doc + docs/mobile-sizing-spec.md §9a (pre-build step, not CI). Found+fixed 8 real bugs live on the real simulator (SIM_DEV_PORT port conflict, missing content:emit, atob() UTF-8 corruption, ZWSP label mismatch, emulated-viewport safe-area-inset zeroing, hard-fail-branch 18s hang, report-selection contamination by tapEvent/marker posts, and — found only by reading the printed table, not the pixelDiff line — report.simulation silently stuck null so the verdicts/taps columns always printed "-"/"0P/0F/0N" even on a clean pixelDiff PASS; root cause + fix in commit 1e178451). One scoped, documented, additive-only deviation outside the owned-files list: src/shared/dev/simProbe.ts (no Node→browser JS-execution channel exists anywhere in this harness — researched before touching it — replay driving code had nowhere else it could live). Before-done checklist: tsc -b clean, node --test sim-capture.test.mjs 181/181 + replay-runner.test.mjs 9/9, vitest src/shared/telemetry 39/39 + step views/panel 25/25. Full report: scratchpad/review/report-A2d.md.
- 2026-09-17 16:35 — lane/A2d MERGED (164aa0e6): tile_tap session events (60/step cap) + "Copy tap replay" in the Layout-trace panel, `sim:capture --replay` by label, `npm run sim:replay` over tests/visual/golden (3 goldens: normal-build-100, huge-bank-125, listening-build-100 — all PASS at 0.0000 % pixelDiff), prove-it-can-fail: --tile-font 18.3→24 px broke all three (0.12 / 12.58 / 5.59 %). Lane also found report.simulation silently null in every replay (fixed). Deviation: simProbe.ts edited (browser-side driver; no other channel exists).
- 2026-09-17 16:37 — preflight of d34aba73 in b26-wt GREEN (18,549 passed); PUSHED to main; ci/deploy watching. Build 28 dry-run archive started from the main checkout (pbxproj 28); upload after ci+deploy green + prod verify.
