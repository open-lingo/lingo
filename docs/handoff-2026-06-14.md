# Session handoff — 2026-06-14 (retention engine + FTUE + Trevor merge)

## What shipped (branch `lesson-ui-srs-deck-2026-06-13`, merged to main)

**Retention engine (Phases 1–4):**
- **1a Visible backlog** — `buildReviewQueue.unseenTotal` → `backlogCount`/`newToday`; Flashcards page shows "N more queued — X new/day".
- **2 Adaptive new-card cap** — `adaptiveNewCardsPerDay` drains backlog over ~3 wk (5→15), self-correcting to unlock rate.
- **3 Track B grammar SRS** — `grammarSrs.ts` schedules the 93 `n5-grammar-points.json` (separate store); `grammarReviewIndex.ts` reuses authored particle-clozes (no fabrication; uncovered points logged); `buildSrsReviewLesson` appends grammar steps (`exercisedGrammar`); `LessonPage` grades both tracks; D4 sentence-vocab credit.
- **4 Derived reviews** — Learn "reviews due" chip is performance-derived from real Track A+B due-state, not the blind module calendar. CLAUDE.md SRS invariant corrected.

**FTUE (research + P0):**
- Deep-research pass → `docs/ftue-design-2026-06-14.md` (value-first, self-chosen goals + commitment CTA, learn-by-doing — evidence-backed, 3 claims refuted/excluded).
- **P0 first-session arc** (`FirstSessionArc.tsx`): new-user-only motivation → self-chosen daily goal ("Commit to my goal") → explicitly-optional placement ("Start from the beginning" primary). Non-patronizing. Reconciled with the pre-existing standalone PlacementPrompt (arc now owns placement). E2E-verified.

**Daily-review quest (1b):** frontend `FlashcardTester` reports session reviews + auto-completes on caught-up. (lingo-core piece is in Trevor's WIP — see below.)

**Merge:** integrated Trevor's `origin/main` (15 commits, 6 conflicts resolved). ~1061 tests green, tsc clean.

---

## NEXT STEPS (prioritized)

### Do soon
1. **FTUE P1 — contextual feature discovery** (the natural next build). Surface SRS/quests at first relevance — esp. a **"first review" prompt after lesson 1** that introduces flashcards, wiring the due-chip + backlog we just shipped. Evidence: learn-by-doing / surface-at-first-relevance. (`docs/ftue-design-2026-06-14.md` §2 P1.)
2. **Validate the retention loop live** — Track B grammar reviews + the derived chip were built but never seen *populated* (walkthroughs ran on no-SRS profiles). Progress a real user through a module → confirm grammar reviews + due-chip fire end-to-end.

### Trevor (lingo-core, cross-repo)
3. **Commit the `daily-reviews` quest** — my edit to `app/quests/logic.py` + `tests/test_quests.py` is **uncommitted in his working tree** (his `app/quests/` is untracked WIP). He should commit it with the rest of the module. Plus: swap-when-nothing-due generation, bump-endpoint trust bounds, `adFreeMinutes` reward application. (`docs/followups.md` → "Daily-review quest".)

### Architecture / bigger
4. **SRS storage unification** (Spencer 2026-06-14) — treat anything SRS as a deck card; one store, not a store per concept. Fold the Track B grammar store into the deck system when doing more grammar SRS; check the course for consistency. (`docs/followups.md` → "SRS storage unification".)
5. **Conjugation Trainer** (FTUE D2 feature) — dedicated drill surface for conjugation sub-patterns, separate from lesson flow. Needs its own design. (`docs/ftue-design-2026-06-14.md` §… / retention §10.)
6. **Retention on-ramp deep-dive** — getting atoms into FSRS earlier without breaking "reviews are the only graded surface" + cross-day spacing nudges. (`memory` + followups.)

### Tax-day / polish
7. **Extend conformance tests to M8–M15** — machine-guards half the course (currently author-discipline only). ⚠ `m[3-7]` range-hardcode landmine.
8. **Lesson UI polish ledger** open items — match-pairs shouldn't use full sentences; trace CHECK clickable on empty canvas; verify Translate/FillBlank CTA anchor; cosmetics. (`docs/followups.md` → "Lesson UI polish ledger".)

### FTUE later (P1/P2)
9. Streak milestone animations; instrument retention + derive our OWN leverage metric (don't copy Duolingo's CURR); coach character (evidence-suggestive).
