# INDEX.md — research map for agents

**Status:** LIVE · built 2026-07-30 · maintained by the daily session-start index job — keep entries to one line, keep status tags current, don't reformat.

Read this file FIRST when researching the project, then jump straight to the named doc. Don't grep docs/ blind: ~half of the 160 md files are stale or archival, and most (but not all) self-flag it.

## Precedence when docs disagree

1. `authoring-invariants-pinned.md` — the pinned JA law
2. `CLAUDE.md` (repo root) — code invariants, SRS write surfaces, commands
3. `retrospective-2026-07-17.md`
4. `lesson-authoring-guide.md` (§13 = locked template)

`PROJECT_STATE.md` is STALE (self-flagged; still ok for launch/feature-flag picture only). `docs/tasks/*` = backlog, unverified — never current state.

## Where to look for X

| Question | Go to |
|---|---|
| JA authoring rules / invariants | `authoring-invariants-pinned.md` → `lesson-authoring-guide.md`; process: `authoring-workflow.md`; per-module packs: `context/mN-context.md` (regen: `node scripts/authoring-context.mjs mN`) |
| Authoring conformance audit | `authoring-audit.md` (generated by `npm run authoring-audit`; per-module invariant backstop over compiled output) |
| IR→compiler pipeline (the pivot) | `content-ir-spec-2026-07-20.md` (APPROVED 07-23); build: `node scripts/compile-ir.mjs mN`. m6–m29 are IR-compiled (`curriculum/ir/m{6..29}.ir.yaml`); m3/m4/m5-neo are hand-authored TS, m30 too (this row said "only m6" until 2026-07-29) |
| SRS / scheduling | CLAUDE.md §SRS (six write surfaces, binding) + `srs-scheduling-model-2026-06-15.md`; research: `srs-memory-retention-research-2026-07-19.md` |
| Max-acceptance grading, sibling distractor banks | **NOT in docs/** — code comments in `src/features/languages/ja/jaAcceptedForms.ts` + `jaSiblingSets.ts` (2026-07-24, newer than all docs) |
| Gate 10 visual QA | `visual-qa-gate-2026-07-17.md` + `scripts/visual-qa/judge-prompt.md` (judge protocol, abort-don't-improvise); run: `npm run visual-qa:contracts` / `visual-qa:capture` |
| Learner-sim QA / course walk audit | `learner-sim/FINDINGS.md` (raw log, 185 findings: 53 BLOCKER/67 CONFUSING/63 NIT); `STATE.md` (learner state, m1–m29 COMPLETE); `TRIAGE.md` (triage record, open sections → backlog/items.yaml 07-28); `INDEX.md` (generated per-module view, `npm run learner-view`) |
| Module readiness gate | `npm run module-gate -- mN` (vitest + TTS coverage + tsc + optional capture/exposure) |
| Pedagogy / what explanations may claim | `pedagogy-principles-2026-07-05.md` (binding); source recon: `jouzu-juls-cure-dolly-recon-2026-07-02.md` |
| Rewrite status / spine | `rewrite-cycle-report-2026-07-20.md` (living log); `spine-draft2-adversarial-audit-2026-07-19.md`, `vocab-frequency-audit-2026-07-19.md`; module specs: `m4-neo-` / `m5-neo-authoring-spec-2026-07-20.md` |
| ES course | **scope: `es-authoring-scope-2026-08-09.md`** (phased Phase 0–3 recommendation, decisions OPEN §8; measured TTS gap 719/1,879); research behind it: `research/es-teaching-research-2026-08-09.md` (4-agent EN→ES pass); style contract: `es-rewrite-brief-2026-07-16.md`; vocab tables: `es-course-spine-2026-07-13.md` (style section there superseded); quality: `es-content-quality-audit-2026-07-16.md`, `es-ja-parity-2026-07-15.md` |
| Kanji behavior | CLAUDE.md (LIVE from M8 via `applyKanjiSurfaces.ts`) — all three 07-16 kanji docs (furigana-plan / implementation-spec / timing-research) are STALE |
| Kana→kanji switchover beat (B061) | **LIVE since 2026-07-30 (B069 phase 1): `withDynamicReviewPrefix` (`lesson/data/dynamicReviewPrefix.ts`) prepends the beat + due atoms + due grammar + new-card seats onto every `ja-mN-neo-review-*` at content-load time; conformance-tested in `dynamicReviewPrefix.test.ts`; preview `/ja/qa/review-prefix`. Phase 2 (IR dynamic slots) deferred.** `kanji-switchover-design-2026-07-28.md` — **§6 is the 07-29 decision** (variant C, 2 steps: animated reveal + graded sentence question); §1 the measured scope, §2 the research. Companion: `kanji-switchover-distributed-spec-2026-07-28.md` (a REJECTED 3-beat plan kept for its findings — §6a proves review tails don't exist at m8+, §7a is the pretesting evidence the decision overrules). Live: `/ja/qa/kanji-switchover` (variant gallery) → `/ja/qa/kanji-reveal` (animation bake-off, 6 candidates). Stroke data: `shared/glyphs/data/kanji.json`, 147 glyphs, rebuild with `node scripts/build-kanjivg-data.mjs` |
| **Taught words that never unlock (B068 — fix option 2 SHIPPED 2026-07-29, 287→226)** | `vocab-exposure-audit-2026-07-29.md` **§5.1** — atoms graded by a live lesson that never enter the unlock store (unlock gates the flashcard deck + every dynamic review pool). Was 287; `isDeadAttribution` in `lessonAtomIndex.ts` now treats an unresolvable `introducedByLessonId` as unset so the module fallback fires (positive-controlled). Residual 226 = legacy-`fromModule` mismatch, cleared per vocab pack by re-homing. Ratcheted in `lessonAtomAttribution.test.ts`: MAX_DANGLING_ATTRIBUTIONS 227, MAX_GRADED_NEVER_UNLOCKABLE 223 (as of packs 1–6, 2026-07-30) — lower with each pack. `applyPlacement.ts` ~147 remains the placement-only second unlock path |
| Vocab pack wave — the PLAN (approved direction) | `vocab-pack-wave-plan-2026-07-29.md` — 16 mixed-POS in-course pack lessons; **packs 1–6 SHIPPED (m11 ×2, m13, m14, m16 ×2) — see the table's per-row ship notes**; then Travel Sprint + Anime quest (blocked by B066), QA gates incl. Sonnet learner-sim; word lists locked per-module at authoring time |
| Open product decisions awaiting Spencer | `decision-brief-2026-07-29.md` — RESOLVED 07-30: B069 (option 3 shipped, phase 2 deferred), MAX_NEW (5→8, "increase as needed"), D1–D8 (§6, approved+binding). STILL OPEN: retired -kata rows keep/drop (drop after packs 5/10), B040 giving-verbs go/no-go + module home, dialogue_sim's 5 design rulings (prototype live at /ja/qa/dialogue-sim) |
| Reverse teaching (ko→ja / ko→en) readiness | `reverse-teaching-readiness-2026-07-29.md` — grading is instruction-language-clean in the lesson player EXCEPT four English-shape couplings (worst `jaAcceptedForms.ts:127` `/^to /` on meaningEn); ~14,960 content-English strings measured; §4 = the D1–D8 authoring rules (APPROVED 07-30, binding packs 5–16); schema rec: build-time extraction + per-language sidecars, ~3–4 months out |
| Stale/old-course reference regressions | `stale-reference-audit-2026-07-29.md` — 240 fromModule mismatches + 696 dead lesson-id refs bucketed; spawned three CI gate families (`fromModuleDrift`, `staleLessonIdReferences`, `docClaimGuards`); the drift table is the pack wave's per-module burn-down list |
| Per-pack learner-sim walks (QA verdicts) | `learner-sim/m11-packs-2026-07-29.md`, `learner-sim/m13-m14-packs-2026-07-29.md` — the pack-wave QA loop: BLOCKER→B074 engine guard, recognition-gap class→`recognitionExposure.test.ts` ratchet (rule 3, binding). 2026-08-09 wave: `m16-packs-2026-08-09.md` (both packs SHIP; MAJOR→B088 pad-pool engine item), `m8-rebuild-walk-2026-08-09.md` (rebuild net-positive, 5 CONFUSING fixed same-day in the IR; deferred→B089/B090), `m30-n4-walk-2026-08-09.md` (evidence file FOR the ratified m30-pilot retirement — don't fix in place) |
| Project-health audit (7-lens, evidence-per-claim) | `project-health-audit-2026-07-29.md` — top verified findings: CLAUDE.md orientation falsehood (fixed same day), 9 skipped m8+ kanji tests whose skip-premise died with the IR wave, manualResetAt dropped by lingo-core schema (cross-device reset loss), grammarHelpers.ts 2054-line hotspot; ends with an honest could-not-verify list |
| Words the live course never teaches (B065/B066/B067, +B070 leakage) | `vocab-exposure-audit-2026-07-29.md`, independently verified 07-29 — **the live map teaches 208 of 678 registered atoms**; 220 are never graded (216 real), 96 never appear at all, **73 are CEJC top-500** (§7 — rank-join hygiene matters, particle rows steal ranks); §5.2: three non-lesson surfaces (grammar-review pool, reading passages, placement bank) DO show a few of them untaught (B070); §7.3: late-teaching stragglers incl. 全部/絶対 first graded m30, giving verbs are an m30 event. §1 the measurement + why `atom.fromModule` is pre-rewrite numbering and must not be read as "where it's taught", §2 the frequency priority, §3 **the IR slot-in recipe + per-module homes** (four traps: registry row vs IR-only, `fromModule` drives the review pool + D2, unlock is automatic via exact-match attribution, the m19 guard test), §4 levers. Instrument checked in: `EXPOSURE_REPORT=1 npx vitest run atomExposureAudit`. Distinct from `scripts/exposure-audit.mjs`, which only counts TAUGHT atoms and is blind to this |
| Grammar deck / conjugation trainer | `grammar-deck-v1-spec-2026-07-02.md`, `conjugation-trainer-v1-spec-2026-07-02.md`; step type SHIPPED (was "draft"): `conjugation-transform-spec-2026-07-23.md` — **read its 2026-08-06 implementation-gap banner first**: the rule table is gated on a per-form ruleset and 52 of 59 live cards had none, so they taught nothing. `masu`/`masu-neg` repaired (m7); 9 forms still owed, ratcheted in `conjugation/transformRulesets.test.ts` |
| **N4 tier — the plan of record** | **`superpowers/specs/2026-08-06-n4-open-and-transform-teaching-design.md`** (LIVE) — retire the legacy m30 pilot, open N4 at m30 = n4-01, + the transform-card teaching layer that gates it. Spine (authoring source of truth): `spine-n4.md`, 22 units m30–m51, every open call decided in §5. Loop + standing hazards + measured N5 metrics: `RUN-PLAN-n4.md` — its "N4 IS CANCELLED" banner is SUPERSEDED (2026-08-06), everything below that banner is still binding. `n4-pilot-spine-2026-07-16.md` and `n4-scoping-2026-07-16.md` are HISTORICAL |
| Placement / test-out | 2026-07-15 specs in `superpowers/specs/` (placement-level-gate, testout-improvements); the 07-08 placement proposals are STALE |
| TTS | CLAUDE.md §TTS (emitter landmine); pipeline + voice QA tooling now lives in `../lingo-data/pipeline/tts/` (`pipeline/README.md`) — the lingo-core copy and `lingo-core/docs/TTS.md` are gone. CDN cutover: `../docs/tts-cdn-migration-2026-07-28.md` |
| Emoji / vocab art | `n5-vocab-emoji-reference-2026-05-18.md` (canonical map) + `emoji-blocked-words-2026-05-18.md` (blocklist current; its ja phrase_card advice stale) |
| Architecture | `ARCHITECTURE_REVIEW_2026-06-14.md` (cross-repo); backend: `../lingo-core/docs/INDEX.md` |
| **Outstanding work / gaps / bugs** | **`backlog/items.yaml` — query it with `node scripts/backlog.mjs`, never read it top to bottom. Schema: `backlog/README.md`. This supersedes the open sections of `followups.md` and `learner-sim/TRIAGE.md`.** |
| **Spencer-facing review queue (plain language)** | IN-APP: `/:lang/qa/review` (linked from the QA test-drive page). Entries live in `src/features/lesson/dev/reviewQueue.ts` — agents append (concrete example MANDATORY, contract in the file + enforced by `reviewQueue.test.ts`); Spencer's verdicts mirror live to `/tmp/lingo-review-queue.json`. How-to: `qa/README.md`. Feeds `backlog/items.yaml` (his answers → `note:` verbatim); replaces neither the backlog nor the walk reports |
| Product backlog / current tasks | `TODO.md` (LIVE 07-17); ignore FEATURES / PRODUCT_BACKLOG / MVP_* (all pre-launch-era STALE) |
| Frontend follow-ups | `followups.md` (LIVE 07-26) — backend/frontend contract gaps and deferred cleanups |
| Real-user feedback | `user-feedback/` (higher product signal than synthesized audits) |
| Competitive / research | `research/` — all 6 active (duolingo survey/teardown/gap, multi-language scoping, onboarding audit, spencer migration) |
| **Mobile / iOS / PWA** | **METHOD — start here for any mobile UI or layout work: `mobile-ui-testing-2026-08-09.md`.** It is the single authority: the gate (`tests/mobile/`) decides layout correctness, the device decides environment, and nothing else adjudicates either. It injects real safe-area insets over CDP (Chromium reports `env(safe-area-inset-*)` as 0, which is why no viewport could ever have caught the Dynamic Island bug), asserts WCAG 2.2 SC 2.5.8 at 24px + spacing instead of a flat 44px warning, and records why the simulator cannot be driven (`simctl` has no tap verb; XCUITest cannot see a WKWebView's DOM). Open work from the 08-09 pass is B091–B101 in `backlog/items.yaml` (`node scripts/backlog.mjs --tag system/mobile-gate`). Then: `mobile-testing-setup-2026-08-06.md` (the tier ladder; Tier 1 PWA SHIPPED 08-06). Native wrapper: `ios-wrapper-setup-2026-08-06.md` — **RUNNING ON HARDWARE 2026-08-07** (iPhone 15 Pro Max, free provisioning = own device only / 7-day profile / no TestFlight); its §2e is the four device-onboarding gotchas (Developer Mode, the `SecurityAgent` keychain hang, first-launch trust, reboot-after-trust) and §5 gap 1 is the still-open "audio not confirmed by ear". Offline/OSS scope: `mobile-offline-oss-scoping-2026-08-06.md`. Gate-coverage traps: `handoff-mobile-gate-was-blind-2026-08-06.md`, `handoff-mcq-fit-2026-08-06.md`. **Audio CONFIRMED by ear 2026-08-08**; "why does my phone look different from the simulator" is answered with the device-inspection recipe (pull the phone's localStorage over Wi-Fi, diff build products) in `handoff-device-vs-simulator-2026-08-08.md`. ⚠️ The **44px** tap-target rule in `mobile-research-2026-07-20.md` §6 assertion 3 is SUPERSEDED — it was the AAA criterion run as a non-failing warning, and measured across 19 routes it flagged 114 elements and zero real defects |
| Machine migration / infrastructure | `mac-migration-2026-07-29.md` — dev environment setup notes |
| AI-workflow / agent process | `ai-workflow-optimization-research-2026-07-17.md`, `dispatch-economics-log.md` (LIVE), `authoring-workflow.md` |
| Ads / finance / economics | `ADS_AND_FINANCE_ARCHITECTURE.md`, `ADS_PLACEMENT.md`, `ECONOMICS.md`, `finance-transparency-endpoint-spec-2026-05-25.md` |
| Past session decisions | thread index + grep cookbook: the `thread-index` memory, auto-loaded from `~/.claude/projects/<cwd-key>/memory/thread-index.md` (Claude keys per-project state by working directory with `/`→`-`; do NOT hard-code a machine's key here — it was `-mnt-c-Users-Spencer` on the retired WSL box). The transcript archives it greps are read by explicit path and their location is recorded in that memory. |

## Landmines (cost real research time)

- **-neo convention:** `m*-neo.ts` are the live modules; the bare old-course files (`m6.ts`…`m28.ts`) moved to `curriculum/_archive/` 2026-07-26 — resolve via course map (`src/shared/domain/mockCourse.ts`), never filename. m2 rows carry `ja-m1-*` ids. m28 EXISTS since the IR wave (`m28-neo`, spine tile s24 — this line said "m28 does not exist" until 2026-07-29). m30 (`ja-m30-*`, no -neo infix) opens N4.
- **Docs contradicting shipped code:** `dataformats/flashcards/anki-import.md` says not-implemented — it IS built (`anki-import-spec-2026-07-07.md` + `scripts/anki-export-known.py`). `lingo-core/docs/xp-curve-design-2026-05-25.md` claims client-side XP — server-authoritative `XpEconomyConfig` won.
- **Dead links:** `docs/SETTINGS_AND_DATES.md` (from settings README) and `FLASHCARD-DATA.md` (from docs/README) don't exist — don't chase them.
- **CI ≠ all tests:** `.github/workflows/ci.yml` runs tsc+vitest+build (lingo) / ruff+pytest (core), **plus a `mobile-e2e` job** (added 08-06; this line said "Playwright e2e is local/manual" until 2026-08-07). Gate 10 visual QA is still local/manual. ⚠️ `mobile-e2e` sets `MOBILE_PUBLIC_ONLY=1`, so CI exercises **2** public routes (`/get-started`, `/try`) — every authed/lesson assertion is local-only. That is 2 of 35 routes; the local full run is 921 assertions. **B094** — newly viable now the authed matrix is green (2026-08-09).
- **The mobile gate was measuring the public internet (found + FIXED 2026-08-07):** `/landing`, `/about` and `/login` are not routes in this app — they left for the live marketing site (`lingo-landing`, served from the apex) and Auth0's hosted login, so 3 of CI's 5 routes asserted layout on third-party origins, and a decorative overflow on the MARKETING site is what turned this repo's CI red on 08-06. `PUBLIC_ROUTES` is now `/get-started` + `/try`; anonymous routes get their own non-bypassed dev server (port 5274) and `_seed.ts` fails any navigation that leaves the origin. Full write-up: `handoff-mobile-gate-was-blind-2026-08-06.md`.
- **`__discover.mjs`** at lingo root is a throwaway debug probe, not infrastructure.
- Large uncommitted volume on main is normal mid-cycle here (fast-walk-loop pattern), not drift.

## Stale/superseded clusters (skip unless doing history)

- **May m3–m7 wave (4 docs, all in `archive/`):** m3-m7-audit-synthesis-2026-05-18, m3-m7-rebuild-spec-2026-05-18, wave-4-m3-m7-reauthor-2026-05-18, wave-4b-dispatch-briefs
- **Kanji (3 docs):** kanji-furigana-plan, kanji-implementation-spec, kanji-timing-research — superseded by shipped `kanjiRollout.ts` / `applyKanjiSurfaces.ts`
- **Placement 07-08 pair:** placement-questions-proposal, placement-testout-derived
- **Launch-era product docs:** FEATURES, MVP_PAGES_PLAN, MVP_PRODUCTION_READINESS, PRODUCTION_ROADMAP, PRODUCT_BACKLOG, n5-content-spec
- **Shipped-and-archival:** katakana-rollout-romaji-fade-spec, ko-conjugation-phase1, `archive/conjugation-trainer-recon-2026-07-02.md`, `archive/flashcards-anki-scoping-2026-06-13.md`, info-step-audit, `archive/qa-live-findings-2026-07-12.md`, workshop-agenda, `archive/practice-features-spec-2026-05-25.md`, `archive/handoff-2026-07-01-katakana-audit.md`, learning-science-foundation (frozen), card-agnostic-reviews, lesson-editor-research (claims SM-2 — wrong), `archive/retention-architecture-design-2026-06-13.md` (partially superseded), n4-scoping + n4-pilot-spine (BOTH historical as of 2026-08-06 — the pilot's m30 is being retired; live N4 authority is spine-n4.md + the 2026-08-06 spec)

## Directory policy

- `archive/` — all historical; its README is the policy. The May design/spec set (`2026-05-1*`, `m2-row-template`, `alphabet-learner`), the closed feature docs (`practice-hub`, `particle-practice`, `japanese-content`, `anki-import`, `lesson-reauthor-sentence-variety`), and the June/July plans (`srs-deck-unification-plan-2026-06-13`, `es-course-gaps-2026-07-13`, `es-course-spine-2026-07-13`) all live there now — don't look for them at the docs root. `superpowers/specs/` — May files stale; only the two 2026-07-15 placement/testout designs active. `tasks/` — backlog, unverified (homepage-ux + practice-hub explicitly closed; japanese-content/particle-practice/lesson-reauthor superseded). `dataformats/` — active except flashcards/anki-import. `context/` — generated packs, regen don't hand-edit. `agents/basecontext/` — dispatch context (AUTH_STRATEGY duplicated by tasks/auth-session-strategy).
