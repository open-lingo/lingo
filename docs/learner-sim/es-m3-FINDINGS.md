# ES m3 — learner-sim findings (4 walks, 2026-08-24)

Four Opus fresh-learner walks (confusion / retention / ease-flow / wildcard)
of `es-m3.md` (the IR-compiled module, pre-human-walk). Read this before
touching the module. Fix round applied same session — each item below is
tagged with its resolution.

## Headline numbers (retention walk)

Next-day: ~10 items cold-producible (hay ~32 retrievals "unshakeable",
aquí, ¿qué es?, la casa, el libro, la/una llave, una silla, en, the es-un/una
frame, the article rule for -o/-a nouns). Recognize-only: celular, ventana,
lápiz/pluma (mutually confusable, tiles-only), papel ("coin-flip the
article"), dinero, puerta ("abandoned for six lessons"). GONE: él/ella,
no entiendo, eres, most m1 numbers — "m3's retention win is paid for out
of m2's."

## BLOCKERS (all four walks converged)

1. **Negation never taught, modeled 3 inconsistent ways, then graded.**
   «no hay dinero» (L5 sim) vs «no hay UN celular aquí» (L6 sim) vs bare
   «no hay» (L9), then L10's build dangles the «un» tile as a trap the
   module itself set. → FIXED: L6 opens on a one-line «no hay» card
   (drop the article), L6 t2 re-modeled article-less, card cashes in the
   sim two steps later; L10's trap is now fair.
2. **L5.9 "m2 check — the YOU-form" has two right answers** (eres AND
   te llamas), plus a leaked internal label. → FIXED: replaced with a
   single-answer «no entiendo» retrieval (also revives a dying m2 item).
3. **🚪 means «la puerta» and «hasta luego» 30 seconds apart in L1**
   (m1 chose 🚪 for hasta luego before puerta existed). → FIXED in m3
   (distractor swapped). COURSE DEBT: m1's hasta-luego emoji collides
   with puerta forever — needs an m1 art decision (Spencer).
4. **Checkpoint = wall of the same button**: 6 two-option article clozes,
   several byte-identical to earlier steps; projected quit point. →
   FIXED: fresh-sentence clozes, la/el rebalanced, free-point ear trials
   made confusable, count build upgraded.
5. **Debut→immediate-same-word cloze is hollow** (answer on screen 4s
   earlier; the L1 card literally prints "EL libro, LA casa" above the
   first cloze). → FIXED: clozes pushed ≥2 steps from their debut and
   the first trials moved to words the card does NOT print.
6. **L9 fizzles**: the visit lands at step 4, then six chores; module's
   emotional finale is a rote speak. Flow law from PM(18) ("integration
   lessons end on their big sim") now enforced. → FIXED: L9 reordered,
   sim is the finale; learner now ASKS «¿qué es?» (t2 flipped — the
   module's promise, finally cashed in the learner's own mouth).
7. **The L1 card oversells the rule** (maestro/maestra analogy implies
   choosable gender; -o/-a implied universal; llave/papel/celular then
   "cheat"). → FIXED: card rewritten honest (endings usually tell you;
   some words you learn WITH their la/el — that's why they always wear
   it), tint legend added at first map reveal.

## CONFUSING (fixed unless noted)

- «en» used twice before its L5 card (L4 distractor + cloze stem) → both removed from L4.
- pink·f/blue·m tint legend never explained → legend line in L1 map revealNote ("about the word, not the person — Ana stays untinted").
- de = "of/from" never bridged; "is of Ana" broken-English gloss → revealNote bridges to «soy de México», later steps gloss "Ana's".
- Plural -s: revealNote said it but too quietly → one line added to the L4 hay card.
- Diego's wallet scene: whose "here"? → re-staged (pats every pocket at the counter; you check yours).
- María recast (m2 bus stranger, not "from the café") → scene corrected.
- la:el trials skewed 6:4 ("always la" scores 60%) → rebalanced.
- puerta/dinero/papel taught-then-abandoned → tails added (printed voicing → later recall for puerta; dinero re-enters L6 card + L10).
- «¿qué es?» dark L5–L7 and never asked BY the learner → L7 match touch + L9 t2 flip.
- Persona: «yo soy de México» put in Sam's mouth → now Estados Unidos.
- L10 had no consolidation match → module match added before the María sim.

## NITs actioned
"a paper" → "a sheet of paper"; grammar-label titles → promises ("Never
learn a side twice", "Say what you see", "Put things in their place");
silla gets the ll=llamo hint + celular the soft-c callback; 2-chip
«¿qué es?» map → listening beat; checkpoint ear trials made genuinely
confusable (aquí/hay/y; tardes/noches/días); numbers 5 and 7 given real
sentences (cinco/siete); L7 card wording honest.

## NOT actioned (deliberate, with owners)
- **9 of 10 lessons end on a speaking step; on a train those get skipped**
  → this is the R9 engine backlog (speaking "later" affordance), still
  the #1 owed engine item — content can't fix it.
- **Review-tail MCQs feel "parachuted"** → they ARE the §13.9 law-1 tails;
  accepted cost, revisit if Spencer's walk agrees.
- **ocho/nueve/diez still unspent; mesa, ¿dónde está?, mi/tu, tengo
  missing** → m4 scope, recorded in the IR header (m4 = location/
  possession is the natural next contrast; the walks independently asked
  for it).
- **eres has no m3 trial** → left to m2 SRS; flag for the m4 tails.
- **hasta-luego 🚪 atom art collision (course-level)** → Spencer call.

## Confirmation walk (1 Opus fresh-learner, post-fix-round)

Verdicts on the seven named defects: negation FIXED ("the standout" — the
L10 un-tile is now "a fair trap on a rule I was actually taught"); L9
finale + learner-asks FIXED ("the first time in three modules I got to
ask instead of answer"); en / 🚪 / YOU-form all FIXED. PARTIAL/NOT-FIXED
items, resolved in round 2 same session:

- **Tint legend "NOT-FIXED"** — the revealNote legend was too buried to
  register from the learner seat. → moved ONTO the L1 card ("la-words
  glow pink (f), el-words blue (m) — it's about the WORD, not the
  person"), revealNote kept as the echo.
- **Hollow echo migrated into map→build same-sentence pairs** (L4, L6,
  L9 — "I solved all three off short-term memory"). → L4's build moved
  behind the sim; L6 and L9 builds now TRANSFER the frame to a different
  noun («la silla es de María», «hay un lápiz aquí» — the latter
  foreshadows the visit's pencil). Doctrine note: §13.3's "build on the
  second encounter" should read *spaced or transferred*, never
  map→build verbatim at i+2 — carried to the guide when §13.3 is next
  touched.
- **Checkpoint still cloze-heavy (5/15 = 33%)** → s1 became a listening
  comp, s7 a listening build (new modality in the cp); cloze share now
  3/15 = 20%, la/el/un/una spread rebalanced (3/4/3/4 module-wide).
- **Repeated aquí ear trial L5+L8** → cp trial now retests ventana
  (the walk's own "recognize-only" item) against puerta/casa.
- **pluma/ventana/papel never spoken** → printed speaks added in
  L3/L4/L6 (also future recall licenses).
- **L10 scene emoji 🚪 read as a vocab answer** → 🔔.
- **"L9 T4 two right answers" is a FALSE ALARM** — «hasta luego» is in
  alsoCorrectOptionIds (max-acceptance); acceptance is invisible in the
  stripped learner view. Same for T1's «bien». No change.
- Kept as-is: café in the visit (glossed NPC humor, house-allowed);
  «no hay» heard once before its card (the walker: "encounter-then-
  explain helped"); f/m letters (now defined on the L1 card).

## m4-wave addendum (2026-08-25)
The learner-view emitter could not render `listening_build` (m3's round-2
cp step + five m4 steps showed "UNRENDERED" to walkers; a case-sensitive
sanity grep hid it). Emitter taught the type; both views re-emitted clean.
